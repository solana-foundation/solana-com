---
title: Быстрая установка
seoTitle: Установите Solana CLI и Anchor одной командой
description:
  Краткое руководство по установке, которое позволяет настроить локальную среду
  разработки Solana с помощью одной команды. Установите Rust, Solana CLI и
  Anchor Framework на Windows (WSL), Linux и Mac.
---

Быстрая установка для настройки локальной среды разработки Solana с помощью
одной команды. Установите Rust, Solana CLI и Anchor Framework на Windows (WSL),
Linux и Mac. (Если предпочитаете, вы можете
[установить каждую зависимость отдельно](/docs/intro/installation/dependencies).)

## Предварительные требования

<Accordions>
<Accordion title="Windows">

Для разработки программ Solana на Windows вы **должны** использовать
[Подсистему Windows для Linux](https://learn.microsoft.com/en-us/windows/wsl/install)
(WSL). После установки WSL все остальные зависимости будут установлены через
терминал Linux.

Чтобы установить WSL, выполните следующую команду в Windows PowerShell:

```terminal
$ wsl --install
```

В процессе установки вам будет предложено создать учетную запись пользователя по
умолчанию.

![Установка WSL](/assets/docs/intro/installation/wsl-install.png)

По умолчанию WSL устанавливает Ubuntu. Вы можете открыть терминал Linux, найдя
"Ubuntu" в строке поиска.

![WSL Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-search.png)

Если ваш терминал Ubuntu выглядит как на изображении ниже, вы можете столкнуться
с проблемой, когда `ctrl + v` (сочетание клавиш для вставки) не работает в
терминале.

![Терминал Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-terminal-1.png)

Если вы столкнулись с этой проблемой, откройте Windows Terminal, найдя
"Terminal" в строке поиска.

![Терминал Windows](/assets/docs/intro/installation/wsl-windows-terminal.png)

Затем закройте Windows Terminal и снова откройте терминал Linux, найдя Ubuntu.
Терминал теперь должен выглядеть как на изображении ниже, где `ctrl + v`
(сочетание клавиш для вставки) работает.

![Терминал Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-terminal-2.png)

Если вы используете VS Code, то
[расширение WSL](https://code.visualstudio.com/docs/remote/wsl-tutorial)
позволяет использовать WSL и VS Code вместе.

![Настройка WSL в VS Code](/assets/docs/intro/installation/wsl-vscode.png)

Вы увидите следующее на строке состояния VS Code:

![WSL: Ubuntu](/assets/docs/intro/installation/wsl-vscode-ubuntu.png)

После настройки WSL вы готовы либо:

- Выполнить [быструю установку](#installation) с использованием терминала Ubuntu
  (Linux)
- Установить [Rust](/docs/intro/installation/dependencies#install-rust),
  [Solana CLI](/docs/intro/installation/dependencies#install-solana-cli) и
  [Anchor CLI](/docs/intro/installation/dependencies#install-anchor-cli) по
  отдельности, как описано ниже

</Accordion>
<Accordion title="Linux">

<Tabs items={[".deb", ".rpm"]}>
<Tab value=".deb" >
Если вы используете Debian или его производные, такие как Ubuntu или Mint, выполните следующие шаги:

1. Обновите списки пакетов, чтобы убедиться, что все пакеты актуальны:

```terminal
$ sudo apt-get update
```

2. Установите следующие зависимости:

```terminal
$ sudo apt-get install -y \
    build-essential \
    pkg-config \
    libudev-dev llvm libclang-dev \
    protobuf-compiler libssl-dev
```

<Callout>
Если вы столкнулись с ошибкой при установке `protobuf-compiler`, убедитесь, что вы сначала выполнили _shell`sudo apt-get update`

```
Package protobuf-compiler is not available, but is referred to by another package.
This may mean that the package is missing, has been obsoleted, or
is only available from another source
```

</Callout>

  </Tab>

    <Tab value=".rpm" >
    Для производных RHEL, таких как Fedora или Red Hat Linux, выполните следующие шаги.

Обновите списки пакетов, чтобы получить более новые версии пакетов, выполнив
команду.

```terminal
$ sudo dnf upgrade --refresh
```

Затем установите следующие зависимости:

```terminal
$ sudo dnf group install c-development development-tools \
&& sudo dnf install pkg-config openssl-devel
```

  </Tab>
</Tabs>

</Accordion>
</Accordions>

## Установка

1. Введите следующую команду в терминал, чтобы установить все необходимые
   зависимости:

```terminal
$ curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
```

Успешная установка вернет вывод, похожий на следующий:

```
Installed Versions:
Rust: rustc 1.86.0 (05f9846f8 2025-03-31)
Solana CLI: solana-cli 2.2.12 (src:0315eb6a; feat:1522022101, client:Agave)
Anchor CLI: anchor-cli 0.31.1
Node.js: v23.11.0
Yarn: 1.22.1
```

2. Убедитесь в успешной установке, проверив версию каждой установленной
   зависимости.

```terminal
$ rustc --version && solana --version && anchor --version && node --version && yarn --version
```

<Callout>
  Если команда быстрой установки не сработала, обратитесь к разделу [Установка
  зависимостей](/docs/intro/installation/dependencies) ниже для инструкций по
  установке каждой зависимости отдельно.
</Callout>

### Следующие шаги

После установки всех необходимых зависимостей вы готовы ознакомиться с
[Основами Solana CLI](/docs/intro/installation/solana-cli-basics) и
[Основами Anchor CLI](/docs/intro/installation/anchor-cli-basics).
