---
title: Instalação rápida
seoTitle: Instale a CLI da Solana e o Anchor com um único comando
description:
  Um guia de instalação rápida que permite configurar seu ambiente de
  desenvolvimento local da Solana com um único comando. Instale Rust, a CLI da
  Solana e o Framework Anchor no Windows (WSL), Linux e Mac.
---

Uma instalação rápida para configurar seu ambiente de desenvolvimento local da
Solana com um único comando. Instale Rust, a CLI da Solana e o Framework Anchor
no Windows (WSL), Linux e Mac. (Se preferir, você pode
[instalar cada dependência individualmente](/docs/intro/installation/dependencies).)

## Pré-requisitos

<Accordions>
<Accordion title="Windows">

Para desenvolver programas Solana no Windows, você **deve** usar o
[Windows Subsystem for Linux](https://learn.microsoft.com/en-us/windows/wsl/install)
(WSL). Após instalar o WSL, todas as outras dependências serão instaladas
através do terminal Linux.

Para instalar o WSL, execute o seguinte comando no Windows PowerShell:

```terminal
$ wsl --install
```

O processo de instalação solicitará que você crie uma conta de usuário padrão.

![Instalação do WSL](/assets/docs/intro/installation/wsl-install.png)

Por padrão, o WSL instala o Ubuntu. Você pode abrir um terminal Linux
pesquisando "Ubuntu" na barra de pesquisa.

![WSL Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-search.png)

Se o seu terminal Ubuntu parecer com a imagem abaixo, você pode encontrar um
problema onde `ctrl + v` (atalho de teclado para colar) não funciona no
terminal.

![Terminal Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-terminal-1.png)

Se você encontrar esse problema, abra o Windows Terminal pesquisando por
"Terminal" na barra de pesquisa.

![Windows Terminal](/assets/docs/intro/installation/wsl-windows-terminal.png)

Em seguida, feche o Windows Terminal e reabra um terminal Linux pesquisando por
Ubuntu novamente. O terminal agora deve parecer com a imagem abaixo, onde
`ctrl + v` (atalho de teclado para colar) funciona.

![Terminal Ubuntu](/assets/docs/intro/installation/wsl-ubuntu-terminal-2.png)

Se você usa o VS Code, a
[extensão WSL](https://code.visualstudio.com/docs/remote/wsl-tutorial) permite
que você use o WSL e o VS Code juntos.

![Configuração do WSL no VS Code](/assets/docs/intro/installation/wsl-vscode.png)

Você verá o seguinte na barra de status do VS Code:

![WSL: Ubuntu](/assets/docs/intro/installation/wsl-vscode-ubuntu.png)

Depois de configurar o WSL, você estará pronto para:

- Completar a [instalação rápida](#installation) usando o terminal do Ubuntu
  (Linux)
- Instalar o [Rust](/docs/intro/installation/dependencies#install-rust), a
  [CLI da Solana](/docs/intro/installation/dependencies#install-solana-cli) e a
  [CLI do Anchor](/docs/intro/installation/dependencies#install-anchor-cli)
  individualmente, conforme descrito abaixo

</Accordion>
<Accordion title="Linux">

<Tabs items={[".deb", ".rpm"]}>
<Tab value=".deb" >
Se estiver usando Debian ou alternativas derivadas do Debian como Ubuntu ou Mint, siga os passos abaixo:

1. Atualize as listas de pacotes para garantir que todos os pacotes estejam
   atualizados:

```terminal
$ sudo apt-get update
```

2. Instale as seguintes dependências:

```terminal
$ sudo apt-get install -y \
    build-essential \
    pkg-config \
    libudev-dev llvm libclang-dev \
    protobuf-compiler libssl-dev
```

<Callout>
Se você encontrar o seguinte erro ao instalar `protobuf-compiler`, certifique-se
de executar primeiro _shell`sudo apt-get update`

```
Package protobuf-compiler is not available, but is referred to by another package.
This may mean that the package is missing, has been obsoleted, or
is only available from another source
```

</Callout>

  </Tab>

    <Tab value=".rpm" >
    Para derivados do RHEL como Fedora ou Red Hat Linux, siga os passos a seguir.

Atualize as listas de pacotes para obter as versões mais recentes dos pacotes
executando o comando.

```terminal
$ sudo dnf upgrade --refresh
```

Em seguida, instale as seguintes dependências:

```terminal
$ sudo dnf group install c-development development-tools \
&& sudo dnf install pkg-config openssl-devel
```

  </Tab>
</Tabs>

</Accordion>
</Accordions>

## Instalação

1. Digite o seguinte comando no seu terminal para instalar todas as dependências
   necessárias:

```terminal
$ curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
```

Uma instalação bem-sucedida retornará uma saída como a seguinte:

```
Installed Versions:
Rust: rustc 1.86.0 (05f9846f8 2025-03-31)
Solana CLI: solana-cli 2.2.12 (src:0315eb6a; feat:1522022101, client:Agave)
Anchor CLI: anchor-cli 0.31.1
Node.js: v23.11.0
Yarn: 1.22.1
```

2. Verifique se a instalação foi bem-sucedida verificando a versão de cada
   dependência instalada.

```terminal
$ rustc --version && solana --version && anchor --version && node --version && yarn --version
```

<Callout>
  Se o comando de instalação rápida falhar, consulte a seção [Instalar
  Dependências](/docs/intro/installation/dependencies) abaixo para obter
  instruções sobre como instalar cada dependência individualmente.
</Callout>

### Próximos passos

Depois de instalar todas as dependências necessárias, você está pronto para
revisar os
[Conceitos Básicos da CLI Solana](/docs/intro/installation/solana-cli-basics) e
[Conceitos Básicos da CLI Anchor](/docs/intro/installation/anchor-cli-basics).
