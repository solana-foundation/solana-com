---
title: getSlotLeader
hideTableOfContents: true
h1: Μέθοδος RPC getSlotLeader
---

Επιστρέφει τον τρέχοντα ηγέτη του slot

<APIMethod>

```jsonc !!request curl
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getSlotLeader",
  "params": [
    // !hover(1:3) 0
    {
      // !hover commitment
      "commitment": "finalized"
    }
  ]
}
```

```ts !!request title="Kit"
import { createSolanaRpc } from "@solana/kit";

const rpc_url = "https://api.devnet.solana.com";
const rpc = createSolanaRpc(rpc_url);

let slotLeader = await rpc.getSlotLeader().send();

console.log(slotLeader);
```

```ts !!request title="web3.js"
import { Connection, clusterApiUrl } from "@solana/web3.js";

const connection = new Connection(clusterApiUrl("devnet"), "confirmed");

let slotLeader = await connection.getSlotLeader();

console.log(slotLeader);
```

### !params

#### !! 0

!type object

Αντικείμενο διαμόρφωσης που περιέχει τα ακόλουθα πεδία:

##### !! commitment

!type string

Το commitment περιγράφει πόσο οριστικοποιημένο είναι ένα μπλοκ σε εκείνο το
χρονικό σημείο. Δείτε
[Διαμόρφωση Δέσμευσης Κατάστασης](/docs/rpc#configuring-state-commitment).

##### !! minContextSlot

!type number

Το ελάχιστο slot στο οποίο μπορεί να αξιολογηθεί το αίτημα

### !!result

```jsonc !response
{
  "jsonrpc": "2.0",
  // !hover result
  "result": "ENvAW7JScgYq6o4zKZwewtkzzJgDzuJAFxYasvmEQdpS",
  "id": 1
}
```

!type string

Ταυτότητα κόμβου Pubkey ως συμβολοσειρά κωδικοποιημένη σε base-58

</APIMethod>
