---
title: Agentowe płatności
description:
  Umożliw agentom AI autonomiczne płacenie za usługi, API i zasoby przy użyciu
  protokołu x402.
---

Agenci AI stają się coraz bardziej autonomiczni w swoich działaniach: piszą kod,
pobierają dane, przydzielają zasoby i nie tylko. Aby działać całkowicie
niezależnie, potrzebują sposobu na dokonywanie „mikropłatności” za usługi w
sposób programistyczny, w czasie rzeczywistym i przy minimalnych kosztach.
Tradycyjne systemy płatności nie sprawdzają się w przypadku mikropłatności.
Opłaty poniżej centa i finalność transakcji w czasie poniżej sekundy w sieci
Solana sprawiają, że jest to technicznie i ekonomicznie możliwe.

## Dlaczego to jest ważne?

Wyobraź sobie na przykład, że deweloper korzystający z LLM do budowy aplikacji
potrzebuje danych z płatnego API. Aby pobrać ważne dane, deweloper musiałby
przerwać swoją pracę, poszukać API, wybrać dostawcę, założyć konto, dodać metodę
płatności, wygenerować klucz API, skopiować go do swojego środowiska, a
następnie wrócić do pracy. Taka bariera przerywa stan skupienia. W przypadku
workflow wspieranych przez AI to poważna przeszkoda.

Zamiast tego, dzięki agentowym płatnościom, agent może złożyć żądanie, zapłacić
za nie i kontynuować pracę. Bez kont. Bez kluczy API. Bez ręcznej ingerencji.

### Płać za API za pomocą pay.sh

[pay.sh](https://pay.sh) to warstwa płatności dla agentów HTTP i narzędzi
wiersza poleceń. Zapoznaj się z [dokumentacją pay.sh](https://pay.sh/docs), aby
zainstalować `pay`. Interfejs wiersza poleceń `pay` opakowuje narzędzia takie
jak `curl`, `codex` i `claude`, dzięki czemu mogą wywoływać płatne API bez kont
dostawców lub kluczy API. Gdy API zwraca wyzwanie MPP lub x402 z kodem
`402 Payment Required`, `pay` prosi lokalny portfel o zatwierdzenie podpisania.

Możesz zainstalować Pay za pomocą:

```bash
brew install pay
```

lub

```bash
npm install -g @solana/pay
```

Zweryfikuj instalację poleceniem:

```bash
pay --version
```

Zestaw narzędzi automatycznie skonfiguruje Twój adres. Uruchom poniższe
polecenie, aby skonfigurować i zasilić swój portfel:

```bash
pay setup
```

## Protokół x402

Płatności agentyczne wymagają mechanizmu, który umożliwi klientom i serwerom
negocjowanie warunków płatności za pośrednictwem standardowej infrastruktury
webowej bez zakłócania tradycyjnych interfejsów sieciowych. Przestrzeń
mikropłatności jest wciąż w powijakach i wraz z jej rozwojem prawdopodobnie
pojawią się nowe narzędzia i standardy. [Protokół x402](https://www.x402.org/)
wyłonił się jednak jako wczesny standard z silnym wsparciem ekosystemu. x402
wykorzystuje
[kod statusu HTTP 402 „Payment Required“](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/402)
— standard obecny od HTTP/1.1, który stał się praktyczny dopiero dzięki
rozliczeniom blockchain.

### Jak to działa

Klient wysyła żądanie, otrzymuje odpowiedź 402 z warunkami płatności, a
następnie ponawia żądanie z podpisaną płatnością. Serwer deleguje weryfikację i
rozliczenie do facylitatora — opcjonalnego pośrednika obsługującego przesyłanie
transakcji onchain. Gdy facylitator potwierdzi rozliczenie, serwer zwraca żądaną
treść. Takie rozdzielenie pozwala dostawcom API przyjmować płatności bez
bezpośredniego zarządzania jakąkolwiek infrastrukturą onchain.

![Diagram przepływu x402](/assets/guides/x402/x402-flow-solana.png)

Protokół jest bezstanowy (brak sesji i kluczy API), współpracuje ze standardową
infrastrukturą HTTP i obsługuje dowolny token SPL akceptowany przez serwer. Oto
fragment kodu przedstawiający konfigurację serwera Express z wykorzystaniem
middleware x402:

```ts
app.use(
  paymentMiddleware(
    {
      "GET /costly-data": {
        accepts: [
          {
            scheme: "exact",
            price: "$0.001",
            network: "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
            payTo: svmAddress
          }
        ],
        description: "Costly data",
        mimeType: "application/json"
      }
    },
    new x402ResourceServer(facilitatorClient).register(
      "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
      new ExactSvmScheme()
    )
  )
);

app.get("/costly-data", (req, res) => {
  res.send({
    report: {
      data: "costly data"
    }
  });
});
```

Aby dowiedzieć się więcej o x402, zapoznaj się z naszymi przewodnikami:

<Cards>
  <Card
    title="Wprowadzenie do x402"
    href="/developers/guides/getstarted/intro-to-x402"
    description="Podstawy protokołu i opcje SDK do budowania API z obsługą płatności."
  />
  <Card
    title="Buduj z Kora"
    href="/developers/guides/getstarted/build-a-x402-facilitator"
    description="Implementuj bezgasowe płatności x402 przy użyciu infrastruktury podpisywania Kora."
  />
</Cards>

### Narzędzia x402

| SDK                                                             | Opis                                 |
| --------------------------------------------------------------- | ------------------------------------ |
| [Corbits](https://corbits.dev/)                                 | Implementacja x402 oparta na Solana  |
| [MCPay.tech](https://mcpay.tech/)                               | Płatność za żądanie dla serwerów MCP |
| [PayAI](https://payai.network/)                                 | Fasilitator x402 z obsługą Solana    |
| [x402 GitHub](https://github.com/coinbase/x402)                 | Implementacja referencyjna           |
| [ACK](https://github.com/agentcommercekit/ack)                  | Agent Commerce Kit                   |
| [A2A x402](https://github.com/google-agentic-commerce/a2a-x402) | Płatności agent-do-agenta Google     |
