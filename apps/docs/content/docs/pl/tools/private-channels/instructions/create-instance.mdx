---
title: CreateInstance
description: Tworzy PDA Instance, który kotwi escrow Private Channels
---

Tworzy PDA `Instance`, który kotwi escrow. Musi być wywołany przed jakąkolwiek
inną operacją.

## Seeds PDA

```
instance        -> ["instance", instance_seed: Pubkey]
allowed_mint    -> ["allowed_mint", instance_pda: Pubkey, mint: Pubkey]
operator        -> ["operator", instance_pda: Pubkey, operator: Pubkey]
event_authority -> ["event_authority"]
```

## Konta

| Konto                          | Zapisywalne | Podpisujący | Opis                                                                 |
| -------------------------------- | -------- | ------ | --------------------------------------------------------------------------- |
| `payer`                          | ✓        | ✓      | Płatnik opłat i rent                                                    |
| `admin`                          |          | ✓      | Początkowy administrator instancji                                               |
| `instance_seed`                  |          | ✓      | Jednorazowy keypair używany wyłącznie do wyprowadzania PDA; jego pubkey staje się seed |
| `instance`                       | ✓        |        | PDA Instance (wyprowadzony z `["instance", instance_seed.pubkey]`)            |
| `system_program`                 |          |        | System Program                                                              |
| `event_authority`                |          |        | PDA authority zdarzeń                                                         |
| `private_channel_escrow_program` |          |        | Program Escrow                                                              |

## Parametry

| Parametr | Typ | Opis                                                                             |
| --------- | ---- | --------------------------------------------------------------------------------------- |
| `bump`    | `u8` | Bump seed dla PDA instancji. Opcjonalny: klient `Async` wyprowadza go automatycznie, jeśli zostanie pominięty |

> IDL zawiera argument `discriminator: u8` (wartość `0`), który jest automatycznie ustawiany
> przez wygenerowanego klienta i nie powinien być przekazywany ręcznie.

## Ważne uwagi

- `instance_seed` to jednorazowy keypair, używany wyłącznie do wyprowadzenia PDA `Instance`.
  Jego pubkey staje się seed; sam keypair nie jest przechowywany ani ponownie używany.
- Używaj `getCreateInstanceInstructionAsync`; automatycznie wyprowadza `bump` i `instance`
  z `instance_seed`, gdy nie są podane. Przekazuj własny `bump` tylko wtedy, gdy
  już go obliczyłeś za pomocą
  `Pubkey::find_program_address(&[b"instance", instance_seed_pubkey.as_ref()], &PROGRAM_ID)`
- Emituje zdarzenie za pomocą CPI `EmitEvent`
