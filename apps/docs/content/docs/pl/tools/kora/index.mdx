---
title: Kora
description: Infrastruktura podpisywania Solana
---

**Kora to Twoja infrastruktura podpisywania Solana.** Umożliwiaj transakcje bez
opłat za gaz, w których użytkownicy płacą prowizje w dowolnym tokenie—USDC, BONK
lub natywnym tokenie Twojej aplikacji—lub obsługuj dowolne podpisywanie
transakcji wymagające zaufanego podpisującego.

### Dlaczego Kora?

- **Lepsze UX**: Użytkownicy nigdy nie potrzebują SOL
- **Kontrola przychodów**: Zbieraj prowizje w USDC, swoim tokenie lub
  czymkolwiek innym
- **Gotowe do produkcji**: Bezpieczna walidacja, ograniczanie liczby żądań,
  wbudowany monitoring
- **Łatwa integracja**: API JSON-RPC + SDK TypeScript
- **Elastyczne wdrożenie**: Railway, Docker lub dowolna platforma chmurowa

### Architektura

- **Język**: Rust z SDK TypeScript
- **Protokół**: JSON-RPC 2.0
- **Podpisujący**: Solana Private Key, Turnkey, Privy
- **Uwierzytelnianie**: Klucz API, HMAC lub brak
- **Wdrożenie**: Elastyczne opcje wdrożenia (Docker, Railway itp.)

### Funkcje

- Konfigurowalne reguły walidacji i listy dozwolonych
- Pełne wsparcie Token-2022 z filtrowaniem rozszerzeń
- Buforowanie Redis dla lepszej wydajności
- Ograniczanie liczby żądań i ochrona przed nadmiernymi wydatkami
- Bezpieczne zarządzanie kluczami (Turnkey, Privy, Vault)
- Uwierzytelnianie HMAC i kluczem API
- Metryki Prometheus i monitoring
- Ulepszone zasady ochrony płatnika opłat

## Szybki start

Zainstaluj Kora:

```bash
cargo install kora-cli
```

Podstawowe użycie:

```bash
kora rpc [OPTIONS] # --help for full list of options
```

**[→ Przewodnik szybkiego startu](/docs/tools/kora/getting-started/quick-start)** -
Uruchom Kora lokalnie w kilka minut

**[→ Przewodnik operatora węzła](/docs/tools/kora/operators)** - Uruchom
paymaster

## SDK TypeScript

Kora udostępnia prosty interfejs JSON-RPC z trzema opcjami klienta:

```typescript
// Option 1: Standalone client
import { KoraClient } from "@solana/kora";
const kora = new KoraClient({ rpcUrl: "http://localhost:8080" });
const signed = await kora.signTransaction({ transaction });

// Option 2: Kit client (recommended) — handles planning, fees, signing, and sending
import { createKitKoraClient } from "@solana/kora";
import { address } from "@solana/kit";
const client = await createKitKoraClient({
  endpoint: "http://localhost:8080",
  rpcUrl: "https://api.mainnet-beta.solana.com",
  feeToken: address("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"),
  feePayerWallet: userSigner
});
```

**[→ Dokumentacja API](/docs/tools/kora/json-rpc-api)** - Metody JSON-RPC i SDK
TypeScript

## Rozwój lokalny

### Wymagania wstępne

- Rust 1.86+ lub
- Solana CLI 2.2+
- Node.js 20+ i pnpm (dla SDK)

### Instalacja

```bash
git clone https://github.com/solana-foundation/kora.git
cd kora
git checkout v2.0.5
just install
```

### Budowanie

```bash
just build
```

### Uruchamianie serwera

Podstawowe użycie:

```bash
kora rpc [OPTIONS]
```

Aby uruchomić z konfiguracją testową, wykonaj:

```bash
just run
```

### Testowanie lokalne

I uruchom wszystkie testy:

```bash
just test-all
```

## Społeczność i wsparcie

- **Masz pytania?** Zadaj je na
  [Solana Stack Exchange](https://solana.stackexchange.com/) (użyj tagu `kora`)
- **Napotkałeś problemy?** Zgłoś je w
  [GitHub Issues](https://github.com/solana-foundation/kora/issues)

## Inne zasoby

- [Kora CLI Crates.io](https://crates.io/crates/kora-cli) - Pakiet Rust do
  uruchamiania węzła Kora
- [@solana/kora NPM Package](https://www.npmjs.com/package/@solana/kora) - SDK
  TypeScript do interakcji z węzłem Kora

## Źródło

- [Repozytorium GitHub](https://github.com/solana-foundation/kora)

Zbudowane i utrzymywane przez [Solana Foundation](https://solana.org).

Licencjonowane na warunkach MIT. Zobacz
[LICENSE](https://github.com/solana-foundation/kora/blob/main/LICENSE.md), aby
poznać szczegóły.
