---
title: Implantação de Programa
description: Como implantar e chamar um programa simples
---

## Teste Completo

```rust title="tests/program_test.rs"
use litesvm::LiteSVM;
use solana_sdk::{
    instruction::{AccountMeta, Instruction},
    pubkey::Pubkey,
    signature::{Keypair, Signer},
    transaction::Transaction,
};

#[test]
fn test_program_deployment() {
    let mut svm = LiteSVM::new();

    // Deploy the program
    let program_id = Pubkey::new_unique();
    let program_bytes = include_bytes!("../target/deploy/hello_world.so");
    svm.add_program(program_id, program_bytes).unwrap();

    // Create payer account
    let payer = Keypair::new();
    svm.airdrop(&payer.pubkey(), 10_000_000_000).unwrap();

    // Create instruction to call program
    let instruction = Instruction {
        program_id,
        accounts: vec![
            AccountMeta::new(payer.pubkey(), true),
        ],
        data: vec![], // Program-specific data
    };

    // Send transaction
    let tx = Transaction::new_signed_with_payer(
        &[instruction],
        Some(&payer.pubkey()),
        &[&payer],
        svm.latest_blockhash(),
    );

    let result = svm.send_transaction(tx).unwrap();

    // Verify program was called
    assert!(result.logs.iter().any(|log|
        log.contains(&format!("Program {} invoke", program_id))
    ));

    println!("Program called successfully!");
    println!("Logs:\n{}", result.pretty_logs());
}
```

## Pontos-chave

1. **Implantação de Programa**: Use `add_program()` para implantar um programa a
   partir de bytes
2. **include_bytes!**: Incorpora o programa compilado em tempo de compilação
3. **ID do Programa**: Use o keypair de `target/deploy/`
4. **Instruções**: Crie instruções personalizadas com ID do programa, contas e
   dados
5. **Verificação de Logs**: Verifique os logs de transação para confirmar a
   execução do programa

Para informações mais detalhadas sobre implantação, leia
[esta seção](/docs/tools/litesvm/testing-your-program/deploy).
