---
title: Funções de Operador
description:
  Funções de operador da biblioteca padrão para operações matemáticas e lógicas
---

## and_bool

`and_bool` retorna o AND binário dos argumentos do lado esquerdo e do lado
direito.

### Entradas

| Nome  | Tipo | Descrição         |
| ----- | ---- | ----------------- |
| `lhs` | bool | Um valor booleano |
| `rhs` | bool | Um valor booleano |

### Saída

| Nome    | Tipo | Descrição                                              |
| ------- | ---- | ------------------------------------------------------ |
| `value` | bool | O resultado de um AND binário entre os dois argumentos |

```hcl
output "my_bool" {
  value = false && true
}
// > my_bool: false
```

---

## or_bool

`or_bool` retorna o OR binário dos argumentos do lado esquerdo e do lado
direito.

### Entradas

| Nome  | Tipo | Descrição         |
| ----- | ---- | ----------------- |
| `lhs` | bool | Um valor booleano |
| `rhs` | bool | Um valor booleano |

### Saída

| Nome    | Tipo | Descrição                                             |
| ------- | ---- | ----------------------------------------------------- |
| `value` | bool | O resultado de um OR binário entre os dois argumentos |

```hcl
output "my_bool" {
  value = false || true
}
// > my_bool: true
```

---

## div

`div` retorna a divisão inteira do argumento do lado esquerdo pelo argumento do
lado direito, arredondando qualquer resto para o inteiro mais próximo abaixo.

### Entradas

| Nome  | Tipo    | Descrição   |
| ----- | ------- | ----------- |
| `lhs` | inteiro | O dividendo |
| `rhs` | inteiro | O divisor   |

### Saída

| Nome    | Tipo    | Descrição                                        |
| ------- | ------- | ------------------------------------------------ |
| `value` | inteiro | O resultado da divisão do dividendo pelo divisor |

```hcl
output "my_int" {
  value = 11 / -3
}
// > my_int: -3
```

---

## eq

`eq` retorna `true` se os argumentos do lado esquerdo e do lado direito forem
iguais e `false` se não forem.

### Entradas

| Nome  | Tipo | Descrição      |
| ----- | ---- | -------------- |
| `lhs` | any  | Qualquer valor |
| `rhs` | any  | Qualquer valor |

### Saída

| Nome    | Tipo | Descrição                                                          |
| ------- | ---- | ------------------------------------------------------------------ |
| `value` | bool | O resultado de uma verificação de igualdade entre as duas entradas |

```hcl
output "is_eq" {
  value = "arg" == "arg"
}
// > is_eq: true
```

---

## gt

`gt` retorna `true` se o argumento do lado esquerdo for maior que o argumento do
lado direito.

### Entradas

| Nome  | Tipo                       | Descrição           |
| ----- | -------------------------- | ------------------- |
| `lhs` | integer \| float \| string | Um valor comparável |
| `rhs` | integer \| float \| string | Um valor comparável |

### Saída

| Nome    | Tipo | Descrição                                                                                          |
| ------- | ---- | -------------------------------------------------------------------------------------------------- |
| `value` | bool | O resultado da verificação se o argumento do lado esquerdo é maior que o argumento do lado direito |

```hcl
output "is_gt" {
  value = 2 > 1
}
// > is_gt: true
```

---

## gte

`gte` retorna `true` se o argumento do lado esquerdo for maior ou igual ao
argumento do lado direito.

### Entradas

| Nome  | Tipo                       | Descrição           |
| ----- | -------------------------- | ------------------- |
| `lhs` | integer \| float \| string | Um valor comparável |
| `rhs` | integer \| float \| string | Um valor comparável |

### Saída

| Nome    | Tipo | Descrição                                                                                                |
| ------- | ---- | -------------------------------------------------------------------------------------------------------- |
| `value` | bool | O resultado da verificação se o argumento do lado esquerdo é maior ou igual ao argumento do lado direito |

```hcl
output "is_gte" {
  value = 2 >= 2
}
// > is_gte: true
```

---

## lt

`lt` retorna `true` se o argumento do lado esquerdo for menor que o argumento do
lado direito.

### Entradas

| Nome  | Tipo                       | Descrição           |
| ----- | -------------------------- | ------------------- |
| `lhs` | integer \| float \| string | Um valor comparável |
| `rhs` | integer \| float \| string | Um valor comparável |

### Saída

| Nome    | Tipo | Descrição                                                                                          |
| ------- | ---- | -------------------------------------------------------------------------------------------------- |
| `value` | bool | O resultado da verificação se o argumento do lado esquerdo é menor que o argumento do lado direito |

```hcl
output "is_lt" {
  value = 2 < 1
}
// > is_lt: false
```

---

## lte

`lte` retorna `true` se o argumento do lado esquerdo for menor ou igual ao
argumento do lado direito.

### Entradas

| Nome  | Tipo                       | Descrição           |
| ----- | -------------------------- | ------------------- |
| `lhs` | integer \| float \| string | Um valor comparável |
| `rhs` | integer \| float \| string | Um valor comparável |

### Saída

| Nome    | Tipo | Descrição                                                                                                |
| ------- | ---- | -------------------------------------------------------------------------------------------------------- |
| `value` | bool | O resultado da verificação se o argumento do lado esquerdo é menor ou igual ao argumento do lado direito |

```hcl
output "is_lte" {
  value = 2 <= 2
}
// > is_lte: true
```

---

## neq

`neq` retorna `true` se os argumentos do lado esquerdo e do lado direito não
forem iguais e `false` caso contrário.

### Entradas

| Nome  | Tipo | Descrição                  |
| ----- | ---- | -------------------------- |
| `lhs` | any  | Um valor para comparar     |
| `rhs` | any  | Um valor para comparar com |

### Saída

| Nome    | Tipo | Descrição                                                                 |
| ------- | ---- | ------------------------------------------------------------------------- |
| `value` | bool | O resultado de uma verificação de igualdade negada entre as duas entradas |

```hcl
output "is_neq" {
  value = "arg" != "arg"
}
// > is_neq: false
```

---

## minus

`minus` retorna o resultado da subtração do argumento `integer` do lado direito
pelo argumento `integer` do lado esquerdo.

### Entradas

| Nome  | Tipo    | Descrição    |
| ----- | ------- | ------------ |
| `lhs` | integer | O minuendo   |
| `rhs` | integer | O subtraendo |

### Saída

| Nome    | Tipo    | Descrição                            |
| ------- | ------- | ------------------------------------ |
| `value` | integer | O resultado da operação de subtração |

```hcl
output "my_integer" {
  value = 10 - 6
}
// > my_integer: 4
```

---

## modulo

`modulo` retorna o resto da divisão do argumento `integer` do lado esquerdo pelo
argumento `integer` do lado direito.

### Entradas

| Nome  | Tipo    | Descrição   |
| ----- | ------- | ----------- |
| `lhs` | integer | O dividendo |
| `rhs` | integer | O divisor   |

### Saída

| Nome    | Tipo    | Descrição                      |
| ------- | ------- | ------------------------------ |
| `value` | integer | O resto da operação de divisão |

```hcl
output "my_mod" {
    value = 10 % 3
}
// > my_mod: 1
```

---

## multiply

`multiply` retorna o produto do argumento `integer` do lado esquerdo pelo
argumento `integer` do lado direito.

### Entradas

| Nome  | Tipo    | Descrição           |
| ----- | ------- | ------------------- |
| `lhs` | integer | O primeiro operando |
| `rhs` | integer | O segundo operando  |

### Saída

| Nome    | Tipo    | Descrição                                |
| ------- | ------- | ---------------------------------------- |
| `value` | integer | O resultado da operação de multiplicação |

```hcl
output "my_product" {
    value = 10 * 5
}
// > my_product: 50
```

---

## add

`add` retorna a soma do argumento `integer` do lado esquerdo e do argumento
`integer` do lado direito.

### Entradas

| Nome  | Tipo    | Descrição           |
| ----- | ------- | ------------------- |
| `lhs` | integer | O primeiro operando |
| `rhs` | integer | O segundo operando  |

### Saída

| Nome    | Tipo    | Descrição                         |
| ------- | ------- | --------------------------------- |
| `value` | integer | O resultado da operação de adição |

```hcl
output "my_sum" {
    value = 10 + 5
}
// > my_sum: 15
```

---

## neg_integer

Retorna a negação do inteiro fornecido.

### Entradas

| Nome    | Tipo    | Descrição        |
| ------- | ------- | ---------------- |
| `value` | integer | Um valor inteiro |

### Saída

| Nome    | Tipo    | Descrição              |
| ------- | ------- | ---------------------- |
| `value` | integer | O valor inteiro negado |

---

## not_bool

Retorna a negação lógica do valor booleano fornecido.

### Entradas

| Nome    | Tipo | Descrição         |
| ------- | ---- | ----------------- |
| `value` | bool | Um valor booleano |

### Saída

| Nome    | Tipo | Descrição                                     |
| ------- | ---- | --------------------------------------------- |
| `value` | bool | A negação lógica do valor booleano de entrada |
