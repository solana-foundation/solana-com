---
title: AddOperator
description: Aprovisiona un nuevo operador creando su PDA de Operator
---

Aprovisiona un nuevo operador creando su PDA de `Operator`. Tras esta llamada, la
billetera `operator` puede llamar a `ReleaseFunds` y `ResetSmtRoot`.

## Cuentas

| Cuenta                          | Modificable | Firmante | Descripción                                                         |
| -------------------------------- | -------- | ------ | ------------------------------------------------------------------- |
| `payer`                          | ✓        | ✓      | Pagador de comisiones                                                           |
| `admin`                          |          | ✓      | Administrador de la instancia                                                      |
| `instance`                       |          |        | PDA de la instancia                                                        |
| `operator`                       |          |        | Billetera del operador a aprovisionar                                        |
| `operator_pda`                   | ✓        |        | PDA del operador - derivada automáticamente de `["operator", instance, operator]` |
| `system_program`                 |          |        | System Program                                                      |
| `event_authority`                |          |        | PDA de la autoridad de eventos                                                 |
| `private_channel_escrow_program` |          |        | Programa de custodia (Escrow)                                                      |

## Parámetros

| Parámetro | Tipo | Descripción                                                                               |
| --------- | ---- | ----------------------------------------------------------------------------------------- |
| `bump`    | `u8` | bump seed para la `operator_pda`. Opcional: el cliente `Async` lo deriva automáticamente si se omite |

> El IDL contiene un argumento `discriminator: u8` (valor `3`) que se establece automáticamente
> por el cliente generado y no debe pasarse manualmente.

## Notas importantes

- Utiliza `getAddOperatorInstructionAsync`; deriva automáticamente `bump` y
  `operator_pda` cuando no se proporcionan. También existe una variante síncrona
  `getAddOperatorInstruction` si ya calculaste `bump`
  por tu cuenta
- Tras esta llamada, la billetera `operator` puede llamar a `ReleaseFunds` y
  `ResetSmtRoot`
- Emite un evento a través de CPI `EmitEvent`
