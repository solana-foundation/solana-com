---
title: Configuración
description: Métodos de construcción para configurar LiteSVM en TypeScript
---

# Configuración

LiteSVM utiliza un patrón de construcción para la configuración. Todos los
métodos devuelven `LiteSVM` para encadenamiento.

## Configuración típica de pruebas

```typescript
import { createClient, generateKeyPairSigner } from "@solana/kit";
import { litesvm } from "@solana/kit-plugin-litesvm";
import { signer } from "@solana/kit-plugin-signer";

const mySigner = await generateKeyPairSigner();
const client = createClient().use(signer(mySigner)).use(litesvm());

client.svm
  .withSigverify(false) // Skip signature verification (faster)
  .withBlockhashCheck(false) // Skip blockhash validation (simpler)
  .withSysvars() // Enable Clock, Rent, etc.
  .withBuiltins() // Enable ed25519, secp256k1
  .withTransactionHistory(100n); // Store last 100 transactions
```

## Firma y Blockhash

### withSigverify

```typescript
withSigverify(sigverify: boolean): LiteSVM
```

Habilita o deshabilita la verificación de firmas.

```typescript
// Disable for faster tests (default is enabled)
client.svm.withSigverify(false);

// Enable for production-like testing
client.svm.withSigverify(true);
```

<Callout type="info">
  Deshabilitar la verificación de firmas hace que las pruebas sean más rápidas,
  pero menos realistas.
</Callout>

### withBlockhashCheck

```typescript
withBlockhashCheck(check: boolean): LiteSVM
```

Habilita o deshabilita la verificación de expiración del blockhash.

```typescript
// Disable for simpler tests
client.svm.withBlockhashCheck(false);
```

## Programas y Sysvars

### withSysvars

```typescript
withSysvars(): LiteSVM
```

Habilita las cuentas sysvar (Clock, rent, EpochSchedule, etc.).

```typescript
client.svm.withSysvars();

// Now you can access sysvars
const clock = client.svm.getClock();
const rent = client.svm.getRent();
```

### withBuiltins

```typescript
withBuiltins(): LiteSVM
```

Habilita los programas integrados (verificación de firmas ed25519, secp256k1).

```typescript
client.svm.withBuiltins();
```

### withPrecompiles

```typescript
withPrecompiles(): LiteSVM
```

Habilitar programas precompilados.

```typescript
client.svm.withPrecompiles();
```

### withDefaultPrograms

```typescript
withDefaultPrograms(): LiteSVM
```

Agregar programas predeterminados (System Program, BPF Loader, etc.).

```typescript
client.svm.withDefaultPrograms();
```

## Límites de recursos

### withLamports

```typescript
withLamports(lamports: bigint): LiteSVM
```

Establecer lamports predeterminados para las cuentas.

```typescript
client.svm.withLamports(1_000_000_000n); // 1 SOL
```

### withLogBytesLimit

```typescript
withLogBytesLimit(limit?: bigint): LiteSVM
```

Establecer el límite de bytes para la salida de registros.

```typescript
client.svm.withLogBytesLimit(10000n);
```

### withTransactionHistory

```typescript
withTransactionHistory(capacity: bigint): LiteSVM
```

Habilitar el almacenamiento del historial de transacciones.

```typescript
// Store last 100 transactions
client.svm.withTransactionHistory(100n);
```

## Conjuntos de funcionalidades

### withFeatureSet

```typescript
withFeatureSet(featureSet: FeatureSet): LiteSVM
```

Configurar el conjunto de funcionalidades para probar versiones específicas de
Solana.
