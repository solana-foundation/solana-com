---
title: Despliegue de Programas
description: Cómo desplegar e invocar un programa simple
---

## Prueba Completa

```rust title="tests/program_test.rs"
use litesvm::LiteSVM;
use solana_sdk::{
    instruction::{AccountMeta, Instruction},
    pubkey::Pubkey,
    signature::{Keypair, Signer},
    transaction::Transaction,
};

#[test]
fn test_program_deployment() {
    let mut svm = LiteSVM::new();

    // Deploy the program
    let program_id = Pubkey::new_unique();
    let program_bytes = include_bytes!("../target/deploy/hello_world.so");
    svm.add_program(program_id, program_bytes).unwrap();

    // Create payer account
    let payer = Keypair::new();
    svm.airdrop(&payer.pubkey(), 10_000_000_000).unwrap();

    // Create instruction to call program
    let instruction = Instruction {
        program_id,
        accounts: vec![
            AccountMeta::new(payer.pubkey(), true),
        ],
        data: vec![], // Program-specific data
    };

    // Send transaction
    let tx = Transaction::new_signed_with_payer(
        &[instruction],
        Some(&payer.pubkey()),
        &[&payer],
        svm.latest_blockhash(),
    );

    let result = svm.send_transaction(tx).unwrap();

    // Verify program was called
    assert!(result.logs.iter().any(|log|
        log.contains(&format!("Program {} invoke", program_id))
    ));

    println!("Program called successfully!");
    println!("Logs:\n{}", result.pretty_logs());
}
```

## Puntos Clave

1. **Despliegue de Programas**: Usa `add_program()` para desplegar un programa
   desde bytes
2. **include_bytes!**: Incorpora el programa compilado en tiempo de compilación
3. **ID del Programa**: Usa el keypair de `target/deploy/`
4. **Instrucciones**: Crea instrucciones personalizadas con el ID del programa,
   las cuentas y los datos
5. **Verificación de Logs**: Comprueba los registros de transacciones para
   verificar la ejecución del programa

Para información más detallada sobre el despliegue, consulta
[esta sección](/docs/tools/litesvm/testing-your-program/deploy).
