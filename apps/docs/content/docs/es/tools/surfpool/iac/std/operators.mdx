---
title: Funciones de Operador
description:
  Funciones de operador de la biblioteca estándar para operaciones matemáticas y
  lógicas
---

## and_bool

`and_bool` devuelve el AND binario de los argumentos del lado izquierdo y
derecho.

### Entradas

| Nombre | Tipo | Descripción       |
| ------ | ---- | ----------------- |
| `lhs`  | bool | Un valor booleano |
| `rhs`  | bool | Un valor booleano |

### Salida

| Nombre  | Tipo | Descripción                                             |
| ------- | ---- | ------------------------------------------------------- |
| `value` | bool | El resultado de un AND binario entre los dos argumentos |

```hcl
output "my_bool" {
  value = false && true
}
// > my_bool: false
```

---

## or_bool

`or_bool` devuelve el OR binario de los argumentos del lado izquierdo y derecho.

### Entradas

| Nombre | Tipo | Descripción       |
| ------ | ---- | ----------------- |
| `lhs`  | bool | Un valor booleano |
| `rhs`  | bool | Un valor booleano |

### Salida

| Nombre  | Tipo | Descripción                                            |
| ------- | ---- | ------------------------------------------------------ |
| `value` | bool | El resultado de un OR binario entre los dos argumentos |

```hcl
output "my_bool" {
  value = false || true
}
// > my_bool: true
```

---

## div

`div` devuelve la división entera del argumento del lado izquierdo entre el
argumento del lado derecho, redondeando cualquier resto hacia abajo al entero
más cercano.

### Entradas

| Nombre | Tipo    | Descripción  |
| ------ | ------- | ------------ |
| `lhs`  | integer | El dividendo |
| `rhs`  | integer | El divisor   |

### Salida

| Nombre  | Tipo    | Descripción                                           |
| ------- | ------- | ----------------------------------------------------- |
| `value` | integer | El resultado de dividir el dividendo entre el divisor |

```hcl
output "my_int" {
  value = 11 / -3
}
// > my_int: -3
```

---

## eq

`eq` devuelve `true` si los argumentos del lado izquierdo y del lado derecho son
iguales e `false` si no lo son.

### Entradas

| Nombre | Tipo | Descripción     |
| ------ | ---- | --------------- |
| `lhs`  | any  | Cualquier valor |
| `rhs`  | any  | Cualquier valor |

### Salida

| Nombre  | Tipo | Descripción                                                         |
| ------- | ---- | ------------------------------------------------------------------- |
| `value` | bool | El resultado de una comprobación de igualdad entre las dos entradas |

```hcl
output "is_eq" {
  value = "arg" == "arg"
}
// > is_eq: true
```

---

## gt

`gt` devuelve `true` si el argumento del lado izquierdo es mayor que el
argumento del lado derecho.

### Entradas

| Nombre | Tipo                       | Descripción         |
| ------ | -------------------------- | ------------------- |
| `lhs`  | integer \| float \| string | Un valor comparable |
| `rhs`  | integer \| float \| string | Un valor comparable |

### Salida

| Nombre  | Tipo | Descripción                                                                                             |
| ------- | ---- | ------------------------------------------------------------------------------------------------------- |
| `value` | bool | El resultado de comprobar si el argumento del lado izquierdo es mayor que el argumento del lado derecho |

```hcl
output "is_gt" {
  value = 2 > 1
}
// > is_gt: true
```

---

## gte

`gte` devuelve `true` si el argumento del lado izquierdo es mayor o igual que el
argumento del lado derecho.

### Entradas

| Nombre | Tipo                       | Descripción         |
| ------ | -------------------------- | ------------------- |
| `lhs`  | integer \| float \| string | Un valor comparable |
| `rhs`  | integer \| float \| string | Un valor comparable |

### Salida

| Nombre  | Tipo | Descripción                                                                                                     |
| ------- | ---- | --------------------------------------------------------------------------------------------------------------- |
| `value` | bool | El resultado de comprobar si el argumento del lado izquierdo es mayor o igual que el argumento del lado derecho |

```hcl
output "is_gte" {
  value = 2 >= 2
}
// > is_gte: true
```

---

## lt

`lt` devuelve `true` si el argumento del lado izquierdo es menor que el
argumento del lado derecho.

### Entradas

| Nombre | Tipo                       | Descripción         |
| ------ | -------------------------- | ------------------- |
| `lhs`  | integer \| float \| string | Un valor comparable |
| `rhs`  | integer \| float \| string | Un valor comparable |

### Salida

| Nombre  | Tipo | Descripción                                                                                             |
| ------- | ---- | ------------------------------------------------------------------------------------------------------- |
| `value` | bool | El resultado de verificar si el argumento del lado izquierdo es menor que el argumento del lado derecho |

```hcl
output "is_lt" {
  value = 2 < 1
}
// > is_lt: false
```

---

## lte

`lte` devuelve `true` si el argumento del lado izquierdo es menor o igual que el
argumento del lado derecho.

### Entradas

| Nombre | Tipo                       | Descripción         |
| ------ | -------------------------- | ------------------- |
| `lhs`  | integer \| float \| string | Un valor comparable |
| `rhs`  | integer \| float \| string | Un valor comparable |

### Salida

| Nombre  | Tipo | Descripción                                                                                                     |
| ------- | ---- | --------------------------------------------------------------------------------------------------------------- |
| `value` | bool | El resultado de verificar si el argumento del lado izquierdo es menor o igual que el argumento del lado derecho |

```hcl
output "is_lte" {
  value = 2 <= 2
}
// > is_lte: true
```

---

## neq

`neq` devuelve `true` si los argumentos del lado izquierdo y derecho no son
iguales y `false` en caso contrario.

### Entradas

| Nombre | Tipo | Descripción                  |
| ------ | ---- | ---------------------------- |
| `lhs`  | any  | Un valor a comparar          |
| `rhs`  | any  | Un valor con el que comparar |

### Salida

| Nombre  | Tipo | Descripción                                                                |
| ------- | ---- | -------------------------------------------------------------------------- |
| `value` | bool | El resultado de una comprobación de igualdad negada entre las dos entradas |

```hcl
output "is_neq" {
  value = "arg" != "arg"
}
// > is_neq: false
```

---

## minus

`minus` devuelve el resultado de restar el argumento `integer` del lado derecho
del argumento `integer` del lado izquierdo.

### Entradas

| Nombre | Tipo    | Descripción   |
| ------ | ------- | ------------- |
| `lhs`  | integer | El minuendo   |
| `rhs`  | integer | El sustraendo |

### Salida

| Nombre  | Tipo    | Descripción                           |
| ------- | ------- | ------------------------------------- |
| `value` | integer | El resultado de la operación de resta |

```hcl
output "my_integer" {
  value = 10 - 6
}
// > my_integer: 4
```

---

## modulo

`modulo` devuelve el resto de dividir el argumento `integer` del lado izquierdo
entre el argumento `integer` del lado derecho.

### Entradas

| Nombre | Tipo    | Descripción  |
| ------ | ------- | ------------ |
| `lhs`  | integer | El dividendo |
| `rhs`  | integer | El divisor   |

### Salida

| Nombre  | Tipo    | Descripción                          |
| ------- | ------- | ------------------------------------ |
| `value` | integer | El resto de la operación de división |

```hcl
output "my_mod" {
    value = 10 % 3
}
// > my_mod: 1
```

---

## multiply

`multiply` devuelve el producto del argumento `integer` del lado izquierdo y el
argumento `integer` del lado derecho.

### Entradas

| Nombre | Tipo    | Descripción         |
| ------ | ------- | ------------------- |
| `lhs`  | integer | El primer operando  |
| `rhs`  | integer | El segundo operando |

### Salida

| Nombre  | Tipo    | Descripción                                    |
| ------- | ------- | ---------------------------------------------- |
| `value` | integer | El resultado de la operación de multiplicación |

```hcl
output "my_product" {
    value = 10 * 5
}
// > my_product: 50
```

---

## add

`add` devuelve la suma del argumento `integer` del lado izquierdo y el argumento
`integer` del lado derecho.

### Entradas

| Nombre | Tipo    | Descripción         |
| ------ | ------- | ------------------- |
| `lhs`  | integer | El primer operando  |
| `rhs`  | integer | El segundo operando |

### Salida

| Nombre  | Tipo    | Descripción                             |
| ------- | ------- | --------------------------------------- |
| `value` | integer | El resultado de la operación de adición |

```hcl
output "my_sum" {
    value = 10 + 5
}
// > my_sum: 15
```

---

## neg_integer

Devuelve la negación del entero dado.

### Entradas

| Nombre  | Tipo    | Descripción     |
| ------- | ------- | --------------- |
| `value` | integer | Un valor entero |

### Salida

| Nombre  | Tipo    | Descripción            |
| ------- | ------- | ---------------------- |
| `value` | integer | El valor entero negado |

---

## not_bool

Devuelve la negación lógica del valor booleano dado.

### Entradas

| Nombre  | Tipo | Descripción       |
| ------- | ---- | ----------------- |
| `value` | bool | Un valor booleano |

### Salida

| Nombre  | Tipo | Descripción                                      |
| ------- | ---- | ------------------------------------------------ |
| `value` | bool | La negación lógica del valor booleano de entrada |
