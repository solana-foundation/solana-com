---
title: Pagos agénticos
description:
  Permite que los agentes de IA paguen por servicios, APIs y recursos de forma
  autónoma utilizando el protocolo x402.
---

Los agentes de IA son cada vez más autónomos en sus flujos de trabajo: escriben
código, obtienen datos, aprovisionan recursos y más. Para operar de forma
completamente independiente, necesitan una forma de realizar "micropagos" por
servicios de manera programática, en tiempo real, a un costo mínimo. Los
sistemas de pago tradicionales no funcionan para micropagos. Las tarifas
inferiores a un centavo de Solana y su finalidad en menos de un segundo hacen
que esto sea técnica y económicamente viable.

## ¿Por qué es importante?

Por ejemplo, imagina que un desarrollador que usa un LLM para construir una
aplicación necesita datos de una API de pago. Para obtener algunos datos
importantes, un desarrollador necesitaría detener su flujo de trabajo,
investigar APIs, seleccionar un proveedor, crear una cuenta, agregar un método
de pago, generar una clave de API, copiar la clave en su entorno, y luego
reanudar su flujo de trabajo. Esta fricción rompe el estado de concentración.
Para flujos de trabajo asistidos por IA, es un obstáculo.

En cambio, con los pagos agénticos, el agente puede hacer la solicitud, pagar
por ella y continuar con su flujo de trabajo. Sin cuentas. Sin claves de API.
Sin intervención manual.

### Paga por APIs con pay.sh

[pay.sh](https://pay.sh) es una capa de pago para agentes HTTP y herramientas de
línea de comandos. Consulta la [documentación de pay.sh](https://pay.sh/docs)
para instalar `pay`. La CLI de `pay` envuelve herramientas como `curl`, `codex`
y `claude` para que puedan llamar a APIs de pago sin cuentas de proveedor ni
claves de API. Cuando una API devuelve un desafío MPP o x402
`402 Payment Required`, `pay` solicita a tu billetera local que apruebe la
firma.

Puedes instalar Pay con:

```bash
brew install pay
```

o

```bash
npm install -g @solana/pay
```

Verifica la instalación con:

```bash
pay --version
```

La cadena de herramientas configurará tu dirección automáticamente. Ejecuta lo
siguiente para configurar y recargar tu billetera:

```bash
pay setup
```

## El Protocolo x402

Los pagos agénticos necesitan una forma en que clientes y servidores negocien
los términos de pago sobre infraestructura web estándar sin interrumpir las
interfaces web tradicionales. El espacio de los micropagos aún está en sus
inicios y es probable que surjan nuevas herramientas y estándares a medida que
el ecosistema evolucione. Sin embargo, el
[protocolo x402](https://www.x402.org/) ha emergido como un estándar temprano
con un sólido respaldo del ecosistema. x402 utiliza el código de estado
[HTTP 402 "Pago Requerido"](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/402)
—un estándar desde HTTP/1.1 que solo se volvió práctico con la liquidación en
blockchain.

### Cómo funciona

El cliente realiza una solicitud, recibe un 402 con los términos de pago y luego
reintenta con un pago firmado. El servidor delega la verificación y liquidación
a un facilitador, un intermediario opcional que gestiona el envío de
transacciones onchain. Una vez que el facilitador confirma la liquidación, el
servidor devuelve el contenido solicitado. Esta separación permite a los
proveedores de API aceptar pagos sin gestionar directamente ninguna
infraestructura onchain.

![Diagrama de flujo x402](/assets/guides/x402/x402-flow-solana.png)

El protocolo es sin estado (sin sesiones ni claves de API), funciona con
infraestructura HTTP estándar y admite cualquier token SPL que el servidor
acepte. Aquí hay un fragmento de cómo configurar un servidor Express usando el
middleware x402:

```ts
app.use(
  paymentMiddleware(
    {
      "GET /costly-data": {
        accepts: [
          {
            scheme: "exact",
            price: "$0.001",
            network: "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
            payTo: svmAddress
          }
        ],
        description: "Costly data",
        mimeType: "application/json"
      }
    },
    new x402ResourceServer(facilitatorClient).register(
      "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
      new ExactSvmScheme()
    )
  )
);

app.get("/costly-data", (req, res) => {
  res.send({
    report: {
      data: "costly data"
    }
  });
});
```

Para obtener más información sobre x402, consulta nuestras guías:

<Cards>
  <Card
    title="Introducción a x402"
    href="/docs/payments/agentic-payments/intro-to-x402"
    description="Fundamentos del protocolo y opciones de SDK para crear APIs con pagos integrados."
  />
  <Card
    title="Construir con Kora"
    href="/docs/payments/agentic-payments/x402-facilitator"
    description="Implementa pagos x402 sin gas usando la infraestructura de firma de Kora."
  />
</Cards>

### Herramientas x402

| SDK                                                             | Descripción                              |
| --------------------------------------------------------------- | ---------------------------------------- |
| [Corbits](https://corbits.dev/)                                 | Implementación x402 centrada en Solana   |
| [MCPay.tech](https://mcpay.tech/)                               | Pago por solicitud para servidores MCP   |
| [PayAI](https://payai.network/)                                 | Facilitador x402 con soporte para Solana |
| [x402 GitHub](https://github.com/coinbase/x402)                 | Implementación de referencia             |
| [ACK](https://github.com/agentcommercekit/ack)                  | Kit de Comercio para Agentes             |
| [A2A x402](https://github.com/google-agentic-commerce/a2a-x402) | Pagos agente a agente de Google          |
