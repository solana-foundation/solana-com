---
title: Programma-implementatie
description: Hoe een eenvoudig programma te implementeren en aan te roepen
---

## Volledige Test

```rust title="tests/program_test.rs"
use litesvm::LiteSVM;
use solana_sdk::{
    instruction::{AccountMeta, Instruction},
    pubkey::Pubkey,
    signature::{Keypair, Signer},
    transaction::Transaction,
};

#[test]
fn test_program_deployment() {
    let mut svm = LiteSVM::new();

    // Deploy the program
    let program_id = Pubkey::new_unique();
    let program_bytes = include_bytes!("../target/deploy/hello_world.so");
    svm.add_program(program_id, program_bytes).unwrap();

    // Create payer account
    let payer = Keypair::new();
    svm.airdrop(&payer.pubkey(), 10_000_000_000).unwrap();

    // Create instruction to call program
    let instruction = Instruction {
        program_id,
        accounts: vec![
            AccountMeta::new(payer.pubkey(), true),
        ],
        data: vec![], // Program-specific data
    };

    // Send transaction
    let tx = Transaction::new_signed_with_payer(
        &[instruction],
        Some(&payer.pubkey()),
        &[&payer],
        svm.latest_blockhash(),
    );

    let result = svm.send_transaction(tx).unwrap();

    // Verify program was called
    assert!(result.logs.iter().any(|log|
        log.contains(&format!("Program {} invoke", program_id))
    ));

    println!("Program called successfully!");
    println!("Logs:\n{}", result.pretty_logs());
}
```

## Kernpunten

1. **Programma-implementatie**: Gebruik `add_program()` om een programma vanuit
   bytes te implementeren
2. **include_bytes!**: Sluit het gecompileerde programma in tijdens het
   compileren
3. **Programma-ID**: Gebruik de keypair van `target/deploy/`
4. **Instructies**: Maak aangepaste instructies met programma-ID, accounts en
   data
5. **Logverificatie**: Controleer transactielogboeken om de uitvoering van het
   programma te verifiëren

Voor meer gedetailleerde implementatie-informatie, lees
[dit gedeelte](/docs/tools/litesvm/testing-your-program/deploy).
