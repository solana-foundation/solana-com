---
title: Op Tijd Gebaseerd Testen
description: Test functies die afhankelijk zijn van slots of tijdstempels
---

## Overzicht

Dit voorbeeld laat zien hoe je tijdsafhankelijke functies kunt testen door slots
en tijdstempels te manipuleren in LiteSVM.

## Volledige Test

```rust title="tests/time_test.rs"
use litesvm::LiteSVM;
use solana_sdk::clock::Clock;

#[test]
fn test_time_locked_feature() {
    let mut svm = LiteSVM::new();

    // Get current time
    let clock: Clock = svm.get_sysvar();
    println!("Starting slot: {}", clock.slot);
    println!("Starting timestamp: {}", clock.unix_timestamp);

    // Test something at current time
    // ... your test logic ...

    // Jump forward 100 slots
    svm.warp_to_slot(clock.slot + 100);

    // Verify time changed
    let new_clock: Clock = svm.get_sysvar();
    assert_eq!(new_clock.slot, clock.slot + 100);

    // Test time-locked feature is now available
    // ... your test logic ...

    println!("Time travel successful!");
}
```

## Op slot Gebaseerde Functies Testen

```rust
#[test]
fn test_slot_based_unlock() {
    let mut svm = LiteSVM::new();

    // Get starting slot
    let start_slot = svm.get_sysvar::<Clock>().slot;

    // Deploy program with slot-based lock
    let program_id = deploy_locked_program(&mut svm);

    // Try to call before unlock slot (should fail)
    let result = call_program(&mut svm, program_id);
    assert!(result.is_err());

    // Warp to unlock slot
    svm.warp_to_slot(start_slot + 1000);

    // Now it should succeed
    let result = call_program(&mut svm, program_id);
    assert!(result.is_ok());
}
```

## Op Tijdstempel Gebaseerde Functies Testen

```rust
#[test]
fn test_timestamp_based_feature() {
    let mut svm = LiteSVM::new();

    // Get current timestamp
    let clock: Clock = svm.get_sysvar();
    let current_time = clock.unix_timestamp;

    // Warp forward 1 hour (3600 seconds)
    let target_slot = clock.slot + (3600.0 / 0.4) as u64; // ~0.4s per slot
    svm.warp_to_slot(target_slot);

    // Verify timestamp increased
    let new_clock: Clock = svm.get_sysvar();
    assert!(new_clock.unix_timestamp >= current_time + 3600);
}
```

## Belangrijke Punten

1. **Clock Sysvar**: Gebruik `svm.get_sysvar::<Clock>()` om de huidige tijd op
   te halen
2. **Slot Warping**: Gebruik `warp_to_slot()` om vooruit te springen in de tijd
3. **Tijdstempelberekening**: Slots komen ruwweg overeen met ~0,4 seconden op
   Solana
4. **Ontgrendelingen Testen**: Test tijdvergrendelde functies door voorbij de
   ontgrendeltijd te springen
5. **Geen Weg Terug**: Je kunt alleen vooruit springen, niet achteruit in de
   tijd

## Veelvoorkomende Gebruikssituaties

- Vestigingsschema's
- Tijdvergrendelde opnames
- Stake/unstake afkoelperiodes
- Eindtijden van veilingen
- Huurperiodes
