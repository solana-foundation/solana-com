---
title: Storten
description:
  Zet SPL-tokens over van de wallet van een gebruiker naar de escrow van Private Channels
---

Zet SPL-tokens over van de associated token account van een gebruiker naar de
associated token account van de escrow, en crediteert het kanaalsaldo van de stortende gebruiker (of het
saldo van een optionele ontvanger).

## Accounts

| Account                          | Schrijfbaar | Ondertekenaar | Beschrijving                                          |
| -------------------------------- | -------- | ------ | ---------------------------------------------------- |
| `payer`                          | ✓        | ✓      | Betaler van de transactiekosten                        |
| `user`                           |          | ✓      | Token-eigenaar die de storting uitvoert                       |
| `instance`                       |          |        | Escrow-instantie PDA                                  |
| `mint`                           |          |        | SPL-token mint                                       |
| `allowed_mint`                   |          |        | PDA die verifieert dat de mint is toegestaan op deze instantie |
| `user_ata`                       | ✓        |        | Associated token account van de gebruiker (bron)             |
| `instance_ata`                   | ✓        |        | Associated token account van de escrow (bestemming)      |
| `system_program`                 |          |        | System Program                                       |
| `token_program`                  |          |        | SPL Token Program                                    |
| `associated_token_program`       |          |        | Associated Token Program                             |
| `event_authority`                |          |        | Event authority PDA voor `EmitEvent` CPI              |
| `private_channel_escrow_program` |          |        | Escrow-programma (zelfverwijzing voor CPI)              |

## Parameters

| Parameter   | Type             | Beschrijving                                                                           |
| ----------- | ---------------- | ------------------------------------------------------------------------------------- |
| `amount`    | `u64`            | Hoeveelheid te storten tokens, uitgedrukt in de kleinste eenheid van het token                             |
| `recipient` | `Option<Pubkey>` | Optionele on-chain ontvanger; indien `None`, wordt het kanaalsaldo van de stortende gebruiker gecrediteerd |

## Retourwaarden

Retourneert een `TransactionBuilder` die gebruikt kan worden om de
transactie samen te stellen en te versturen.

## Voorbeeld

```typescript
import { getDepositInstructionAsync } from "../private-channel-escrow-program/clients/typescript/src/generated";

// allowedMint and instanceAta are auto-derived from mint and instance
const depositIx = await getDepositInstructionAsync({
  payer: payerSigner,
  user: userSigner,
  instance: instancePda,
  mint: mintAddress,
  userAta: userAtaAddress,
  amount: 1_000_000n,
  recipient: null
});
```

## Belangrijke opmerkingen

- Programma-ID: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- Voor Token-2022 mints moet `tokenProgram: TOKEN_2022_PROGRAM_ADDRESS`
  expliciet worden meegegeven, omdat standaard het gewone SPL Token Program wordt gebruikt
- De `allowed_mint` PDA moet bestaan (aangemaakt door `AllowMint`); het storten van een
  niet-geregistreerde mint geeft de fout `InvalidAllowedMint`
- Als `recipient` gelijk is aan `None`, wordt het kanaalsaldo van de stortende gebruiker gecrediteerd;
  als er een waarde is opgegeven, wordt in plaats daarvan het saldo van de opgegeven wallet gecrediteerd
- Zowel `payer` als `user` moeten de transactie ondertekenen
- De `instance_ata` moet al bestaan (aangemaakt door `AllowMint` voor deze mint)
