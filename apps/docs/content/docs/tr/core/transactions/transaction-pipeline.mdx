---
title: İşlem hattı
description:
  Solana işlemlerinin gönderimden taahhüde kadar nasıl işlendiği, 8 aşamalı
  doğrulama hattının tamamı, ağdan işlem detaylarının okunması ve yaygın
  doğrulama hataları dahil.
url: /docs/core/transactions/transaction-pipeline
type: reference
prerequisites:
  - /docs/core/transactions
  - /docs/core/transactions/transaction-structure
related:
  - /docs/core/transactions/durable-nonces
  - /docs/core/fees/fee-structure
  - /docs/core/accounts/account-runtime
  - /docs/core/programs/program-execution
---

<Callout type="info" title="Özet">
  İşlemler 8 aşamadan geçer: alma, imza doğrulama, temizleme, bütçe/yaş
  kontrolleri, ücret ödeyici doğrulama, hesap yükleme, instruction yürütme ve
  taahhüt.
</Callout>

## İşlem işleme hattı

Bir işlem validator'a ulaştığında, bir dizi doğrulama ve yürütme aşamasından
geçer. Aşağıda, alımdan taahhüde kadar tam hat açıklanmakta olup,
[agave](https://github.com/anza-xyz/agave/tree/v3.1.8) validator istemcisine
kaynak dosya referansları verilmiştir.

### 1. Alma ve deserialize etme

Validator, UDP/QUIC üzerinden işlem byte'larını alır. Ham byte'lar tek bir paket
içine sığmalıdır (_rs`PACKET_DATA_SIZE`_ = 1.232 byte). Byte'lar, imza dizisi ve
bir _rs`VersionedMessage`_ (legacy veya v0) içeren bir
_rs`VersionedTransaction`_ içine deserialize edilir.

### 2. İmza doğrulama (sigverify)

İmzalar, işlem banking aşamasına girmeden önce
[sigverify aşamasında](https://github.com/anza-xyz/agave/blob/v3.1.8/perf/src/sigverify.rs)
doğrulanır. `i` indeksindeki her imza için, doğrulayıcı Ed25519(`signatures[i]`,
`account_keys[i]`, `message_bytes`) kontrolü yapar. Herhangi bir imza
geçersizse, paket atılır.

Doğrulama paralelleştirilmiştir: validator, paket gruplarını
[`VERIFY_PACKET_CHUNK_SIZE`](https://github.com/anza-xyz/agave/blob/v3.1.8/perf/src/sigverify.rs#L27)
(128) parçalarına böler ve bunları paralel olarak işler.

### 3. Temizleme

Deserialize edilmiş işlem, bir _rs`SanitizedTransaction`_ (veya
[`RuntimeTransaction`](https://github.com/anza-xyz/agave/blob/v3.1.8/runtime-transaction/src/runtime_transaction.rs#L31))
üretmek için temizlenir. Temizleme, yapısal değişmezleri doğrular:

- İmza sayısı, başlıktaki `num_required_signatures` ile eşleşir
- Tüm instruction `program_id_index` ve `account_indices` sınırlar içindedir
- Ücret ödeyici (hesap indeksi 0) yazılabilir bir imzalayıcıdır

_rs`RuntimeTransaction`_ sarmalayıcısı,
[`TransactionMeta`](https://github.com/anza-xyz/agave/blob/v3.1.8/runtime-transaction/src/transaction_meta.rs#L38-L44)'den
önceden hesaplanmış metadata'yı önbelleğe alır: mesaj hash'i, oy işlemi bayrağı,
precompile imza sayıları (Ed25519/secp256k1/secp256r1), compute budget
instruction detayları ve toplam instruction data uzunluğu.

### 4. Hesaplama bütçesini, yaşını ve durum önbelleğini kontrol edin

[`check_transactions`](https://github.com/anza-xyz/agave/blob/v3.1.8/runtime/src/bank/check_transactions.rs#L55-L69)
metodu, işlem başına birkaç kontrol gerçekleştirir:

**Hesaplama bütçesi**: İşlemin hesaplama bütçesi talimatları önce ayrıştırılır
ve doğrulanır. Ücret detayları, bütçe limitleri ve önceliklendirme ücretinden
hesaplanır. Hesaplama bütçesi geçersiz veya çelişkili ise, işlem
_rs`DuplicateInstruction`_, `InstructionError(..., InvalidInstructionData)` veya
_rs`InvalidLoadedAccountsDataSizeLimit`_ gibi hesaplama bütçesi ayrıştırma
hatalarıyla başarısız olur.

**Blockhash yaşı**: İşlemin `recent_blockhash` değeri
[`BlockhashQueue`](https://github.com/anza-xyz/agave/blob/v3.1.8/accounts-db/src/blockhash_queue.rs#L33)
içinde aranır. Hash bulunursa ve yaşı _rs`MAX_PROCESSING_AGE`_ (150 slot)
içindeyse, işlem devam eder. Bulunamazsa, validator geçerli bir
[durable nonce](/docs/core/transactions/durable-nonces) olup olmadığını kontrol
eder.

**Durum önbelleği**: İşlemin mesaj hash'i bir durum önbelleğine karşı kontrol
edilir. Bulunursa, işlem _rs`AlreadyProcessed`_ ile reddedilir.

### 5. Nonce ve ücret ödeyiciyi doğrulayın

SVM'deki
[`validate_transaction_nonce_and_fee_payer`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/transaction_processor.rs#L580-L620)
metodu iki doğrulama işler:

**Nonce doğrulaması** (uygulanabilirse): Nonce işlemleri için, validator nonce
hesabını yükler ve şunları doğrular:

- Hesap System Program'a aittir
- _rs`State::Initialized`_ olarak ayrıştırılır
- Saklanan durable nonce, işlemin `recent_blockhash` değeriyle eşleşir
- Nonce ilerletilebilir (mevcut durable nonce, bir sonraki durable nonce'tan
  farklıdır, yani nonce mevcut blokta zaten kullanılmamıştır)
- Nonce yetkisi işlemi imzalamıştır

Geçerliyse, nonce bir sonraki durable nonce değerine ilerletilir. Bkz.
[`validate_transaction_nonce`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/transaction_processor.rs#L676-L729).

**Ücret ödeyici doğrulaması**: Ücret ödeyici hesabı (her zaman indeks 0)
yüklenir ve
[`validate_fee_payer`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/account_loader.rs#L369-L417)
tarafından kontrol edilir:

- Hesap mevcut olmalıdır (lamports > 0), aksi takdirde _rs`AccountNotFound`_
- Hesap bir sistem hesabı veya nonce hesabı olmalıdır, aksi takdirde
  _rs`InvalidAccountForFee`_
- Lamports, `min_balance + total_fee` değerini karşılamalıdır; burada
  `min_balance` sistem hesapları için 0 veya nonce hesapları için
  `rent.minimum_balance(NonceState::size())`'dir; aksi takdirde
  _rs`InsufficientFundsForFee`_
- Ücret kesintisinden sonra, hesap kira muafiyetini korumalıdır (kira
  muafiyetinden kira ödemeye geçiş yapamaz)

Ücret bu aşamada ücret ödeyiciden düşülür. Ücret düşülmüş ücret ödeyicinin (ve
varsa gelişmiş nonce'un) bir anlık görüntüsü _rs`RollbackAccounts`_ olarak
kaydedilir; bunlar, yürütme başarısız olsa bile commit edilen hesaplardır.

### 6. Hesapları yükle

[`load_transaction`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/account_loader.rs#L419-L455)
işlem tarafından referans verilen tüm hesapları yükler.
[`AccountLoader`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/account_loader.rs#L161-L165)
harici hesap deposunu sarar ve batch-yerel bir önbellek tutar, böylece aynı
batch içindeki önceki işlemler tarafından değiştirilen hesaplar sonraki işlemler
için görünür olur.

Her ücret ödeyici olmayan hesap için yükleyici:

1. Hesabı önbellekten veya accounts-db'den getirir
2. Gerekirse kira muafiyeti durumunu günceller
3. Hesabın veri boyutunu
   [`loaded_accounts_data_size_limit`](https://github.com/anza-xyz/agave/blob/v3.1.8/program-runtime/src/execution_budget.rs#L53)
   limitine ekler (varsayılan 64 MiB). Her hesap
   [`TRANSACTION_ACCOUNT_BASE_SIZE`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/account_loader.rs#L45)
   (64 bayt) temel ek yükü artı veri uzunluğu kadar maliyet getirir

İşlemin talimatları tarafından çağrılan her program için yükleyici, program
hesabının var olduğunu ve geçerli bir yükleyiciye (`NativeLoader` veya
_rs`PROGRAM_OWNERS`_'den biri) ait olduğunu doğrular. Geçersiz programlar
_rs`ProgramAccountNotFound`_ veya _rs`InvalidProgramForExecution`_ ile başarısız
olur.

LoaderV3 (yükseltilebilir) programları, yüklenen veri boyutu limitine dahil olan
ilişkili programdata hesabını örtük olarak yükler.

Hesap yükleme başarısız olursa ancak ücret ödeyici başarıyla doğrulanmışsa,
işlem
[`FeesOnly`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/account_loader.rs#L147-L152)
sonucu olur: ücret yine de tahsil edilir ancak hiçbir talimat yürütülmez.

### 7. Talimatları yürüt

[`execute_loaded_transaction`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/transaction_processor.rs#L847)
tüm yüklenmiş hesaplarla bir _rs`TransactionContext`_ oluşturur ve
[`process_message`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/message_processor.rs)'ı
çağırır. Talimatlar, mesajda göründükleri sırayla sıralı olarak yürütülür. Her
talimat çağrısı bir _rs`InvokeContext`_ oluşturur ve hedef programı çağırır.

#### Talimat işleme detayları

Runtime'ın
[`process_message`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/message_processor.rs#L15)
fonksiyonu her talimat üzerinde iterasyon yapar ve hedef programı çağırır:

1. Her talimat için, çalışma zamanı
   [`prepare_next_top_level_instruction`](https://github.com/anza-xyz/agave/blob/v3.1.8/program-runtime/src/invoke_context.rs#L436)
   fonksiyonunu çağırır ve bu fonksiyon
   [`InstructionContext`](https://github.com/anza-xyz/agave/blob/v3.1.8/transaction-context/src/lib.rs#L519)
   oluşturur. Bu bağlam, talimatın hesaplarına (derlenmiş indekslerden
   çözümlenen), instruction data'ya ve program account indeksine referanslar
   içerir.
2. Çalışma zamanı, programın bir [precompile](/docs/core/programs/precompiles)
   (Ed25519, Secp256k1, Secp256r1) olup olmadığını kontrol eder. Precompile'lar
   BPF VM çağrılmadan doğrudan doğrulanır.
3. Diğer tüm programlar için, çalışma zamanı
   [`process_instruction`](https://github.com/anza-xyz/agave/blob/v3.1.8/program-runtime/src/invoke_context.rs#L477)
   fonksiyonunu çağırır, bu fonksiyon programı önbellekten yükler ve BPF sanal
   makinesinde çalıştırır.
4. Talimat tamamlandıktan sonra, çalışma zamanı tüm talimat hesaplarındaki
   toplam lamport bakiyesinin değişmediğini
   [doğrular](https://github.com/anza-xyz/agave/blob/v3.1.8/transaction-context/src/lib.rs#L366-L398)
   (_rs`UnbalancedInstruction`_ kontrolü).
5. Herhangi bir talimat başarısız olursa, tüm işlem geri alınır. Hiçbir ara
   durum değişikliği kaydedilmez.

Her talimat, instruction trace'i artırır. Trace hem üst düzey talimatları hem de
bunların çağırdığı [CPI'ları](/docs/core/cpi) içerir. Toplam trace uzunluğu (üst
düzey talimatlar artı tüm iç içe CPI'lar) 64'ü
(_rs`MAX_INSTRUCTION_TRACE_LENGTH`_) aşamaz. Bu sınırın aşılması
_rs`InstructionError::MaxInstructionTraceLengthExceeded`_ döndürür.

Çalıştırma sonrasında, çalışma zamanı şunları doğrular:

- Tüm hesaplardaki lamport toplamının değişmediği
- Hiçbir hesabın rent-exempt durumundan rent-paying durumuna geçmediği

### 8. Commit veya rollback

Çalıştırma başarılı olursa, `TransactionContext`'den değiştirilmiş hesap
durumları _rs`AccountLoader`_'ın batch-local önbelleğine geri yazılır.
Çalıştırma başarısız olursa, yalnızca _rs`RollbackAccounts`_ (ücret düşülmüş fee
payer ve ileri alınmış nonce) geri yazılır. Ücret yine de tahsil edilir, ancak
diğer tüm hesap değişiklikleri iptal edilir.

### Pipeline özeti

```
Receive packet (UDP/QUIC)
  --> Deserialize into VersionedTransaction
  --> Sigverify (parallel Ed25519 verification)
  --> Sanitize (structural validation, metadata extraction)
  --> Parse compute budget, calculate fees
  --> Check blockhash age (or verify nonce account)
  --> Check status cache (dedup)
  --> Validate nonce authority and advanceability (if nonce transaction)
  --> Validate fee payer (load, check balance, deduct fee)
  --> Load all accounts (with data size limits)
  --> Load programs (verify loaders)
  --> Execute instructions sequentially
  --> Verify post-conditions (lamport balance, rent state)
  --> Commit account changes (or rollback on failure)
```

## Transaction error referansı

Aşağıdaki tablo tüm
[`TransactionError`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L15)
varyantlarını ve bunların hangi pipeline aşamasında oluştuğunu listeler:

| Hata                                                                                                                                    | Aşama                     | Neden                                                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------ |
| [`AccountInUse`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L18)                           | Zamanlama                 | Hesap aynı toplu işlemdeki başka bir işlem tarafından zaten kilitlenmiş                    |
| [`AccountLoadedTwice`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L22)                     | Zamanlama                 | Bir pubkey işlemin `account_keys` içinde iki kez görünüyor                                 |
| [`AccountNotFound`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L25)                        | Ücret ödeyen doğrulama    | Ücret ödeyen hesap mevcut değil                                                            |
| [`ProgramAccountNotFound`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L28)                 | Hesap yükleme             | Çağrılan program mevcut değil                                                              |
| [`InsufficientFundsForFee`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L31)                | Ücret ödeyen doğrulama    | Ücret ödeyen, ücret + rent-exempt minimumunu karşılayamıyor                                |
| [`InvalidAccountForFee`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L34)                   | Ücret ödeyen doğrulama    | Ücret ödeyen bir sistem veya nonce hesabı değil                                            |
| [`AlreadyProcessed`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L39)                       | Durum önbelleği           | İşlem zaten işlendi                                                                        |
| [`BlockhashNotFound`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L43)                      | Yaş kontrolü              | Blockhash kuyrukta değil ve geçerli bir nonce değil                                        |
| [`InstructionError`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L47)                       | Yürütme                   | Bir talimat işlenirken hata oluştu (talimat indeksi ve spesifik `InstructionError` içerir) |
| [`CallChainTooDeep`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L50)                       | Hesap yükleme             | Yükleyici çağrı zinciri çok derin                                                          |
| [`MissingSignatureForFee`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L53)                 | Sanitize                  | İşlem ücret gerektiriyor ancak imza mevcut değil                                           |
| [`InvalidAccountIndex`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L56)                    | Sanitize                  | İşlem geçersiz bir hesap referansı içeriyor                                                |
| [`SignatureFailure`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L59)                       | Sigverify                 | Ed25519 imzası doğrulanamıyor (paket atılıyor)                                             |
| [`InvalidProgramForExecution`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L62)             | Hesap yükleme             | Program geçerli bir yükleyiciye ait değil                                                  |
| [`SanitizeFailure`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L67)                        | Sanitize                  | İşlem hesap ofsetlerini doğru şekilde sanitize edemedi                                     |
| [`ClusterMaintenance`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L69)                     | Zamanlama                 | Küme bakımı nedeniyle işlemler şu anda devre dışı                                          |
| [`AccountBorrowOutstanding`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L72)               | Yürütme                   | İşlem işleme, bekleyen ödünç alınmış referansa sahip bir hesap bıraktı                     |
| [`WouldExceedMaxBlockCostLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L75)           | Zamanlama                 | İşlem maksimum blok maliyet limitini aşacak                                                |
| [`UnsupportedVersion`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L78)                     | Sanitize                  | İşlem sürümü desteklenmiyor                                                                |
| [`InvalidWritableAccount`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L81)                 | Hesap yükleme             | İşlem yazılamayan yazılabilir bir hesap yüklüyor                                           |
| [`WouldExceedMaxAccountCostLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L84)         | Zamanlama                 | İşlem blok içindeki maksimum hesap maliyet limitini aşacak                                 |
| [`WouldExceedAccountDataBlockLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L87)       | Zamanlama                 | İşlem blok içindeki hesap veri limitini aşacak                                             |
| [`TooManyAccountLocks`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L90)                    | Zamanlama                 | İşlem çok fazla hesap kilitledi                                                            |
| [`AddressLookupTableNotFound`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L93)             | Hesap yükleme             | Adres arama tablosu hesabı mevcut değil                                                    |
| [`InvalidAddressLookupTableOwner`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L96)         | Hesap yükleme             | Adres arama tablosu yanlış programa ait                                                    |
| [`InvalidAddressLookupTableData`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L99)          | Hesap yükleme             | Adres arama tablosu geçersiz veri içeriyor                                                 |
| [`InvalidAddressLookupTableIndex`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L102)        | Hesap yükleme             | Adres tablosu araması geçersiz bir indeks kullanıyor                                       |
| [`InvalidRentPayingAccount`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L105)              | Yürütme sonrası kontrol   | Hesap rent-exempt'ten rent-paying'e geçiş yaptı                                            |
| [`WouldExceedMaxVoteCostLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L108)           | Zamanlama                 | İşlem maksimum oy maliyet limitini aşacak                                                  |
| [`WouldExceedAccountDataTotalLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L111)      | Zamanlama                 | İşlem toplam hesap veri limitini aşacak                                                    |
| [`DuplicateInstruction`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L114)                  | Compute budget ayrıştırma | Aynı işlemde yinelenen compute budget talimat varyantı                                     |
| [`InsufficientFundsForRent`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L117)              | Yürütme sonrası kontrol   | Hesap veri boyutu için rent'i karşılayacak yeterli lamport'a sahip değil                   |
| [`MaxLoadedAccountsDataSizeExceeded`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L122)     | Hesap yükleme             | Toplam yüklenen veri 64 MiB limitini aşıyor                                                |
| [`InvalidLoadedAccountsDataSizeLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L125)    | Compute budget ayrıştırma | `SetLoadedAccountsDataSizeLimit` 0 olarak ayarlandı                                        |
| [`ResanitizationNeeded`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L128)                  | Sanitize                  | İşlem özellik aktivasyonundan önce/sonra farklılaştı ve yeniden sanitizasyon gerektiriyor  |
| [`ProgramExecutionTemporarilyRestricted`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L131) | Hesap yükleme             | Program yürütme referans verilen hesapta geçici olarak kısıtlanmış                         |
| [`UnbalancedTransaction`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L136)                 | Yürütme sonrası kontrol   | İşlemden önceki toplam lamport bakiyesi işlem sonrası bakiyeye eşit değil                  |
| [`ProgramCacheHitMaxLimit`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L139)               | Hesap yükleme             | Program önbelleği maksimum limite ulaştı                                                   |
| [`CommitCancelled`](https://github.com/anza-xyz/solana-sdk/blob/clock%40v2.2.3/transaction-error/src/lib.rs#L142)                       | Commit                    | Commit dahili olarak iptal edildi                                                          |
