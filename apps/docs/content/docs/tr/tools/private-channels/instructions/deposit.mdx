---
title: Para Yatırma
description:
  SPL tokenlarını kullanıcının cüzdanından Private Channels emanet hesabına aktarır
---

SPL tokenlarını kullanıcının associated token account hesabından emanet hesabının
associated token account hesabına aktarır; para yatıran kullanıcının kanal bakiyesini (veya
isteğe bağlı bir alıcının bakiyesini) günceller.

## Hesaplar

| Hesap                          | Yazılabilir | İmzalayan | Açıklama                                          |
| -------------------------------- | -------- | ------ | ---------------------------------------------------- |
| `payer`                          | ✓        | ✓      | İşlem için ücret ödeyici                        |
| `user`                           |          | ✓      | Para yatırma işlemini gerçekleştiren token sahibi                       |
| `instance`                       |          |        | Emanet örneği PDA                                  |
| `mint`                           |          |        | SPL token mint                                       |
| `allowed_mint`                   |          |        | Mint'in bu örnekte izinli olduğunu doğrulayan PDA |
| `user_ata`                       | ✓        |        | Kullanıcının associated token account hesabı (kaynak)             |
| `instance_ata`                   | ✓        |        | Emanet hesabının associated token account hesabı (hedef)      |
| `system_program`                 |          |        | System Program                                       |
| `token_program`                  |          |        | SPL Token Program                                    |
| `associated_token_program`       |          |        | Associated Token Program                             |
| `event_authority`                |          |        | `EmitEvent` CPI için olay yetkisi PDA              |
| `private_channel_escrow_program` |          |        | Emanet Programı (CPI için öz-referans)              |

## Parametreler

| Parametre   | Tür             | Açıklama                                                                           |
| ----------- | ---------------- | ------------------------------------------------------------------------------------- |
| `amount`    | `u64`            | Yatırılacak token miktarı, tokenın en küçük birimi cinsinden                             |
| `recipient` | `Option<Pubkey>` | İsteğe bağlı zincir üstü alıcı; `None` ise para yatıran kullanıcının kanal bakiyesine eklenir |

## Döndürülen Değer

İşlemi oluşturmak ve göndermek için kullanılabilecek bir `TransactionBuilder` döndürür.

## Örnek

```typescript
import { getDepositInstructionAsync } from "../private-channel-escrow-program/clients/typescript/src/generated";

// allowedMint and instanceAta are auto-derived from mint and instance
const depositIx = await getDepositInstructionAsync({
  payer: payerSigner,
  user: userSigner,
  instance: instancePda,
  mint: mintAddress,
  userAta: userAtaAddress,
  amount: 1_000_000n,
  recipient: null
});
```

## Önemli Notlar

- Program ID: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- Token-2022 mint'leri için, varsayılan olarak standart SPL Token Program kullanıldığından
  `tokenProgram: TOKEN_2022_PROGRAM_ADDRESS` değerini açıkça belirtin
- `allowed_mint` PDA'sının mevcut olması gerekir (`AllowMint` tarafından oluşturulur);
  listede olmayan bir mint yatırılmaya çalışıldığında `InvalidAllowedMint` hatası fırlatılır
- `recipient` değeri `None` olduğunda para yatıran kullanıcının kanal bakiyesine eklenir;
  belirtildiğinde ise ilgili cüzdanın bakiyesine eklenir
- Hem `payer` hem de `user` işlemi imzalamalıdır
- `instance_ata` hesabının önceden mevcut olması gerekir (bu mint için `AllowMint` tarafından oluşturulur)
