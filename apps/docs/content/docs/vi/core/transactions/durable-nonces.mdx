---
title: Durable nonces
description:
  Cách hoạt động của durable nonces trên Solana cho việc ký ngoại tuyến và gửi
  giao dịch trễ, bao gồm vòng đời nonce, phát hiện, xác thực và hành vi lỗi.
url: /docs/core/transactions/durable-nonces
type: conceptual
prerequisites:
  - /docs/core/transactions
  - /docs/core/transactions/transaction-structure
related:
  - /docs/core/transactions/transaction-pipeline
  - /docs/core/programs/builtin-programs
---

<Callout type="info" title="Tóm tắt">
  Giao dịch durable nonce thay thế blockhash gần đây bằng giá trị nonce được lưu
  trữ, loại bỏ cửa sổ hết hạn 150 slot cho giao dịch. Điều này cho phép ký ngoại
  tuyến và gửi trễ.
</Callout>

<Callout type="warn">
  Durable nonces có thể bị loại bỏ trong phiên bản tương lai. Xem [thảo luận
  SIMD](https://github.com/solana-foundation/solana-improvement-documents/discussions/415)
  để biết chi tiết.
</Callout>

## Durable nonces

Giao dịch durable nonce thay thế blockhash gần đây bằng giá trị nonce được lưu
trữ, loại bỏ cửa sổ hết hạn 150 slot. Điều này cho phép ký ngoại tuyến và gửi
trễ.

### Cách hoạt động của giao dịch nonce

Tài khoản nonce là tài khoản thuộc sở hữu của System Program lưu trữ giá trị
_rs`State::Initialized`_ chứa ba trường: một **authority** pubkey (người có thể
nâng cấp nonce), một giá trị **durable nonce** (hash được tạo từ blockhash gần
đây), và **lamports per signature** (tỷ lệ phí khi nonce được nâng cấp lần
cuối).

Để sử dụng durable nonce:

1. Tạo và khởi tạo tài khoản nonce bằng _rs`CreateAccount`_ +
   _rs`InitializeNonceAccount`_
2. Xây dựng giao dịch với _rs`AdvanceNonceAccount`_ là **lệnh đầu tiên** và giá
   trị nonce là `recent_blockhash`
3. Ký giao dịch (có thể thực hiện ngoại tuyến, vì nonce không hết hạn)
4. Gửi khi sẵn sàng

### Phát hiện nonce

Runtime phát hiện giao dịch nonce bằng cách kiểm tra xem lệnh đầu tiên (chỉ số
[`NONCED_TX_MARKER_IX_INDEX`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm-transaction/src/svm_message.rs#L20)
= 0) có phải là lời gọi đến System Program với lệnh _rs`AdvanceNonceAccount`_
hay không. Tài khoản nonce phải là tài khoản đầu tiên của lệnh đó và phải có thể
ghi. Xem
[`get_durable_nonce`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm-transaction/src/svm_message.rs#L89-L121).

### Quy trình xác thực nonce

Khi `recent_blockhash` của giao dịch không được tìm thấy trong `BlockhashQueue`,
validator sẽ kiểm tra xem đó có phải là giao dịch nonce hợp lệ hay không thông
qua
[`check_transaction_age`](https://github.com/anza-xyz/agave/blob/v3.1.8/runtime/src/bank/check_transactions.rs#L152-L180):

1. `durable_nonce` được lưu trữ của nonce phải khác với durable nonce tiếp theo
   (được tạo ra từ blockhash hiện tại). Điều này đảm bảo nonce chưa được sử dụng
   trong block này
2. Tài khoản nonce phải được tải và phải phân tích được dưới dạng
   _rs`State::Initialized`_
3. `durable_nonce` được lưu trữ phải khớp với `recent_blockhash` của giao dịch

Việc kiểm tra chữ ký của nonce authority được thực hiện sau đó
([`validate_transaction_nonce`](https://github.com/anza-xyz/agave/blob/v3.1.8/svm/src/transaction_processor.rs#L676-L729)).

Nếu tất cả các kiểm tra đều thành công, nonce sẽ được nâng cấp lên giá trị
durable nonce tiếp theo trước khi bắt đầu thực thi. Khi thực thi thất bại, nonce
đã được nâng cấp và phí đã được khấu trừ từ fee payer vẫn được cam kết (ngăn
chặn replay trong khi thu phí).

### Hành vi khi nonce thất bại

Nếu giao dịch nonce **thất bại xác thực** (nonce đã được sử dụng, authority
không ký, tài khoản không tìm thấy), toàn bộ giao dịch sẽ bị loại bỏ. Không thu
phí, không có thay đổi trạng thái.

Nếu giao dịch nonce **vượt qua xác thực nhưng thực thi thất bại** (một
instruction trả về lỗi), nonce vẫn được nâng cấp và phí vẫn được thu. Điều này
ngăn chặn giao dịch bị replay trong khi đảm bảo validator được bồi thường.
