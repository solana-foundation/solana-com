---
title: Keychain
description: Ký giao dịch Solana thống nhất trên nhiều backend quản lý khóa
---

**Solana Keychain cung cấp một giao diện thống nhất để ký các giao dịch Solana
trên nhiều backend quản lý khóa khác nhau.** Phát triển cục bộ với khóa trong bộ
nhớ, rồi chuyển sang quản lý khóa doanh nghiệp, HSM đám mây hoặc dịch vụ ví được
quản lý trong môi trường production — mà không cần viết lại code ký của bạn.

### Tại sao nên dùng Keychain?

- **Giao Diện Đơn Nhất**: Một trait `SolanaSigner` hoạt động trên tất cả các
  backend
- **Hoán Đổi Backend**: Thay đổi nhà cung cấp quản lý khóa thông qua cấu hình,
  không phải code
- **Trừu Tượng Hóa Không Chi Phí**: Feature flag chỉ bao gồm những gì bạn cần
- **Đã Kiểm Tra**: Được đánh giá bảo mật bởi [Accretion](https://accretion.xyz)

### Kiến trúc

- **Ngôn Ngữ**: Rust + TypeScript, hoàn toàn tương đương
- **Trait**: Giao diện `SolanaSigner` thống nhất
- **Backend**: Memory, Vault, AWS KMS, GCP KMS, Privy, Turnkey, Fireblocks, CDP,
  Crossmint, Dfns, Openfort, Para, Utila
- **Tương Thích**: `@solana/kit` và `@solana/signers` tương thích (TypeScript) |
  `solana-sdk` và `solana-sdk-v3` tương thích (Rust)

### Các Backend được hỗ trợ

Các backend bao gồm phát triển cục bộ, quản lý khóa tự lưu trữ và trên đám mây,
cùng các dịch vụ ví được quản lý hoặc MPC. Xem
[Chọn backend](/docs/tools/keychain/choosing-a-backend) để so sánh các mô hình
lưu ký và chọn mô hình phù hợp.

| Backend         | Trường Hợp Sử Dụng            | Rust | TypeScript |
| --------------- | ----------------------------- | ---- | ---------- |
| Memory          | Phát triển, kiểm thử          | ✓    | ✓          |
| HashiCorp Vault | Quản lý khóa tự lưu trữ       | ✓    | ✓          |
| AWS KMS         | Cloud-native (AWS)            | ✓    | ✓          |
| GCP KMS         | Cloud-native (GCP)            | ✓    | ✓          |
| Privy           | Ví nhúng                      | ✓    | ✓          |
| Turnkey         | Quản lý khóa không lưu ký     | ✓    | ✓          |
| Fireblocks      | Lưu ký tổ chức                | ✓    | ✓          |
| CDP             | Coinbase Developer Platform   | ✓    | ✓          |
| Crossmint       | Ví được quản lý bởi Crossmint | ✓    | ✓          |
| Dfns            | Hạ tầng ví Dfns               | ✓    | ✓          |
| Openfort        | Ví backend Openfort           | ✓    | ✓          |
| Para            | Ví MPC Para                   | ✓    | ✓          |
| Utila           | Lưu ký MPC Utila              | ✓    | ✓          |

## Bắt đầu nhanh

### Rust

Cài đặt Rust crate:

```bash
cargo add solana-keychain
```

Cách sử dụng cơ bản:

```rust
use solana_keychain::{Signer, SolanaSigner};

// Create a signer from any backend
let signer = Signer::from_memory("base58_private_key")?;

// All signers share the same interface
let pubkey = signer.pubkey();
let signature = signer.sign_transaction(&mut tx).await?;
```

- [Hướng dẫn Rust](/docs/tools/keychain/getting-started/rust) - Cài đặt đầy đủ
  và cấu hình backend
- [Crates.io](https://crates.io/crates/solana-keychain) - Rust crate

### TypeScript

Cài đặt gói TypeScript:

```bash
pnpm add @solana/keychain
```

Cách sử dụng cơ bản:

```typescript
import { createKeychainSigner } from "@solana/keychain";
import { signTransactionWithSigners } from "@solana/signers";

// Create any signer via the unified factory
const signer = await createKeychainSigner({
  backend: "vault",
  vaultAddr: "https://vault.example.com:8200",
  vaultToken: "hvs.xxxxx",
  keyName: "my-solana-key",
  publicKey: "base58_public_key"
});

// Sign an already-compiled transaction
const signedTx = await signTransactionWithSigners(
  [signer],
  compiledTransaction
);
```

- [Hướng dẫn TypeScript](/docs/tools/keychain/getting-started/typescript) - Các
  gói `@solana/keychain`
- [Gói npm](https://www.npmjs.com/package/@solana/keychain) - Gói npm

## Các bước tiếp theo

<Cards>
  <Card title="Hướng dẫn Rust" href="/docs/tools/keychain/getting-started/rust">
    Cài đặt crate và cấu hình từng backend.
  </Card>
  <Card
    title="Hướng dẫn TypeScript"
    href="/docs/tools/keychain/getting-started/typescript"
  >
    Cài đặt các gói và cấu hình từng backend.
  </Card>
  <Card title="Chọn backend" href="/docs/tools/keychain/choosing-a-backend">
    So sánh các mô hình lưu ký và chọn backend ký phù hợp.
  </Card>
  <Card
    title="Các phương pháp tốt nhất cho môi trường sản xuất"
    href="/docs/tools/keychain/production-best-practices"
  >
    Vận hành ký an toàn: phân tách hot/cold, luân chuyển khóa, giám sát.
  </Card>
  <Card
    title="Thêm trình ký tùy chỉnh"
    href="/docs/tools/keychain/adding-signers"
  >
    Tích hợp một backend quản lý khóa mới.
  </Card>
</Cards>

## Nguồn

- [Kho lưu trữ GitHub](https://github.com/solana-foundation/solana-keychain)
- [Lịch sử phát hành](https://github.com/solana-foundation/solana-keychain/releases)

Được xây dựng và duy trì bởi [Solana Foundation](https://solana.org). Được cấp
phép theo MIT.
