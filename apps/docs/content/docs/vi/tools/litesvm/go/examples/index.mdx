---
title: Ví dụ
description: Các ví dụ Go hoàn chỉnh cho LiteSVM
---

Các ví dụ hoàn chỉnh, có thể chạy được, minh họa cách sử dụng `litesvm-go`. Mỗi ví dụ là một
bài kiểm tra Go độc lập mà bạn có thể thêm vào một package và chạy bằng `go test`.

<Cards>
  <Card
    title="Chuyển SOL"
    description="Xây dựng và gửi lệnh chuyển tiền qua System Program"
    href="/docs/tools/litesvm/go/examples/sol-transfer"
  />
  <Card
    title="Thiết lập Tài khoản"
    description="Khởi tạo tài khoản kiểm thử bằng seed và trạng thái dữ liệu chương trình"
    href="/docs/tools/litesvm/go/examples/account-setup"
  />
  <Card
    title="Kiểm thử Chương trình"
    description="Tải và thử nghiệm các chương trình SBF tùy chỉnh"
    href="/docs/tools/litesvm/go/examples/program-testing"
  />
  <Card
    title="Kiểm thử Dựa trên Thời gian"
    description="Kiểm thử logic phụ thuộc thời gian với WarpToSlot / SetClock"
    href="/docs/tools/litesvm/go/examples/time-based"
  />
</Cards>

## Chạy các Ví dụ

```bash
# 1) Initialize a Go module (skip if you already have a go.mod)
go mod init mytest

# 2) Add the dependencies to your Go project
go get github.com/LiteSVM/litesvm-go
go get github.com/gagliardetto/solana-go

# 3) Drop an example into a _test.go file and run
go test ./...
```

Trên Alpine / musl, thêm `-tags musl` vào lệnh `go test` của bạn để liên kết đúng
archive được vendor.

Để xem ví dụ dòng lệnh độc lập (không cần test runner), hãy xem
[`examples/transfer/main.go`](https://github.com/LiteSVM/litesvm-go/blob/main/examples/transfer/main.go)
trong repo `litesvm-go`.
