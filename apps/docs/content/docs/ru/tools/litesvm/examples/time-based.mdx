---
title: Тестирование на основе времени
description: Тестирование функций, зависящих от slot или временных меток
---

## Обзор

Этот пример показывает, как тестировать функции, зависящие от времени, путём
управления slot и временными метками в LiteSVM.

## Полный тест

```rust title="tests/time_test.rs"
use litesvm::LiteSVM;
use solana_sdk::clock::Clock;

#[test]
fn test_time_locked_feature() {
    let mut svm = LiteSVM::new();

    // Get current time
    let clock: Clock = svm.get_sysvar();
    println!("Starting slot: {}", clock.slot);
    println!("Starting timestamp: {}", clock.unix_timestamp);

    // Test something at current time
    // ... your test logic ...

    // Jump forward 100 slots
    svm.warp_to_slot(clock.slot + 100);

    // Verify time changed
    let new_clock: Clock = svm.get_sysvar();
    assert_eq!(new_clock.slot, clock.slot + 100);

    // Test time-locked feature is now available
    // ... your test logic ...

    println!("Time travel successful!");
}
```

## Тестирование функций на основе slot

```rust
#[test]
fn test_slot_based_unlock() {
    let mut svm = LiteSVM::new();

    // Get starting slot
    let start_slot = svm.get_sysvar::<Clock>().slot;

    // Deploy program with slot-based lock
    let program_id = deploy_locked_program(&mut svm);

    // Try to call before unlock slot (should fail)
    let result = call_program(&mut svm, program_id);
    assert!(result.is_err());

    // Warp to unlock slot
    svm.warp_to_slot(start_slot + 1000);

    // Now it should succeed
    let result = call_program(&mut svm, program_id);
    assert!(result.is_ok());
}
```

## Тестирование функций на основе временных меток

```rust
#[test]
fn test_timestamp_based_feature() {
    let mut svm = LiteSVM::new();

    // Get current timestamp
    let clock: Clock = svm.get_sysvar();
    let current_time = clock.unix_timestamp;

    // Warp forward 1 hour (3600 seconds)
    let target_slot = clock.slot + (3600.0 / 0.4) as u64; // ~0.4s per slot
    svm.warp_to_slot(target_slot);

    // Verify timestamp increased
    let new_clock: Clock = svm.get_sysvar();
    assert!(new_clock.unix_timestamp >= current_time + 3600);
}
```

## Ключевые моменты

1. **Clock Sysvar**: Используйте `svm.get_sysvar::<Clock>()` для получения
   текущего времени
2. **Перемотка slot**: Используйте `warp_to_slot()` для перехода вперёд во
   времени
3. **Расчёт временных меток**: Каждый slot примерно соответствует ~0,4 секунды в
   Solana
4. **Тестирование разблокировок**: Тестируйте функции с временной блокировкой,
   перематывая время за момент разблокировки
5. **Без возврата назад**: Перемотка возможна только вперёд, но не назад

## Распространённые случаи использования

- Графики вестинга
- Вывод средств с временной блокировкой
- Периоды ожидания для стейкинга/анстейкинга
- Время окончания аукционов
- Периоды аренды
