---
title: Инструкции
description:
  Инструкции Solana — это наименьшая единица выполнения программы. Каждая
  инструкция предназначена для одной программы и определяет аккаунты, данные и
  требования к подписантам.
url: /docs/core/instructions
type: conceptual
prerequisites:
  - /docs/core/accounts
  - /docs/core/programs
related:
  - /docs/core/instructions/instruction-structure
  - /docs/core/transactions
  - /docs/core/cpi
---

Инструкция — это запрос на выполнение определённой функции в программе Solana.
Инструкции являются базовым строительным блоком для операций в блокчейне. Каждая
инструкция указывает ровно одну вызываемую программу, необходимые аккаунты и
массив байтов данных, который программа интерпретирует (обычно это дискриминатор
для определения нужного обработчика инструкции, а затем сериализованные
аргументы). Логика выполнения для каждой инструкции хранится в
[программе](/docs/core/programs), где каждая программа определяет свой набор
инструкций. Для взаимодействия с сетью Solana одна или несколько инструкций
добавляются в [транзакцию](/docs/core/transactions) и отправляются в сеть для
обработки.

![Диаграмма, показывающая транзакцию с инструкцией, разбитую на 3 компонента](/assets/docs/core/transactions/instruction.svg)

<Cards>
  <Card
    title="Структура инструкции"
    href="/docs/core/instructions/instruction-structure"
  >
    Три поля инструкции (program_id, accounts, data), AccountMeta,
    скомпилированные инструкции и пример перевода SOL.
  </Card>
  <Card
    title="Интроспекция инструкций"
    href="/docs/core/instructions/instruction-introspection"
  >
    Как программы анализируют соседние инструкции в текущей транзакции через
    системную переменную Instructions.
  </Card>
</Cards>

## Основные факты

- **Одна программа**: Каждая инструкция предназначена ровно для одной программы
  через `program_id`.
- **Метаданные аккаунта**: Массив `accounts` содержит
  [`AccountMeta`](/docs/core/instructions/instruction-structure#account-metadata)
  для каждого аккаунта, который инструкция читает или изменяет.
- **Непрозрачные данные**: Поле `data` — это массив байтов, формат которого
  определяется целевой программой.
