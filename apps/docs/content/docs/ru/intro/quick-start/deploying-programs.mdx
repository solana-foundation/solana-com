---
title: Развертывание программы
description:
  Узнайте, как создать, развернуть и протестировать вашу первую программу Solana
  с использованием фреймворка Anchor и Solana Playground. Это руководство для
  начинающих проведет вас через создание простой программы, ее развертывание на
  devnet, выполнение тестов и завершение работы программы.
h1: Разверните вашу первую программу Solana
---

Создайте, разверните и протестируйте вашу первую
[программу](/docs/core/programs) на блокчейне Solana с использованием фреймворка
Anchor. Это руководство поможет вам лучше ознакомиться с Solana Playground и
подготовит вас к использованию Program Derived Addresses (PDA) и Cross Program
Invocation (CPI).

<Steps>

<Step>

### Создание проекта Anchor

1. Откройте [Solana Playground](https://beta.solpg.io) в вашем веб-браузере
2. В левой панели нажмите **+ Создать новый проект**
3. Введите **Имя проекта**
4. Выберите **Anchor (Rust)** в качестве фреймворка
5. Нажмите **Создать**

![Новый проект](/assets/docs/intro/quickstart/pg-new-project.gif)

Откроется исходный файл `src/lib.rs`. В нём содержится одна структура с именем
`Initialize`, которая выполняет следующее:

- Принимает обязательный параметр _rs`data: u64`_
- Создаёт новый аккаунт
- Сохраняет значение _rs`data: u64`_ в новый аккаунт
- Добавляет сообщение в логи программы транзакции

<WithNotes>

```rust title="lib.rs"
use anchor_lang::prelude::*;

// This is your program's public key and it will update
// automatically when you build the project.
// !tooltip[/declare_id/] declare_id
declare_id!("11111111111111111111111111111111");

// !tooltip[/program/] program
#[program]
mod hello_anchor {
    use super::*;
    // !tooltip[/data: u64/] parameter
    pub fn initialize(ctx: Context<Initialize>, data: u64) -> Result<()> {
        // !tooltip[/ctx.accounts.new_account.data = data/] initialize
        ctx.accounts.new_account.data = data;
        // !tooltip[/msg!/] msg
        msg!("Changed data to: {}!", data); // Message will show up in the tx logs
        Ok(())
    }
}

// !tooltip[/Accounts/] accounts
#[derive(Accounts)]
pub struct Initialize<'info> {
    // We must specify the space in order to initialize an account.
    // First 8 bytes are default account discriminator,
    // next 8 bytes come from NewAccount.data being type u64.
    // (u64 = 64 bits unsigned integer = 8 bytes)
    // !tooltip[/account/] constraints
    #[account(
      // !tooltip[/init/] init
      init,
      // !tooltip[/payer/] payer
      payer = signer,
      // !tooltip[/space/] space
      space = 8 + 8
    )]
    pub new_account: Account<'info, NewAccount>,
    // !tooltip[/account/] constraints
    #[account(mut)]
    pub signer: Signer<'info>,
    pub system_program: Program<'info, System>,
}

// !tooltip[/account/] account
#[account]
pub struct NewAccount {
    data: u64
}
```

### !declare_id

Адрес вашей программы в сети. Он будет обновлен при сборке проекта

### !program

Модуль, содержащий инструкции программы

### !accounts

Аннотирует структуру, содержащую аккаунты программы

### !account

Структура, представляющая типы данных аккаунтов, которые эта программа может
создавать и владеть ими

### !constraints

Ограничения аккаунтов. Например, общие проверки или функции, которые повышают
безопасность программы

### !parameter

Значение, передаваемое инструкции при ее вызове

### !initialize

Обращается к полю `data` аккаунта `new_account` и устанавливает его значение
равным `data: u64`

### !msg

Макрос _rs`msg!`_ записывает сообщение в логи программы транзакции

### !init

Вызывает System Program для создания нового аккаунта и делает эту программу
владельцем. Как владелец программы, эта программа может затем записывать данные
в аккаунт.

### !payer

Аккаунт, который оплачивает создание нового аккаунта.

### !space

Количество байтов, выделяемых для аккаунта.

</WithNotes>

<Accordions>

<Accordion title="Объяснение">

<WithMentions>

- Макрос [`declare_id!`](mention:one) указывает on-chain адрес вашей программы.
  Solana Playground автоматически обновит этот адрес при сборке программы.

```rs
// !mention one
declare_id!("11111111111111111111111111111111");
```

</WithMentions>

<WithMentions>

- Атрибут [`#[program]`](mention:one) аннотирует модуль, содержащий функции,
  которые представляют инструкции программы.

```rs
// !mention one
#[program]
mod hello_anchor {
    use super::*;
    // !mention two
    pub fn initialize(ctx: Context<Initialize>, data: u64) -> Result<()> {
        // !mention three
        ctx.accounts.new_account.data = data;
        msg!("Changed data to: {}!", data); // Message will show up in the tx logs
        Ok(())
    }
}
```

В этом примере инструкция [`initialize`](mention:two) принимает два параметра:

- `ctx: Context<Initialize>`: Передаёт аккаунты, необходимые для этой
  инструкции, в функцию, как указано в структуре `Initialize`.
- `data: u64`: Пользовательский параметр, который вы должны указать при вызове
  инструкции.

Тело функции [устанавливает поле `data` аккаунта `new_account`](mention:three) в
переданное значение аргумента `data`, а затем выводит сообщение в логи
программы.

</WithMentions>

<WithMentions>

- Атрибут [`#[derive(Accounts)]`](mention:one) определяет структуру, которая
  указывает аккаунты, необходимые для конкретной инструкции. Каждое поле
  представляет отдельный аккаунт.

  Anchor автоматически обрабатывает стандартные проверки безопасности, связанные
  с валидацией аккаунтов, через типы полей (например, `Signer<'info>`) и
  ограничения (например, `#[account(mut)]`).

  ```rs
  // !mention one
  #[derive(Accounts)]
  pub struct Initialize<'info> {
      #[account(init, payer = signer, space = 8 + 8)]
      pub new_account: Account<'info, NewAccount>,
      #[account(mut)]
      pub signer: Signer<'info>,
      pub system_program: Program<'info, System>,
  }
  ```

</WithMentions>

<WithMentions>

- Атрибут [`#[account]`](mention:one) определяет структуру, которая представляет
  тип данных для аккаунтов, создаваемых и принадлежащих этой программе.

```rs
// !mention one
#[account]
pub struct NewAccount {
  data: u64
}
```

</WithMentions>

</Accordion>
</Accordions>

</Step>

<Step>

### Сборка и развертывание программы

1. Скомпилируйте код программы

```terminal
$ build
Building...
Build successful. Completed in 1.46s.
```

<Callout>
  Ваш [program ID](/docs/intro/quick-start/deploying-programs), определённый в
  _rs`declare_id!()`_, теперь будет обновлён.
</Callout>

2. Добавьте SOL в ваш кошелёк _(опционально)_

Развертывание программы требует наличия SOL в ончейн-аккаунте программы. При
необходимости
[добавьте SOL в ваш кошелёк](/docs/intro/quick-start#add-sol-to-your-wallet)
перед переходом к шагу 3.

3. Разверните программу в сети

```terminal
$ deploy
Deploying... This could take a while depending on the program size and network conditions.
Warning: 1 transaction not confirmed, retrying...
Deployment successful. Completed in 19s.
```

<Callout type="info">
  После развертывания байт-код программы сохраняется в исполняемом аккаунте
  программы.
</Callout>

**Примечание:** Если вы предпочитаете, вы можете использовать кнопки **Сборка**
и **Развертывание** на левой панели, как показано на скриншоте ниже.

![Сборка и развертывание](/assets/docs/intro/quickstart/pg-build-deploy.png)

</Step>

<Step>

### Тестирование программы

<WithMentions>

1. Откройте тестовый файл с именем `tests/anchor.test.ts`. В этом файле
   показано, как вызывать структуру [`initialize`](mention:one) программы с
   клиента.

```ts title="anchor.test.ts"
// No imports needed: web3, anchor, pg and more are globally available

describe("Test", () => {
  it("initialize", async () => {
    // Generate keypair for the new account
    const newAccountKp = new web3.Keypair();

    // Send transaction
    const data = new BN(42);
    const txHash = await pg.program.methods
      // !mention one
      .initialize(data)
      .accounts({
        newAccount: newAccountKp.publicKey,
        signer: pg.wallet.publicKey,
        systemProgram: web3.SystemProgram.programId
      })
      .signers([newAccountKp])
      .rpc();
    console.log(`Use 'solana confirm -v ${txHash}' to see the logs`);

    // Confirm transaction
    await pg.connection.confirmTransaction(txHash);

    // Fetch the created account
    const newAccount = await pg.program.account.newAccount.fetch(
      newAccountKp.publicKey
    );

    console.log("On-chain data is:", newAccount.data.toString());

    // Check whether the data on-chain is equal to local 'data'
    assert(data.eq(newAccount.data));
  });
});
```

</WithMentions>

2. Запустите тестовый файл

```terminal
$ test
Running tests...
  hello_anchor.test.ts:
  hello_anchor
    Use 'solana confirm -v 3TewJtiUz1EgtT88pLJHvKFzqrzDNuHVi8CfD2mWmHEBAaMfC5NAaHdmr19qQYfTiBace6XUmADvR4Qrhe8gH5uc' to see the logs
    On-chain data is: 42
    ✔ initialize (961ms)
  1 passing (963ms)
```

3. Сохраните хеш транзакции (`TxHash`), который содержится в выводе

(В приведённом выше примере хеш транзакции —
`3TewJtiUz1EgtT88pLJHvKFzqrzDNuHVi8CfD2mWmHEBAaMfC5NAaHdmr19qQYfTiBace6XUmADvR4Qrhe8gH5uc`.)

**Примечание:** При желании вы можете использовать кнопку `Test` на левой
панели.

![Запуск теста](/assets/docs/intro/quickstart/pg-test.png)

4. Запустите `solana confirm -v [TxHash]`, чтобы просмотреть логи транзакции

```terminal
$ solana confirm -v 3TewJtiUz1EgtT88pLJHvKFzqrzDNuHVi8CfD2mWmHEBAaMfC5NAaHdmr19qQYfTiBace6XUmADvR4Qrhe8gH5uc
RPC URL: https://api.devnet.solana.com
Default Signer: Playground Wallet
Commitment: confirmed

Transaction executed in slot 308150984:
  Block Time: 2024-06-25T12:52:05-05:00
  Version: legacy
  Recent Blockhash: 7AnZvY37nMhCybTyVXJ1umcfHSZGbngnm4GZx6jNRTNH
  Signature 0: 3TewJtiUz1EgtT88pLJHvKFzqrzDNuHVi8CfD2mWmHEBAaMfC5NAaHdmr19qQYfTiBace6XUmADvR4Qrhe8gH5uc
  Signature 1: 3TrRbqeMYFCkjsxdPExxBkLAi9SB2pNUyg87ryBaTHzzYtGjbsAz9udfT9AkrjSo1ZjByJgJHBAdRVVTZv6B87PQ
  Account 0: srw- 3z9vL1zjN6qyAFHhHQdWYRTFAcy69pJydkZmSFBKHg1R (fee payer)
  Account 1: srw- c7yy8zdP8oeZ2ewbSb8WWY2yWjDpg3B43jk3478Nv7J
  Account 2: -r-- 11111111111111111111111111111111
  Account 3: -r-x 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r
  Instruction 0
    Program:   2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r (3)
    Account 0: c7yy8zdP8oeZ2ewbSb8WWY2yWjDpg3B43jk3478Nv7J (1)
    Account 1: 3z9vL1zjN6qyAFHhHQdWYRTFAcy69pJydkZmSFBKHg1R (0)
    Account 2: 11111111111111111111111111111111 (2)
    Data: [175, 175, 109, 31, 13, 152, 155, 237, 42, 0, 0, 0, 0, 0, 0, 0]
  Status: Ok
    Fee: ◎0.00001
    Account 0 balance: ◎5.47001376 -> ◎5.46900152
    Account 1 balance: ◎0 -> ◎0.00100224
    Account 2 balance: ◎0.000000001
    Account 3 balance: ◎0.00139896
  Log Messages:
    Program 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r invoke [1]
    Program log: Instruction: Initialize
    Program 11111111111111111111111111111111 invoke [2]
    Program 11111111111111111111111111111111 success
    Program log: Changed data to: 42!
    Program 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r consumed 5661 of 200000 compute units
    Program 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r success

Confirmed
```

4. Просмотрите детали транзакции

Откройте либо [SolanaFM](https://solana.fm/), либо
[Solana Explorer](https://explorer.solana.com/?cluster=devnet) и найдите хэш
транзакции.

<Callout type="warn">
  **Solana Explorer:** Установите кластер сети на `devnet`, чтобы он совпадал с
  Solana Playground.
</Callout>

</Step>

<Step>

### Закрытие программы

Закройте программу, чтобы вернуть SOL, выделенные для ончейн-программы. Для
этого запустите {/* CODE_PLACEHOLDER_8bc4245b2bb4ee5931d2d6ac95f50814 */},
используя адрес программы, сохранённый в макросе _rs`declare_id!()`_.

```terminal
$ solana program close 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r
Closed Program Id 2VvQ11q8xrn5tkPNyeraRsPaATdiPx8weLAD8aD4dn2r, 2.79511512 SOL reclaimed
```

<Callout type="warn">
  Программы могут быть **только** закрыты или обновлены аккаунтом с полномочиями
  на обновление. Ваш кошелёк Playground автоматически становится владельцем
  полномочий на обновление для каждой программы при её развертывании. Если
  полномочия на обновление отозваны, программа становится неизменяемой.
</Callout>

Поздравляем! Вы создали и развернули свою первую программу Solana с
использованием Anchor.

</Step>

</Steps>

## Следующие шаги

Теперь, когда вы развернули свою первую программу, вы готовы изучить
[адреса, производные от программ](/docs/intro/quick-start/program-derived-address)
и [вызов между программами](/docs/intro/quick-start/cross-program-invocation).
