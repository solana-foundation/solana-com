---
title: "Швидкий старт: прийом платежу"
description:
  Add a working payment button to a React app in about five minutes and take
  your first stablecoin payment.
---

У результаті ви отримаєте кнопку оплати у React-додатку, яка підключає гаманець, дозволяє платнику вибрати токен і підтверджує реальний платіж на вашу адресу. Вам знадобляться Node.js 20+, додаток на React або Next.js (щойно створений `create-next-app` підійде), а також гаманець для оплати.

<Callout type="caution">
  Commerce Kit наразі перебуває в бета-версії. API може змінитися до стабільного релізу.
</Callout>

## 1. Встановіть Commerce Kit

```bash
pnpm add @solana-commerce/kit
```

## 2. Додайте кнопку

Розмістіть компонент `PaymentButton` будь-де у вашому додатку та вкажіть гаманець продавця — адресу, якою ви керуєте:

```tsx
import { PaymentButton } from "@solana-commerce/kit";

function Checkout() {
  return (
    <PaymentButton
      config={{
        merchant: { name: "My Store", wallet: "your-wallet-address" },
        mode: "tip"
      }}
      onPaymentSuccess={(signature) => {
        console.log("Payment confirmed:", signature);
      }}
    />
  );
}
```

Компонент самостійно обробляє підключення гаманця, вибір токена, обробку транзакцій і стан інтерфейсу. `mode: "tip"` дозволяє платнику самостійно вибрати суму; перегляньте [режими оплати](/docs/payments/accept-payments/payment-button) для оплати за фіксованою ціною та оформлення замовлень у кошику.

## Спробуйте у браузері

Налаштуйте кнопку оплати та запустіть процес чайових, не залишаючи цієї сторінки.

<iframe
  src="https://launch.solana.com/products/commercekit/playground"
  title="Commerce Kit Playground"
  width="100%"
  height="1080"
  loading="lazy"
  allow="clipboard-read; clipboard-write; payment"
  referrerPolicy="strict-origin-when-cross-origin"
  style={{
    display: "block",
    width: "100%",
    border: "1px solid rgba(148, 163, 184, 0.25)",
    borderRadius: "12px",
    background: "#fff"
  }}
/>

[Відкрити Commerce Kit Playground на повний екран](https://launch.solana.com/products/commercekit/playground)

## 3. Прийміть платіж

Запустіть додаток, натисніть кнопку, підключіть гаманець і здійсніть оплату. Зворотний виклик `onPaymentSuccess` отримує підпис транзакції, який є вашою квитанцією: перевірте його за допомогою [getTransaction](/docs/rpc/http/gettransaction) або в будь-якому провіднику.

## Що робити далі

- [Як працюють платежі в Solana](/docs/payments/how-payments-work) — гаманці, стейблкоїни, token account та комісії
- [Перевірка платежів](/docs/payments/accept-payments/verification-tools) — підтвердіть платіж на стороні сервера перед виконанням замовлення
- [Надсилання платежів](/docs/payments/send-payments) — виплати та перекази з вашого власного бекенду
- [Готовність до продакшну](/docs/tools/production-readiness) — перш ніж переходити до реальних грошей
