---
title: Термінологія
description:
  Дізнайтеся про основну термінологію, що використовується в блокчейні Solana та
  моделях розробки.
keywords:
  - терміни
  - словник
  - визначення
  - визначити
  - моделі програмування
---

Наступні терміни використовуються в документації Solana та екосистемі розробки.

## обліковий запис

Запис у реєстрі Solana, який містить дані або є виконуваною програмою.

Як і рахунок у традиційному банку, обліковий запис Solana може містити кошти,
які називаються [lamports](#lamport). Як файл у Linux, він адресується ключем,
який часто називають [публічним ключем](#public-key-pubkey) або pubkey.

Ключ може бути одним із:

- публічний ключ ed25519
- адреса облікового запису, похідна від програми (32-байтове значення, примусово
  виведене з кривої ed25519)
- хеш публічного ключа ed25519 з 32-символьним рядком

## власник облікового запису

Адреса програми, яка володіє обліковим записом. Тільки програма-власник здатна
змінювати обліковий запис.

Див. також [повноваження](#authority).

## застосунок

Фронтенд-застосунок, який взаємодіє з кластером Solana.

## повноваження

Адреса користувача, який має певний вид дозволу на обліковий запис.

Наприклад:

- Можливість створювати нові токени надається обліковому запису, який є
  'повноваженням емісії' для випуску токенів.
- Можливість оновлювати програму надається обліковому запису, який є
  'повноваженням оновлення' програми.

## стан банку

Результат інтерпретації всіх програм у реєстрі на певній
[висоті тіку](#tick-height). Він включає принаймні набір усіх
[облікових записів](#account), що містять ненульові
[нативні токени](#native-token).

## блок

Безперервний набір [записів](#entry) у реєстрі, охоплений
[голосуванням](#ledger-vote). [Лідер](#leader) створює щонайбільше один блок на
[slot](#slot).

## blockhash

Унікальне значення ([хеш](#hash)), яке ідентифікує запис (блок). Solana обчислює
blockhash з останнього [ідентифікатора запису](#entry-id) блоку.

## висота блоку

Кількість [блоків](#block) під поточним блоком. Перший блок після
[початкового блоку](#genesis-block) має висоту один.

## bootstrap validator

[Validator](#validator), який створює початковий (перший) [блок](#block)
блокчейну.

## BPF loader

Програма Solana, яка володіє та завантажує
[BPF](/docs/core/programs#berkeley-packet-filter-bpf)
[onchain програми](#onchain-program), дозволяючи програмі взаємодіяти з
середовищем виконання.

## клієнт

Комп'ютерна програма, яка отримує доступ до мережі серверів Solana
[кластер](#cluster).

## підтвердження

Міра підтвердження мережею для [блоку](#block).

## компактний формат масиву

Компактний масив — це масив, серіалізований у такому форматі:

- Довжина масиву (закодована як
  [compact-u16](https://github.com/anza-xyz/agave/blob/v2.1.13/short-vec/src/lib.rs))
- Елементи масиву, перераховані один за одним

![Компактний формат масиву](/assets/docs/core/transactions/compact_array_format.png)

Компактний формат масиву використовується для кодування як масиву
[адрес облікових записів](/docs/core/transactions#account-addresses), так і
масиву [інструкцій](/docs/core/transactions#instructions) у повідомленнях
транзакцій.

## кластер

Набір [валідаторів](#validator), що підтримують єдиний [реєстр](#ledger).

## обчислювальний бюджет

Максимальна кількість [обчислювальних одиниць](#compute-units), що споживаються
за одну транзакцію.

## обчислювальні одиниці

Найменша одиниця виміру споживання обчислювальних ресурсів блокчейну.
Обчислювальні одиниці використовуються для вимірювання обчислювальних ресурсів,
використаних для обробки транзакції.

## час підтвердження

Тривалість реального часу між створенням [лідером](#leader) [запису тіку](#tick)
та створенням [підтвердженого блоку](#confirmed-block).

## підтверджений блок

[Блок](#block), який отримав [супербільшість](#supermajority)
[голосів реєстру](#ledger-vote).

## площина керування

Мережа обміну інформацією, що з'єднує всі [вузли](#node) [кластера](#cluster).

## період охолодження

Певна кількість [епох](#epoch) після деактивації [стейку](#stake), протягом яких
він поступово стає доступним для виведення. Протягом цього періоду стейк
вважається "деактивованим". Більше інформації про:
[розігрів та охолодження](https://docs.anza.xyz/implemented-proposals/staking-rewards#stake-warmup-cooldown-withdrawal)

## одиниці вартості

Також називається `transaction cost`, є комплексною оцінкою всіх ресурсів,
необхідних для обробки транзакції, що вимірюється в
[обчислювальних одиницях](#compute-units). Більше інформації
[тут](https://www.anza.xyz/blog/why-solana-transaction-costs-and-compute-units-matter-for-developers).

## кредит

Див. [кредит голосування](#vote-credit).

## міжпрограмний виклик (CPI)

Виклик з однієї [онлайн-програми](#onchain-program) до іншої. Для отримання
додаткової інформації див. [виклики між програмами](/docs/core/cpi).

## площина даних

Мультикаст-мережа, яка використовується для ефективної перевірки
[записів](#entry) та досягнення консенсусу.

## дрон

Позаланцюговий сервіс, який виступає зберігачем приватного ключа користувача.
Зазвичай служить для перевірки та підписання транзакцій.

## запис

Запис у [реєстрі](#ledger), який є або [тіком](#tick), або
[записом транзакції](#transactions-entry).

## ідентифікатор запису

Стійкий до пошуку прообразу [хеш](#hash) кінцевого вмісту запису, який виступає
глобально унікальним ідентифікатором [запису](#entry). Хеш служить доказом:

- Запис був створений після певного проміжку часу
- Вказані [транзакції](#transaction) є тими, що включені в запис
- Позиція запису відносно інших записів у [реєстрі](#ledger)

Дивіться [доказ історії](#proof-of-history-poh).

## epoch

Час, тобто кількість [слотів](#slot), протягом якого діє
[розклад лідерів](#leader-schedule).

## рахунок комісій

Рахунок комісій у транзакції — це рахунок, який оплачує вартість включення
транзакції в реєстр. Це перший рахунок у транзакції. Цей рахунок повинен бути
оголошений як такий, що читається-записується (доступний для запису) в
транзакції, оскільки оплата транзакції зменшує баланс рахунку.

## остаточність

Коли вузли, що представляють 2/3 [ставки](#stake), мають спільний
[корінь](#root).

## форк

[Реєстр](#ledger), отриманий із спільних записів, але потім розділений.

## блок генезису

Перший [блок](#block) у ланцюзі.

## конфігурація генезису

Конфігураційний файл, який готує [реєстр](#ledger) для
[блоку генезису](#genesis-block).

## хеш

Цифровий відбиток послідовності байтів.

## інфляція

Збільшення пропозиції токенів з часом, що використовується для фінансування
винагород за валідацію та для фінансування подальшого розвитку Solana.

## внутрішня інструкція

Див. [міжпрограмний виклик](#cross-program-invocation-cpi).

## інструкція

Виклик для запуску конкретного [обробника інструкцій](#instruction-handler) у
[програмі](#program). Інструкція також визначає, які рахунки вона хоче прочитати
або змінити, та додаткові дані, які слугують допоміжним вводом для
[обробника інструкцій](#instruction-handler). [Клієнт](#client) повинен включити
принаймні одну інструкцію в [транзакцію](#transaction), і всі інструкції мають
бути виконані, щоб транзакція вважалася успішною.

## обробник інструкцій

Обробники інструкцій — це функції [програми](#program), які обробляють
[інструкції](#instruction) з [транзакцій](#transaction). Обробник інструкцій
може містити один або більше
[міжпрограмних викликів](#cross-program-invocation-cpi).

## keypair

[Публічний ключ](#public-key-pubkey) та відповідний
[приватний ключ](#private-key) для доступу до облікового запису.

## lamport

Дробовий [нативний токен](#native-token) вартістю 0.000000001 [sol](#sol).

> У межах обчислювального бюджету кількість
> _[мікро-lamport](https://github.com/solana-labs/solana/blob/ced8f6a512c61e0dd5308095ae8457add4a39e94/program-runtime/src/prioritization_fee.rs#L1-L2)_
> використовується при розрахунку
> [комісій за пріоритизацію](#prioritization-fee).

## лідер

Роль [валідатора](#validator), коли він додає [записи](#entry) до
[леджера](#ledger).

## розклад лідерів

Послідовність [публічних ключів](#public-key-pubkey) [валідаторів](#validator),
зіставлених зі [слотами](#slot). Кластер використовує розклад лідерів, щоб
визначити, який валідатор є [лідером](#leader) у будь-який момент часу.

## леджер

Список [записів](#entry), що містять [транзакції](#transaction), підписані
[клієнтами](#client). Концептуально це можна простежити до
[блоку генезису](#genesis-block), але фактичний леджер [валідатора](#validator)
може мати лише новіші [блоки](#block) для зменшення обсягу зберігання, оскільки
старіші за дизайном не потрібні для валідації майбутніх блоків.

## голосування в леджері

[Хеш](#hash) [стану валідатора](#bank-state) на певній
[висоті тіку](#tick-height). Він включає підтвердження [валідатора](#validator),
що отриманий [блок](#block) було перевірено, а також обіцянку не голосувати за
конфліктуючий [блок](#block) (тобто [форк](#fork)) протягом певного періоду
часу, періоду [блокування](#lockout).

## легкий клієнт

Тип [клієнта](#client), який може перевірити, що він вказує на дійсний
[кластер](#cluster). Він виконує більше перевірок реєстру, ніж
[тонкий клієнт](#thin-client), і менше, ніж [validator](#validator).

## завантажувач

[Програма](#program) з можливістю інтерпретувати бінарне кодування інших програм
у мережі.

## блокування

Тривалість часу, протягом якого [validator](#validator) не може
[голосувати](#ledger-vote) за інший [форк](#fork).

## повідомлення

Структурований вміст [транзакції](#transaction). Зазвичай містить заголовок,
масив адрес рахунків, нещодавній [blockhash](#blockhash) та масив
[інструкцій](#instruction).

Дізнайтеся більше про
[формат повідомлень всередині транзакцій](/docs/core/transactions#message-header)
тут.

## мікро-lamport

1 000 000 мікро-lamport = 1 lamport

## коефіцієнт Накамото

Міра децентралізації, коефіцієнт Накамото — це найменша кількість незалежних
суб'єктів, які можуть діяти колективно, щоб зупинити блокчейн. Термін був
введений Баладжі С. Срініваcаном і Леландом Лі у
[Quantifying Decentralization](https://news.earn.com/quantifying-decentralization-e39db233c28e).

## нативний токен

[Токен](#token), який використовується для відстеження роботи, виконаної
[вузлами](#node) у кластері.

## вузол

Комп'ютер, що бере участь у [кластері](#cluster).

## кількість вузлів

Кількість [validator-ів](#validator), що беруть участь у [кластері](#cluster).

## програма в мережі

Виконуваний код у блокчейні Solana, який інтерпретує [інструкції](#instruction),
надіслані всередині кожної [транзакції](#transaction), для читання та
модифікації рахунків, над якими він має контроль. Ці програми часто називають
"[_смарт-контрактами_](/docs/core/programs)" в інших блокчейнах.

## PoH

Див. [Доказ історії](#proof-of-history-poh).

## пункт

Зважений [кредит](#credit) у системі винагород. У режимі винагород
[validator](#validator)
[rewards regime](https://docs.anza.xyz/consensus/stake-delegation-and-rewards),
кількість пунктів, належних [ставці](#stake) під час погашення, є добутком
зароблених [кредитів голосування](#vote-credit) та кількості lamport у ставці.

## приватний ключ

Приватний ключ [keypair](#keypair).

## програма

Дивіться [програма в мережі](#onchain-program).

## рахунок, похідний від програми (PDA)

Рахунок, чий підписуючий орган є програмою, і тому не контролюється приватним
ключем, як інші рахунки.

## ідентифікатор програми

Публічний ключ [рахунку](#account), що містить [програму](#program).

## доказ історії (PoH)

Стек доказів, кожен з яких доводить, що певні дані існували до створення доказу
і що пройшов точний проміжок часу після попереднього доказу. Як і
[VDF](#verifiable-delay-function-vdf), Доказ історії можна перевірити за менший
час, ніж потрібно для його створення.

## плата за пріоритизацію

Додаткова плата, яку користувач може вказати в [інструкції](#instruction)
обчислювального бюджету для пріоритизації своїх [транзакцій](#transaction).

Плата за пріоритизацію розраховується шляхом множення запитаних максимальних
обчислювальних одиниць на ціну обчислювальної одиниці (вказану в приростах
0,000001 lamport за обчислювальну одиницю), округлену до найближчого lamport.

Транзакції повинні запитувати мінімальну кількість обчислювальних одиниць,
необхідних для виконання, щоб мінімізувати плату.

## публічний ключ (pubkey)

Публічний ключ [keypair](#keypair).

## rent

Плата, яку сплачують [Рахунки](#account) та [Програми](#program) для зберігання
даних у блокчейні. Коли на рахунках недостатньо балансу для оплати rent, вони
можуть бути зібрані Збирачем сміття.

Дивіться також [звільнення від rent](#rent-exempt) нижче. Дізнайтеся більше про
rent тут: [Що таке rent?](/docs/intro/rent).

## звільнення від rent

Рахунки, які підтримують мінімальний баланс lamport, пропорційний кількості
даних, що зберігаються на рахунку. Всі новостворені рахунки зберігаються в
мережі постійно, доки рахунок не буде закрито. Неможливо створити рахунок, який
падає нижче порогу звільнення від rent.

## root

[Блок](#block) або [slot](#slot), який досяг максимального [lockout](#lockout)
на [validator](#validator). Root — це найвищий блок, який є предком усіх
активних форків на валідаторі. Усі блоки-предки root також транзитивно є root.
Блоки, які не є предками та не є нащадками root, виключаються з розгляду для
консенсусу і можуть бути відкинуті.

## runtime

Компонент [validator](#validator), відповідальний за виконання
[програм](#program).

## Sealevel

Паралельне середовище виконання Solana для [onchain програм](#onchain-program).

## shred

Частина [блоку](#block); найменша одиниця, що передається між
[validator](#validator).

## signature

64-байтовий ed25519 підпис R (32 байти) та S (32 байти). З вимогою, що R є
упакованою точкою Едвардса не малого порядку, а S є скаляром у діапазоні
`0 <= S < L`. Ця вимога гарантує відсутність підробки підпису. Кожна транзакція
повинна мати принаймні один підпис для
[облікового запису комісії](#fee-account). Таким чином, перший підпис у
транзакції може розглядатися як [ідентифікатор транзакції](#transaction-id)

## skip rate

Відсоток [пропущених слотів](#skipped-slot) від загальної кількості лідерських
слотів у поточному epoch. Цей показник може вводити в оману, оскільки має високу
варіативність після межі епохи, коли розмір вибірки малий, а також для
валідаторів з невеликою кількістю лідерських слотів, однак також може бути
корисним для виявлення неправильних конфігурацій вузлів.

## skipped slot

Минулий [slot](#slot), який не створив [блок](#block), оскільки лідер був офлайн
або [форк](#fork), що містив slot, був відкинутий на користь кращої альтернативи
за консенсусом кластера. Пропущений slot не з'явиться як предок для блоків у
наступних слотах, не збільшить [висоту блоку](#block-height) і не закінчить
термін дії найстарішого `recent_blockhash`.

Чи був slot пропущений, можна визначити лише тоді, коли він стає старішим за
останній [закріплений](#root) (тобто не пропущений) slot.

## slot

Період часу, протягом якого кожен [лідер](#leader) приймає транзакції та створює
[блок](#block).

Разом slots створюють логічний годинник. Slots упорядковані послідовно та не
перекриваються, охоплюючи приблизно однакові проміжки реального часу відповідно
до [PoH](#proof-of-history-poh).

## смарт-контракт

Дивіться [програма в мережі](#onchain-program).

## SOL

[Нативний токен](#native-token) Solana [кластера](#cluster).

## Бібліотека програм Solana (SPL)

[Бібліотека програм](https://spl.solana.com/) на Solana, таких як spl-token, що
полегшує виконання завдань, як-от створення та використання токенів.

## стейк

Токени, конфісковані [кластером](#cluster), якщо можна довести зловмисну
поведінку [валідатора](#validator).

## якість обслуговування, зважена за стейком (SWQoS)

SWQoS дозволяє
[надавати перевагу транзакціям, що надходять від валідаторів зі стейком](/developers/guides/advanced/stake-weighted-qos).

## супербільшість

2/3 [кластера](#cluster).

## sysvar

Системний [обліковий запис](#account).
[Sysvars](https://docs.anza.xyz/runtime/sysvars) надають інформацію про стан
кластера, таку як поточна висота тіка, значення винагород [поінтів](#point)
тощо. Програми можуть отримувати доступ до Sysvars через обліковий запис Sysvar
(pubkey) або шляхом запиту через системний виклик.

## тонкий клієнт

Тип [клієнта](#client), який довіряє тому, що він спілкується з дійсним
[кластером](#cluster).

## тік

Запис [ledger](#ledger), який оцінює тривалість настінного годинника.

## висота тіка

N-й [тік](#tick) у [леджері](#ledger).

## токен

Цифровий актив, що може передаватися.

## Token Extensions Program

[Token Extensions Program](https://spl.solana.com/token-2022) має program ID
`TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb` і включає всі ті самі функції, що
й [Token Program](#token-program), але постачається з розширеннями, такими як
конфіденційні перекази, користувацька логіка переказів, розширені метадані та
багато іншого.

## токен-мінт

Обліковий запис, який може виробляти (або 'карбувати') токени. Різні токени
розрізняються за їхніми унікальними адресами токен-мінту.

## Token Program

[Token Program](https://spl.solana.com/token) має program ID
`TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA` і надає базові можливості
переказу, заморожування та карбування токенів.

## tps

[Транзакцій](#transaction) за секунду.

## tpu

[Блок обробки транзакцій](https://docs.anza.xyz/validator/tpu).

## транзакція

Одна або більше [інструкцій](#instruction), підписаних [клієнтом](#client) за
допомогою одного або кількох [ключових пар](#keypair) і виконаних атомарно з
лише двома можливими результатами: успіх або невдача.

## ідентифікатор транзакції

Перший [підпис](#signature) у [транзакції](#transaction), який можна
використовувати для унікальної ідентифікації транзакції в повному
[реєстрі](#ledger).

## підтвердження транзакції

Кількість [підтверджених блоків](#confirmed-block) з моменту прийняття
транзакції до [реєстру](#ledger). Транзакція вважається завершеною, коли її блок
стає [кореневим](#root).

## запис транзакцій

Набір [транзакцій](#transaction), які можуть виконуватися паралельно.

## tvu

[Блок валідації транзакцій](https://docs.anza.xyz/validator/tvu).

## validator

Повноцінний учасник мережевого [кластера](#cluster) Solana, який виробляє нові
[блоки](#block). Validator перевіряє транзакції, додані до [реєстру](#ledger)

## VDF

Див. [функція верифікованої затримки](#verifiable-delay-function-vdf).

## функція верифікованої затримки (VDF)

Функція, яка потребує фіксованого часу для виконання та створює доказ свого
виконання, який потім можна перевірити за менший час, ніж потрібно для його
створення.

## голосування

Див. [голосування в реєстрі](#ledger-vote).

## кредит голосування

Облік винагород для [валідаторів](#validator). Кредит голосування нараховується
валідатору на його рахунок голосування, коли валідатор досягає [кореня](#root).

## гаманець

Набір [ключових пар](#keypair), який дозволяє користувачам керувати своїми
коштами.

## період розігріву

Певна кількість [епох](#epoch) після делегування [ставки](#stake), протягом яких
вона поступово стає ефективною. Протягом цього періоду ставка вважається
"активуючою". Більше інформації про:
[розігрів та охолодження](https://docs.anza.xyz/consensus/stake-delegation-and-rewards#stake-warmup-cooldown-withdrawal)
