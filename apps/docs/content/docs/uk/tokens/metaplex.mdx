---
title: Метадані Metaplex
description:
  Дізнайтеся, як додавати метадані до SPL-токенів за допомогою програми Metaplex
  Token Metadata Program.
---

Стандартні SPL-токени не включають метадані, такі як назва, символ або
зображення.
[Metaplex Token Metadata Program](https://developers.metaplex.com/token-metadata)
вирішує цю проблему, створюючи обліковий запис метаданих, пов'язаний з кожним
mint-токеном.

<Callout type="info">
  Цей посібник охоплює Metaplex Token Metadata Program, яку можна
  використовувати для стандартних SPL-токенів та Token-2022. Щоб використовувати
  розширення метаданих Token-2022, перегляньте [посібник з розширення
  метаданих](/docs/tokens/extensions/metadata), яке зберігає метадані
  безпосередньо в mint-обліковому записі.
</Callout>

## Як працюють метадані токенів

Token Metadata Program створює Program Derived Address (PDA) для кожного
mint-токена. Цей обліковий запис метаданих зберігає он-чейн інформацію, таку як
назва та символ токена, а також URI, що вказує на оф-чейн JSON-метадані
(зображення, описи тощо).

```text
┌─────────────────┐       ┌─────────────────────┐
│   Mint Account  │       │  Metadata Account   │
│                 │       │  (PDA)              │
│  - Supply       │◄──────│  - Name             │
│  - Decimals     │       │  - Symbol           │
│  - Authority    │       │  - URI              │
└─────────────────┘       │  - Seller Fee       │
                          │  - Creators         │
                          └─────────────────────┘
```

Метадані PDA виводяться з seeds: `["metadata", program_id, mint_address]`

## Створення токена з метаданими

Інструкція `createV1` створює як mint account, так і його метадані в одній
транзакції.

### Typescript

<CodeTabs storage="token-ts">

```ts !! title="Kit"
import {
  airdropFactory,
  appendTransactionMessageInstructions,
  createSolanaRpc,
  createSolanaRpcSubscriptions,
  createTransactionMessage,
  generateKeyPairSigner,
  lamports,
  pipe,
  sendAndConfirmTransactionFactory,
  setTransactionMessageFeePayerSigner,
  setTransactionMessageLifetimeUsingBlockhash,
  signTransactionMessageWithSigners
} from "@solana/kit";
import {
  getCreateV1InstructionAsync,
  TokenStandard
} from "@metaplex-foundation/mpl-token-metadata-kit";

// Create connection
const rpc = createSolanaRpc("http://127.0.0.1:8899");
const rpcSubscriptions = createSolanaRpcSubscriptions("ws://127.0.0.1:8900");

// Generate keypairs
const payer = await generateKeyPairSigner();
const mint = await generateKeyPairSigner();

// Fund payer
await airdropFactory({ rpc, rpcSubscriptions })({
  recipientAddress: payer.address,
  lamports: lamports(1_000_000_000n),
  commitment: "confirmed"
});

// Create fungible token with metadata
const createInstruction = await getCreateV1InstructionAsync({
  mint,
  authority: payer,
  payer,
  name: "My Token",
  symbol: "MTK",
  uri: "https://example.com/token.json",
  sellerFeeBasisPoints: 0,
  tokenStandard: TokenStandard.Fungible
});

// Build and send transaction
const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();

const transactionMessage = pipe(
  createTransactionMessage({ version: 0 }),
  (tx) => setTransactionMessageFeePayerSigner(payer, tx),
  (tx) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, tx),
  (tx) => appendTransactionMessageInstructions([createInstruction], tx)
);

const signedTransaction =
  await signTransactionMessageWithSigners(transactionMessage);

await sendAndConfirmTransactionFactory({ rpc, rpcSubscriptions })(
  signedTransaction,
  { commitment: "confirmed" }
);

console.log("Mint Address:", mint.address);
```

```ts !! title="Kit Helper"
import {
  airdropFactory,
  appendTransactionMessageInstructions,
  createSolanaRpc,
  createSolanaRpcSubscriptions,
  createTransactionMessage,
  generateKeyPairSigner,
  lamports,
  pipe,
  sendAndConfirmTransactionFactory,
  setTransactionMessageFeePayerSigner,
  setTransactionMessageLifetimeUsingBlockhash,
  signTransactionMessageWithSigners
} from "@solana/kit";
import { createFungible } from "@metaplex-foundation/mpl-token-metadata-kit";

// Create connection
const rpc = createSolanaRpc("http://127.0.0.1:8899");
const rpcSubscriptions = createSolanaRpcSubscriptions("ws://127.0.0.1:8900");

// Generate keypairs
const payer = await generateKeyPairSigner();
const mint = await generateKeyPairSigner();

// Fund payer
await airdropFactory({ rpc, rpcSubscriptions })({
  recipientAddress: payer.address,
  lamports: lamports(1_000_000_000n),
  commitment: "confirmed"
});

// Use the createFungible helper for simpler API
const createInstruction = await createFungible({
  mint,
  authority: payer,
  payer,
  name: "My Token",
  symbol: "MTK",
  uri: "https://example.com/token.json",
  sellerFeeBasisPoints: 0
});

// Build and send transaction
const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();

const transactionMessage = pipe(
  createTransactionMessage({ version: 0 }),
  (tx) => setTransactionMessageFeePayerSigner(payer, tx),
  (tx) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, tx),
  (tx) => appendTransactionMessageInstructions([createInstruction], tx)
);

const signedTransaction =
  await signTransactionMessageWithSigners(transactionMessage);

await sendAndConfirmTransactionFactory({ rpc, rpcSubscriptions })(
  signedTransaction,
  { commitment: "confirmed" }
);

console.log("Mint Address:", mint.address);
```

</CodeTabs>

## Отримання метаданих токена

Щоб дізнатися, як отримати метадані для існуючих токенів, перегляньте рецепт у
cookbook
[Отримання метаданих токена](/developers/cookbook/tokens/fetch-token-metadata).

## Оновлення метаданих токена

Орган оновлення може змінювати метадані, якщо обліковий запис є змінним.

<CodeTabs storage="token-ts">

```ts !! title="Kit"
import {
  appendTransactionMessageInstructions,
  createSolanaRpc,
  createSolanaRpcSubscriptions,
  createTransactionMessage,
  pipe,
  sendAndConfirmTransactionFactory,
  setTransactionMessageFeePayerSigner,
  setTransactionMessageLifetimeUsingBlockhash,
  signTransactionMessageWithSigners
} from "@solana/kit";
import {
  getUpdateV1InstructionAsync,
  findMetadataPda,
  fetchMetadata
} from "@metaplex-foundation/mpl-token-metadata-kit";

const rpc = createSolanaRpc("http://127.0.0.1:8899");
const rpcSubscriptions = createSolanaRpcSubscriptions("ws://127.0.0.1:8900");

// authority must be a KeyPairSigner with update authority
const mintAddress = "YOUR_MINT_ADDRESS";

// Fetch current metadata to preserve existing values
const [metadataAddress] = await findMetadataPda({ mint: mintAddress });
const currentMetadata = await fetchMetadata(rpc, metadataAddress);

// Update metadata (must provide all data fields)
const updateInstruction = await getUpdateV1InstructionAsync({
  mint: mintAddress,
  authority, // Update authority signer
  payer: authority,
  data: {
    name: "Updated Token Name",
    symbol: "UPD",
    uri: "https://example.com/updated-token.json",
    sellerFeeBasisPoints: 100, // 1%
    creators:
      currentMetadata.data.creators.__option === "Some"
        ? currentMetadata.data.creators.value
        : null
  }
});

// Build and send transaction
const { value: latestBlockhash } = await rpc.getLatestBlockhash().send();

const transactionMessage = pipe(
  createTransactionMessage({ version: 0 }),
  (tx) => setTransactionMessageFeePayerSigner(authority, tx),
  (tx) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, tx),
  (tx) => appendTransactionMessageInstructions([updateInstruction], tx)
);

const signedTransaction =
  await signTransactionMessageWithSigners(transactionMessage);

await sendAndConfirmTransactionFactory({ rpc, rpcSubscriptions })(
  signedTransaction,
  { commitment: "confirmed" }
);

console.log("Metadata updated successfully");
```

</CodeTabs>

## Стандарти токенів

Token Metadata Program підтримує різні стандарти токенів:

| Стандарт                         | Опис                                                  | Випадок використання         |
| -------------------------------- | ----------------------------------------------------- | ---------------------------- |
| `Fungible`                       | Стандартний взаємозамінний токен з метаданими         | Валюти, бали                 |
| `FungibleAsset`                  | Взаємозамінний токен, що представляє унікальний актив | Напів-взаємозамінні предмети |
| `NonFungible`                    | NFT з Master Edition                                  | Унікальні твори мистецтва    |
| `ProgrammableNonFungible`        | NFT з примусовими роялті                              | Роялті для авторів           |
| `NonFungibleEdition`             | Друкована копія NFT                                   | Обмежені видання             |
| `ProgrammableNonFungibleEdition` | Друкована копія з примусовими роялті                  | Обмежені видання             |

```ts
import { TokenStandard } from "@metaplex-foundation/mpl-token-metadata-kit";

// For fungible tokens
tokenStandard: TokenStandard.Fungible;

// For NFTs
tokenStandard: TokenStandard.NonFungible;

// For programmable NFTs (enforced royalties)
tokenStandard: TokenStandard.ProgrammableNonFungible;
```

## Формат позаланцюгових метаданих

Поле `uri` вказує на JSON-файл, що містить розширені метадані. Стандартний
формат відповідає
[Стандарту метаданих токенів Metaplex](https://developers.metaplex.com/token-metadata/token-standard):

```json title="token-metadata.json"
{
  "name": "My Token",
  "symbol": "MTK",
  "description": "A description of the token",
  "image": "https://example.com/token-image.png",
  "external_url": "https://example.com",
  "attributes": [
    {
      "trait_type": "Category",
      "value": "Utility"
    }
  ],
  "properties": {
    "files": [
      {
        "uri": "https://example.com/token-image.png",
        "type": "image/png"
      }
    ]
  }
}
```

<Callout type="caution">
  Зберігайте свій JSON метаданих у надійному, постійному сховищі, такому як
  Arweave, IPFS або спеціалізована CDN. Якщо URI стане недоступним, гаманці та
  оглядачі не зможуть відобразити метадані вашого токена.
</Callout>

## Структура облікового запису метаданих

Обліковий запис метаданих у ланцюзі містить:

```rust
pub struct Metadata {
    pub key: Key,                              // Account type identifier
    pub update_authority: Pubkey,              // Can update metadata
    pub mint: Pubkey,                          // Associated mint
    pub name: String,                          // Token name (max 32 chars)
    pub symbol: String,                        // Token symbol (max 10 chars)
    pub uri: String,                           // URI to off-chain JSON (max 200 chars)
    pub seller_fee_basis_points: u16,          // Royalty % (100 = 1%)
    pub creators: Option<Vec<Creator>>,        // Creator list with shares
    pub primary_sale_happened: bool,           // Primary sale flag
    pub is_mutable: bool,                      // Can metadata be updated
    pub edition_nonce: Option<u8>,             // Edition nonce
    pub token_standard: Option<TokenStandard>, // Token type
    pub collection: Option<Collection>,        // Collection info
    pub uses: Option<Uses>,                    // Use tracking
}
```

## Найкращі практики

1. **Встановіть відповідну змінюваність**: Використовуйте `isMutable: false` для
   токенів, які ніколи не повинні змінюватися
2. **Використовуйте надійний хостинг URI**: Позаланцюгові метадані мають
   зберігатися у постійному сховищі
3. **Перевіряйте творців**: Адреси творців повинні бути підтверджені для
   підтвердження автентичності
4. **Враховуйте роялті**: Встановіть `sellerFeeBasisPoints` для роялті з
   вторинних продажів (маркетплейси можуть застосовувати їх або ні)
