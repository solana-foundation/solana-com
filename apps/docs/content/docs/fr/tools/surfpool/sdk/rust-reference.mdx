---
title: Référence de l'API Rust
description:
  API publique complète pour la crate surfpool-sdk — Surfnet, builder,
  cheatcodes, builders, erreurs et ré-exportations.
---

La crate `surfpool-sdk` expose une surface restreinte centrée sur la structure
`Surfnet`. Cette page répertorie chaque élément public ; pour une utilisation
narrative, consultez les guides commençant par
[Vue d'ensemble](/docs/tools/surfpool/sdk/overview).

## Exportations de la racine de la crate

| Élément               | Type          | Notes                                                                       |
| --------------------- | ------------- | --------------------------------------------------------------------------- |
| `Surfnet`             | struct        | Instance Surfnet en cours d'exécution — endpoints RPC/WS, payer, cheatcodes |
| `SurfnetBuilder`      | struct        | Builder fluent utilisé par `Surfnet::builder()`                             |
| `Cheatcodes<'_>`      | struct        | Helper de cheatcode emprunté retourné par `Surfnet::cheatcodes()`           |
| `SurfnetError`        | enum          | Toutes les variantes d'erreur retournées par le SDK                         |
| `SurfnetResult<T>`    | alias de type | `Result<T, SurfnetError>`                                                   |
| `Keypair`             | ré-export     | `solana_keypair::Keypair`                                                   |
| `Pubkey`              | ré-export     | `solana_pubkey::Pubkey`                                                     |
| `Signer`              | ré-export     | `solana_signer::Signer`                                                     |
| `RpcClient`           | ré-export     | `solana_rpc_client::rpc_client::RpcClient`                                  |
| `BlockProductionMode` | ré-export     | `surfpool_types::BlockProductionMode`                                       |
| `SimnetCommand`       | ré-export     | `surfpool_types::SimnetCommand`                                             |
| `SimnetEvent`         | ré-export     | `surfpool_types::SimnetEvent`                                               |
| `SvmFeatureConfig`    | ré-export     | `surfpool_types::SvmFeatureConfig`                                          |

## Surfnet

```rust
pub struct Surfnet { /* private */ }
```

### Constructeurs

```rust
impl Surfnet {
    pub async fn start() -> SurfnetResult<Self>;
    pub fn builder() -> SurfnetBuilder;
}
```

### Accesseurs

```rust
impl Surfnet {
    pub fn rpc_url(&self) -> &str;
    pub fn ws_url(&self) -> &str;
    pub fn payer(&self) -> &Keypair;
    pub fn instance_id(&self) -> &str;
    pub fn rpc_client(&self) -> RpcClient;
    pub fn cheatcodes(&self) -> Cheatcodes<'_>;
    pub fn events(&self) -> &crossbeam_channel::Receiver<SimnetEvent>;
    pub fn send_command(&self, cmd: SimnetCommand) -> SurfnetResult<()>;
}
```

### Cycle de vie

```rust
impl Surfnet {
    pub fn stop(&mut self) -> SurfnetResult<()>;
}

impl Drop for Surfnet { /* calls stop() if not already stopped */ }
```

`stop` attend jusqu'à cinq secondes que le runtime libère ses ports. Un échec
lors de `Drop` est silencieux ; un échec lors d'un appel explicite à `stop`
retourne une `SurfnetError::Runtime`.

## SurfnetBuilder

```rust
pub struct SurfnetBuilder { /* private */ }
```

Chaque setter retourne `Self`, permettant ainsi de chaîner les appels. L'appel
terminal est `start().await`.

```rust
impl SurfnetBuilder {
    pub fn offline(self, offline: bool) -> Self;
    pub fn remote_rpc_url(self, url: impl Into<String>) -> Self;
    pub fn block_production_mode(self, mode: BlockProductionMode) -> Self;
    pub fn slot_time_ms(self, ms: u64) -> Self;
    pub fn airdrop_addresses(self, addrs: Vec<Pubkey>) -> Self;
    pub fn airdrop_sol(self, lamports: u64) -> Self;
    pub fn skip_blockhash_check(self, skip: bool) -> Self;
    pub fn payer(self, payer: Keypair) -> Self;
    pub fn enable_feature(self, feature_id: Pubkey) -> Self;
    pub fn disable_feature(self, feature_id: Pubkey) -> Self;
    pub fn feature_config(self, config: SvmFeatureConfig) -> Self;
    pub async fn start(self) -> SurfnetResult<Surfnet>;
}
```

## Cheatcodes

```rust
pub struct Cheatcodes<'a> { /* private */ }
```

### Approvisionnement en SOL

```rust
impl<'a> Cheatcodes<'a> {
    pub fn fund_sol(&self, address: &Pubkey, lamports: u64) -> SurfnetResult<()>;
    pub fn fund_sol_many(&self, accounts: &[(&Pubkey, u64)]) -> SurfnetResult<()>;
}
```

### Approvisionnement en tokens

```rust
impl<'a> Cheatcodes<'a> {
    pub fn fund_token(
        &self,
        owner: &Pubkey,
        mint: &Pubkey,
        amount: u64,
        token_program: Option<&Pubkey>,
    ) -> SurfnetResult<()>;

    pub fn fund_token_many(
        &self,
        owners: &[&Pubkey],
        mint: &Pubkey,
        amount: u64,
        token_program: Option<&Pubkey>,
    ) -> SurfnetResult<()>;

    pub fn set_token_balance(
        &self,
        owner: &Pubkey,
        mint: &Pubkey,
        amount: u64,
        token_program: Option<&Pubkey>,
    ) -> SurfnetResult<()>;

    pub fn get_ata(
        &self,
        owner: &Pubkey,
        mint: &Pubkey,
        token_program: Option<&Pubkey>,
    ) -> Pubkey;
}
```

### Mutation de compte

```rust
impl<'a> Cheatcodes<'a> {
    pub fn set_account(
        &self,
        address: &Pubkey,
        lamports: u64,
        data: &[u8],
        owner: &Pubkey,
    ) -> SurfnetResult<()>;

    pub fn execute<B: CheatcodeBuilder>(&self, builder: B) -> SurfnetResult<()>;
}
```

### Voyage dans le temps

```rust
impl<'a> Cheatcodes<'a> {
    pub fn time_travel_to_slot(&self, slot: u64) -> SurfnetResult<EpochInfo>;
    pub fn time_travel_to_epoch(&self, epoch: u64) -> SurfnetResult<EpochInfo>;
    pub fn time_travel_to_timestamp(&self, timestamp_ms: u64) -> SurfnetResult<EpochInfo>;
}
```

### Déploiement de programme

```rust
impl<'a> Cheatcodes<'a> {
    pub fn deploy_program(&self, program_name: &str) -> SurfnetResult<Pubkey>;
    pub fn deploy(&self, builder: DeployProgram) -> SurfnetResult<Pubkey>;
}
```

## Trait CheatcodeBuilder

```rust
pub trait CheatcodeBuilder {
    const METHOD: &'static str;
    fn build(self) -> serde_json::Value;
}
```

Implémentez ceci pour les cheatcodes JSON-RPC personnalisés qui ne sont pas
encore encapsulés par un builder typé.

## Builders

Tous les builders se trouvent dans `surfpool_sdk::cheatcodes::builders`.

### SetAccount

```rust
pub struct SetAccount { /* private */ }

impl SetAccount {
    pub fn new(address: Pubkey) -> Self;
    pub fn lamports(self, lamports: u64) -> Self;
    pub fn data(self, data: Vec<u8>) -> Self;
    pub fn owner(self, owner: Pubkey) -> Self;
    pub fn rent_epoch(self, epoch: u64) -> Self;
    pub fn executable(self, executable: bool) -> Self;
}
```

### SetTokenAccount

```rust
pub struct SetTokenAccount { /* private */ }

impl SetTokenAccount {
    pub fn new(owner: Pubkey, mint: Pubkey) -> Self;
    pub fn amount(self, amount: u64) -> Self;
    pub fn delegate(self, delegate: Pubkey) -> Self;
    pub fn clear_delegate(self) -> Self;
    pub fn state(self, state: impl Into<String>) -> Self;
    pub fn delegated_amount(self, amount: u64) -> Self;
    pub fn close_authority(self, authority: Pubkey) -> Self;
    pub fn clear_close_authority(self) -> Self;
    pub fn token_program(self, program: Pubkey) -> Self;
}
```

### ResetAccount

```rust
pub struct ResetAccount { /* private */ }

impl ResetAccount {
    pub fn new(address: Pubkey) -> Self;
    pub fn include_owned_accounts(self, include: bool) -> Self;
}
```

### StreamAccount

```rust
pub struct StreamAccount { /* private */ }

impl StreamAccount {
    pub fn new(address: Pubkey) -> Self;
    pub fn include_owned_accounts(self, include: bool) -> Self;
}
```

### DeployProgram

```rust
pub struct DeployProgram { /* private */ }

impl DeployProgram {
    pub fn new(program_id: Pubkey) -> Self;
    pub fn from_keypair_path(path: impl AsRef<Path>) -> SurfnetResult<Self>;
    pub fn so_path(self, path: impl Into<PathBuf>) -> Self;
    pub fn so_bytes(self, bytes: Vec<u8>) -> Self;
    pub fn idl_path(self, path: impl Into<PathBuf>) -> Self;
}
```

## SurfnetError

```rust
#[derive(Debug)]
pub enum SurfnetError {
    PortAllocation(String),
    Startup(String),
    Runtime(String),
    Cheatcode(String),
    Aborted(String),
}
```

| Variante         | Cause                                                                |
| ---------------- | -------------------------------------------------------------------- |
| `PortAllocation` | Impossible de lier un port libre pour RPC ou WebSocket.              |
| `Startup`        | Le runtime n'a pas pu s'initialiser.                                 |
| `Runtime`        | Le thread du runtime a paniqué ou s'est terminé de façon inattendue. |
| `Cheatcode`      | Un appel JSON-RPC de cheatcode a retourné une erreur.                |
| `Aborted`        | Le runtime a été arrêté ou annulé lors du démarrage.                 |

La crate implémente `std::error::Error` et `Display` sur `SurfnetError`, ce qui
la rend compatible avec `?` et `anyhow` sans configuration supplémentaire.
