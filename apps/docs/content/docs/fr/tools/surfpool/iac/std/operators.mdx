---
title: Fonctions opérateurs
description:
  Fonctions opérateurs de la bibliothèque standard pour les opérations
  mathématiques et logiques
---

## and_bool

`and_bool` retourne le ET binaire des arguments gauche et droit.

### Entrées

| Nom   | Type | Description          |
| ----- | ---- | -------------------- |
| `lhs` | bool | Une valeur booléenne |
| `rhs` | bool | Une valeur booléenne |

### Sortie

| Nom     | Type | Description                                          |
| ------- | ---- | ---------------------------------------------------- |
| `value` | bool | Le résultat d'un ET binaire entre les deux arguments |

```hcl
output "my_bool" {
  value = false && true
}
// > my_bool: false
```

---

## or_bool

`or_bool` retourne le OU binaire des arguments gauche et droit.

### Entrées

| Nom   | Type | Description          |
| ----- | ---- | -------------------- |
| `lhs` | bool | Une valeur booléenne |
| `rhs` | bool | Une valeur booléenne |

### Sortie

| Nom     | Type | Description                                          |
| ------- | ---- | ---------------------------------------------------- |
| `value` | bool | Le résultat d'un OU binaire entre les deux arguments |

```hcl
output "my_bool" {
  value = false || true
}
// > my_bool: true
```

---

## div

`div` retourne la division entière de l'argument gauche par l'argument droit, en
arrondissant tout reste vers le bas à l'entier le plus proche.

### Entrées

| Nom   | Type    | Description  |
| ----- | ------- | ------------ |
| `lhs` | integer | Le dividende |
| `rhs` | integer | Le diviseur  |

### Sortie

| Nom     | Type    | Description                                             |
| ------- | ------- | ------------------------------------------------------- |
| `value` | integer | Le résultat de la division du dividende par le diviseur |

```hcl
output "my_int" {
  value = 11 / -3
}
// > my_int: -3
```

---

## eq

`eq` retourne `true` si les arguments de gauche et de droite sont égaux et
`false` s'ils ne le sont pas.

### Entrées

| Nom   | Type | Description  |
| ----- | ---- | ------------ |
| `lhs` | any  | Toute valeur |
| `rhs` | any  | Toute valeur |

### Sortie

| Nom     | Type | Description                                                     |
| ------- | ---- | --------------------------------------------------------------- |
| `value` | bool | Le résultat d'une vérification d'égalité entre les deux entrées |

```hcl
output "is_eq" {
  value = "arg" == "arg"
}
// > is_eq: true
```

---

## gt

`gt` retourne `true` si l'argument de gauche est supérieur à l'argument de
droite.

### Entrées

| Nom   | Type                       | Description           |
| ----- | -------------------------- | --------------------- |
| `lhs` | integer \| float \| string | Une valeur comparable |
| `rhs` | integer \| float \| string | Une valeur comparable |

### Sortie

| Nom     | Type | Description                                                                                 |
| ------- | ---- | ------------------------------------------------------------------------------------------- |
| `value` | bool | Le résultat de la vérification si l'argument de gauche est supérieur à l'argument de droite |

```hcl
output "is_gt" {
  value = 2 > 1
}
// > is_gt: true
```

---

## gte

`gte` retourne `true` si l'argument de gauche est supérieur ou égal à l'argument
de droite.

### Entrées

| Nom   | Type                       | Description           |
| ----- | -------------------------- | --------------------- |
| `lhs` | integer \| float \| string | Une valeur comparable |
| `rhs` | integer \| float \| string | Une valeur comparable |

### Sortie

| Nom     | Type | Description                                                                                         |
| ------- | ---- | --------------------------------------------------------------------------------------------------- |
| `value` | bool | Le résultat de la vérification si l'argument de gauche est supérieur ou égal à l'argument de droite |

```hcl
output "is_gte" {
  value = 2 >= 2
}
// > is_gte: true
```

---

## lt

`lt` retourne `true` si l'argument de gauche est inférieur à l'argument de
droite.

### Entrées

| Nom   | Type                       | Description           |
| ----- | -------------------------- | --------------------- |
| `lhs` | integer \| float \| string | Une valeur comparable |
| `rhs` | integer \| float \| string | Une valeur comparable |

### Sortie

| Nom     | Type | Description                                                                                 |
| ------- | ---- | ------------------------------------------------------------------------------------------- |
| `value` | bool | Le résultat de la vérification si l'argument de gauche est inférieur à l'argument de droite |

```hcl
output "is_lt" {
  value = 2 < 1
}
// > is_lt: false
```

---

## lte

`lte` retourne `true` si l'argument de gauche est inférieur ou égal à l'argument
de droite.

### Entrées

| Nom   | Type                       | Description           |
| ----- | -------------------------- | --------------------- |
| `lhs` | integer \| float \| string | Une valeur comparable |
| `rhs` | integer \| float \| string | Une valeur comparable |

### Sortie

| Nom     | Type | Description                                                                                         |
| ------- | ---- | --------------------------------------------------------------------------------------------------- |
| `value` | bool | Le résultat de la vérification si l'argument de gauche est inférieur ou égal à l'argument de droite |

```hcl
output "is_lte" {
  value = 2 <= 2
}
// > is_lte: true
```

---

## neq

`neq` retourne `true` si les arguments de gauche et de droite ne sont pas égaux
et `false` dans le cas contraire.

### Entrées

| Nom   | Type | Description                |
| ----- | ---- | -------------------------- |
| `lhs` | any  | Une valeur à comparer      |
| `rhs` | any  | Une valeur à comparer avec |

### Sortie

| Nom     | Type | Description                                                          |
| ------- | ---- | -------------------------------------------------------------------- |
| `value` | bool | Le résultat d'une vérification d'égalité niée entre les deux entrées |

```hcl
output "is_neq" {
  value = "arg" != "arg"
}
// > is_neq: false
```

---

## minus

`minus` retourne le résultat de la soustraction de l'argument `integer` du côté
droit de l'argument `integer` du côté gauche.

### Entrées

| Nom   | Type    | Description   |
| ----- | ------- | ------------- |
| `lhs` | integer | Le diminuende |
| `rhs` | integer | Le diminuteur |

### Sortie

| Nom     | Type    | Description                                |
| ------- | ------- | ------------------------------------------ |
| `value` | integer | Le résultat de l'opération de soustraction |

```hcl
output "my_integer" {
  value = 10 - 6
}
// > my_integer: 4
```

---

## modulo

`modulo` retourne le reste de la division de l'argument `integer` du côté gauche
par l'argument `integer` du côté droit.

### Entrées

| Nom   | Type    | Description  |
| ----- | ------- | ------------ |
| `lhs` | integer | Le dividende |
| `rhs` | integer | Le diviseur  |

### Sortie

| Nom     | Type    | Description                         |
| ------- | ------- | ----------------------------------- |
| `value` | integer | Le reste de l'opération de division |

```hcl
output "my_mod" {
    value = 10 % 3
}
// > my_mod: 1
```

---

## multiply

`multiply` retourne le produit de l'argument `integer` du côté gauche et de
l'argument `integer` du côté droit.

### Entrées

| Nom   | Type    | Description         |
| ----- | ------- | ------------------- |
| `lhs` | integer | Le premier opérande |
| `rhs` | integer | Le second opérande  |

### Sortie

| Nom     | Type    | Description                                  |
| ------- | ------- | -------------------------------------------- |
| `value` | integer | Le résultat de l'opération de multiplication |

```hcl
output "my_product" {
    value = 10 * 5
}
// > my_product: 50
```

---

## add

`add` retourne la somme de l'argument `integer` du côté gauche et de l'argument
`integer` du côté droit.

### Entrées

| Nom   | Type    | Description         |
| ----- | ------- | ------------------- |
| `lhs` | integer | Le premier opérande |
| `rhs` | integer | Le second opérande  |

### Sortie

| Nom     | Type    | Description                           |
| ------- | ------- | ------------------------------------- |
| `value` | integer | Le résultat de l'opération d'addition |

```hcl
output "my_sum" {
    value = 10 + 5
}
// > my_sum: 15
```

---

## neg_integer

Retourne la négation de l'entier donné.

### Entrées

| Nom     | Type    | Description        |
| ------- | ------- | ------------------ |
| `value` | integer | Une valeur entière |

### Sortie

| Nom     | Type    | Description                     |
| ------- | ------- | ------------------------------- |
| `value` | integer | La valeur entière avec négation |

---

## not_bool

Retourne la négation logique de la valeur booléenne donnée.

### Entrées

| Nom     | Type | Description          |
| ------- | ---- | -------------------- |
| `value` | bool | Une valeur booléenne |

### Sortie

| Nom     | Type | Description                                         |
| ------- | ---- | --------------------------------------------------- |
| `value` | bool | La négation logique de la valeur booléenne d'entrée |
