---
title: Modifications du SDK (Bêta)
description:
  Modifications du SDK TypeScript dans Kora v2.2.0-beta — nouveau constructeur,
  plugin Kit et méthodes de bundle.
---

Le SDK `@solana/kora` a été mis à jour avec de nouvelles API pour la version
v2.2.0-beta. Installez le SDK bêta :

```bash
pnpm add @solana/kora@beta
```

## Méthodes de bundle

Le SDK prend désormais en charge toutes les opérations de bundle :

```typescript
// Estimate bundle fees
const estimate = await client.estimateBundleFee({
  transactions: [base64Tx1, base64Tx2],
  fee_token: "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"
});

// Sign a bundle without submitting
const { signed_transactions } = await client.signBundle({
  transactions: [base64Tx1, base64Tx2, base64Tx3],
  signer_key: koraSignerAddress
});

// Sign and submit a bundle to Jito
const { bundle_uuid } = await client.signAndSendBundle({
  transactions: [base64Tx1, base64Tx2, base64Tx3],
  signer_key: koraSignerAddress
});
```

## getVersion

```typescript
const { version } = await client.getVersion();
console.log("Kora server version:", version);
```
