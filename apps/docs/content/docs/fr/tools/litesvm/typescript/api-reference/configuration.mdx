---
title: Configuration
description: Méthodes de construction pour configurer LiteSVM en TypeScript
---

# Configuration

LiteSVM utilise un patron de construction pour la configuration. Toutes les
méthodes retournent `LiteSVM` pour le chaînage.

## Configuration Typique de Test

```typescript
import { createClient, generateKeyPairSigner } from "@solana/kit";
import { litesvm } from "@solana/kit-plugin-litesvm";
import { signer } from "@solana/kit-plugin-signer";

const mySigner = await generateKeyPairSigner();
const client = createClient().use(signer(mySigner)).use(litesvm());

client.svm
  .withSigverify(false) // Skip signature verification (faster)
  .withBlockhashCheck(false) // Skip blockhash validation (simpler)
  .withSysvars() // Enable Clock, Rent, etc.
  .withBuiltins() // Enable ed25519, secp256k1
  .withTransactionHistory(100n); // Store last 100 transactions
```

## Signature & Blockhash

### withSigverify

```typescript
withSigverify(sigverify: boolean): LiteSVM
```

Activer ou désactiver la vérification des signatures.

```typescript
// Disable for faster tests (default is enabled)
client.svm.withSigverify(false);

// Enable for production-like testing
client.svm.withSigverify(true);
```

<Callout type="info">
  La désactivation de la vérification des signatures rend les tests plus rapides
  mais moins réalistes.
</Callout>

### withBlockhashCheck

```typescript
withBlockhashCheck(check: boolean): LiteSVM
```

Activer ou désactiver la vérification de l'expiration des blockhash.

```typescript
// Disable for simpler tests
client.svm.withBlockhashCheck(false);
```

## Programmes & Sysvars

### withSysvars

```typescript
withSysvars(): LiteSVM
```

Activer les comptes sysvar (Clock, rent, EpochSchedule, etc.).

```typescript
client.svm.withSysvars();

// Now you can access sysvars
const clock = client.svm.getClock();
const rent = client.svm.getRent();
```

### withBuiltins

```typescript
withBuiltins(): LiteSVM
```

Activer les programmes intégrés (vérification de signatures ed25519, secp256k1).

```typescript
client.svm.withBuiltins();
```

### withPrecompiles

```typescript
withPrecompiles(): LiteSVM
```

Activer les programmes précompilés.

```typescript
client.svm.withPrecompiles();
```

### withDefaultPrograms

```typescript
withDefaultPrograms(): LiteSVM
```

Ajouter les programmes par défaut (System Program, BPF Loader, etc.).

```typescript
client.svm.withDefaultPrograms();
```

## Limites des ressources

### withLamports

```typescript
withLamports(lamports: bigint): LiteSVM
```

Définir les lamport par défaut pour les comptes.

```typescript
client.svm.withLamports(1_000_000_000n); // 1 SOL
```

### withLogBytesLimit

```typescript
withLogBytesLimit(limit?: bigint): LiteSVM
```

Définir la limite d'octets pour la sortie des journaux.

```typescript
client.svm.withLogBytesLimit(10000n);
```

### withTransactionHistory

```typescript
withTransactionHistory(capacity: bigint): LiteSVM
```

Activer le stockage de l'historique des transactions.

```typescript
// Store last 100 transactions
client.svm.withTransactionHistory(100n);
```

## Ensembles de fonctionnalités

### withFeatureSet

```typescript
withFeatureSet(featureSet: FeatureSet): LiteSVM
```

Configurer l'ensemble de fonctionnalités pour tester des versions spécifiques de
Solana.
