---
title: Tipi di account
description:
  Tipi di account Solana — program account per codice eseguibile, data account
  per lo stato del programma, system account di proprietà del System Program e
  sysvar per lo stato a livello di cluster a indirizzi predefiniti.
url: /docs/core/accounts/account-types
type: conceptual
prerequisites:
  - /docs/core/accounts
  - /docs/core/accounts/account-structure
related:
  - /docs/core/programs
  - /docs/core/accounts/modification-rules
  - /docs/core/programs/builtin-programs
---

<Callout type="info" title="Riepilogo">
  I program account contengono codice eseguibile sBPF. I data account
  memorizzano lo stato, di proprietà dei programmi. I system account sono di
  proprietà del System Program. I sysvar forniscono lo stato a livello di
  cluster accessibile a indirizzi predefiniti.
</Callout>

Il campo [`executable`](/docs/core/accounts/account-structure) determina la
categoria di un account:

- **[Program account](#program-accounts)**: `executable` = `true`. Contiene
  codice eseguibile.
- **[Data account](#data-accounts)**: `executable` = `false`. Memorizza lo stato
  o i dati utente.

Questa separazione del codice dallo stato mutabile significa che un programma
viene distribuito una volta e può gestire un numero qualsiasi di data account.

## Program account

Un program account memorizza codice eseguibile. Ogni program account è di
proprietà di un
[loader program](/docs/core/programs/builtin-programs#loader-programs). Quando
un [programma](/docs/core/programs) viene distribuito, il runtime crea un
program account per contenere il suo bytecode.

![Diagram of a program account, its 4 components and its loader program.](/assets/docs/core/accounts/program-account-simple.svg)

### Program data account

I programmi distribuiti utilizzando loader-v3 (vedi
[Loader program](/docs/core/programs/builtin-programs#loader-programs)) non
memorizzano il bytecode eseguibile nel proprio campo `data`. Invece, il loro
`data` punta a un **program data account** separato che contiene il codice del
programma. (Vedi il diagramma qui sotto.)

![A program account with data. The data points to a separate program data account](/assets/docs/core/accounts/program-account-expanded.svg)

<Callout>
  Durante la distribuzione o gli aggiornamenti del programma, i buffer account
  vengono utilizzati per preparare temporaneamente il caricamento.
</Callout>

L'esempio seguente recupera il Token Program account. Il campo `executable` è
`true`, confermando che si tratta di un program account.
