---
title: Kora
description: Infrastruttura di firma Solana
---

**Kora è la tua infrastruttura di firma Solana.** Abilita transazioni senza gas
dove gli utenti pagano le commissioni in qualsiasi token—USDC, BONK o il token
nativo della tua app—oppure gestisci qualsiasi firma di transazione che richiede
un firmatario affidabile.

### Perché Kora?

- **UX migliore**: Gli utenti non hanno mai bisogno di SOL
- **Controllo dei ricavi**: Raccogli commissioni in USDC, nel tuo token o in
  qualsiasi altra cosa
- **Pronto per la produzione**: Validazione sicura, limitazione della velocità,
  monitoraggio integrato
- **Integrazione semplice**: API JSON-RPC + SDK TypeScript
- **Distribuzione flessibile**: Railway, Docker o qualsiasi piattaforma cloud

### Architettura

- **Linguaggio**: Rust con SDK TypeScript
- **Protocollo**: JSON-RPC 2.0
- **Firmatari**: Chiave privata Solana, Turnkey, Privy
- **Autenticazione**: Chiave API, HMAC o nessuna
- **Distribuzione**: Opzioni di distribuzione flessibili (Docker, Railway, ecc.)

### Funzionalità

- Regole di validazione e allowlist configurabili
- Supporto completo Token-2022 con filtraggio delle estensioni
- Caching Redis per prestazioni migliorate
- Limitazione della velocità e protezione della spesa
- Gestione sicura delle chiavi (Turnkey, Privy, Vault)
- Autenticazione HMAC e chiave API
- Metriche Prometheus e monitoraggio
- Politiche avanzate di protezione del pagatore delle commissioni

## Avvio rapido

Installa Kora:

```bash
cargo install kora-cli
```

Utilizzo di base:

```bash
kora rpc [OPTIONS] # --help for full list of options
```

**[→ Guida all'avvio rapido](/docs/tools/kora/getting-started/quick-start)** -
Metti in funzione Kora localmente in pochi minuti

**[→ Guida per operatori di nodi](/docs/tools/kora/operators)** - Esegui un
paymaster

## SDK TypeScript

Kora fornisce un'interfaccia JSON-RPC semplice con tre opzioni client:

```typescript
// Option 1: Standalone client
import { KoraClient } from "@solana/kora";
const kora = new KoraClient({ rpcUrl: "http://localhost:8080" });
const signed = await kora.signTransaction({ transaction });

// Option 2: Kit client (recommended) — handles planning, fees, signing, and sending
import { createKitKoraClient } from "@solana/kora";
import { address } from "@solana/kit";
const client = await createKitKoraClient({
  endpoint: "http://localhost:8080",
  rpcUrl: "https://api.mainnet-beta.solana.com",
  feeToken: address("EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"),
  feePayerWallet: userSigner
});
```

**[→ Riferimento API](/docs/tools/kora/json-rpc-api)** - Metodi JSON-RPC e SDK
TypeScript

## Sviluppo locale

### Prerequisiti

- Rust 1.86+ oppure
- Solana CLI 2.2+
- Node.js 20+ e pnpm (per l'SDK)

### Installazione

```bash
git clone https://github.com/solana-foundation/kora.git
cd kora
git checkout v2.0.5
just install
```

### Build

```bash
just build
```

### Esecuzione del Server

Utilizzo base:

```bash
kora rpc [OPTIONS]
```

Oppure per l'esecuzione con una configurazione di test, eseguire:

```bash
just run
```

### Test Locali

Ed eseguire tutti i test:

```bash
just test-all
```

## Community e Supporto

- **Domande?** Chiedi su
  [Solana Stack Exchange](https://solana.stackexchange.com/) (utilizza il tag
  `kora`)
- **Problemi?** Segnala su
  [GitHub Issues](https://github.com/solana-foundation/kora/issues)

## Altre Risorse

- [Kora CLI Crates.io](https://crates.io/crates/kora-cli) - Crate Rust per
  eseguire un nodo Kora
- [@solana/kora NPM Package](https://www.npmjs.com/package/@solana/kora) - SDK
  TypeScript per interagire con un nodo Kora

## Sorgente

- [Repository GitHub](https://github.com/solana-foundation/kora)

Sviluppato e mantenuto dalla [Solana Foundation](https://solana.org).

Licenziato sotto MIT. Vedi
[LICENSE](https://github.com/solana-foundation/kora/blob/main/LICENSE.md) per i
dettagli.
