---
title: RemoveOperator
description: Revoca un operatore chiudendo il relativo PDA Operator
---

Revoca un operatore chiudendo il relativo PDA `Operator`. Il wallet revocato non
può più chiamare `ReleaseFunds` o `ResetSmtRoot`.

## Account

| Account                          | Scrivibile | Firmatario | Descrizione                               |
| -------------------------------- | -------- | ------ | ----------------------------------------- |
| `payer`                          | ✓        | ✓      | Pagante delle commissioni                                 |
| `admin`                          |          | ✓      | Admin dell'istanza                            |
| `instance`                       |          |        | PDA dell'istanza                              |
| `operator`                       |          |        | Wallet dell'operatore da revocare                 |
| `operator_pda`                   | ✓        |        | PDA dell'operatore (chiuso da questa istruzione) |
| `system_program`                 |          |        | System Program                            |
| `event_authority`                |          |        | PDA dell'autorità evento                       |
| `private_channel_escrow_program` |          |        | Programma Escrow                            |

Questa istruzione non prevede parametri.

## Note importanti

- Chiude il `operator_pda`; il wallet revocato non può più chiamare
  `ReleaseFunds` o `ResetSmtRoot`
- Qualsiasi transazione dell'operatore in corso fallirà con `InvalidOperatorPda` dopo
  questa chiamata
- Emette un evento tramite CPI `EmitEvent`
