---
title: インストール
description:
  環境をセットアップし、Private Channels TypeScript クライアントを生成する
---

## 前提条件

- Rust 1.91+
- Solana CLI 3.1.13 (Agave):
  `sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.13/install)"`
- Node.js 24.7.0+
- pnpm 10.15.1+: `npm install -g pnpm@10.15.1`

## リポジトリのクローン

```bash
git clone https://github.com/solana-foundation/solana-private-channels.git
cd solana-private-channels
```

## TypeScript クライアントの生成

TypeScript クライアントは npm に**公開されていません**。Codama を使用して IDL から生成する必要があります。

```bash
# Escrow program client
cd private-channel-escrow-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..

# Withdraw program client
cd private-channel-withdraw-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..
```

生成されたクライアントの出力先：

- `private-channel-escrow-program/clients/typescript/src/generated/`
- `private-channel-withdraw-program/clients/typescript/src/generated/`

アプリケーションではこれらのローカルパスからインポートしてください。npm に `@private-channels/client` パッケージは存在しません。このセクションのコードサンプルでは、クローンしたリポジトリと同じ階層にコードが配置されていることを前提とした相対パス（`../private-channel-escrow-program/...`）を使用しています。実際のプロジェクトのインポート元に合わせてパスを調整してください。

## Devnet への接続

```bash
solana config set --url devnet
solana airdrop 2
solana balance
```

- エスクロープログラム ID: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- 引き出しプログラム ID: `J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi`

## 次のステップ

**[トークンの預け入れ](/docs/tools/private-channels/quickstart/deposit)** - 最初の預け入れを行う
