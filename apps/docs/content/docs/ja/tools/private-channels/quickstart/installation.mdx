---
title: インストール
description:
  環境をセットアップし、Private Channels TypeScriptクライアントを生成する
---

## 前提条件

- Rust 1.91+
- Solana CLI 3.1.13 (Agave):
  `sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.13/install)"`
- Node.js 24.7.0+
- pnpm 10.15.1+: `npm install -g pnpm@10.15.1`

## リポジトリのクローン

```bash
git clone https://github.com/solana-foundation/solana-private-channels.git
cd solana-private-channels
```

## TypeScriptクライアントの生成

TypeScriptクライアントはnpmに**公開されていません**。Codamaを使用してIDLから生成する必要があります。

```bash
# Escrow program client
cd private-channel-escrow-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..

# Withdraw program client
cd private-channel-withdraw-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..
```

生成されたクライアントの出力先：

- `private-channel-escrow-program/clients/typescript/src/generated/`
- `private-channel-withdraw-program/clients/typescript/src/generated/`

アプリケーションではこれらのローカルパスからインポートしてください。npmに
`@private-channels/client`パッケージは存在しません。このセクションのコードサンプルでは、クローンしたリポジトリと同じ階層にコードが配置されていることを前提とした相対パス（`../private-channel-escrow-program/...`）を使用しています。実際のプロジェクトでのインポート元に合わせてパスを調整してください。

## Devnetへの接続

```bash
solana config set --url devnet
solana airdrop 2
solana balance
```

- エスクロープログラムID: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- 引き出しプログラムID: `J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi`

## 次のステップ

**[トークンのデポジット](/docs/tools/private-channels/quickstart/deposit)** - 最初のデポジットを行う
