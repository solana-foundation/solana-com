---
title: 安装
description:
  配置您的开发环境并生成 Private Channels TypeScript 客户端
---

## 前提条件

- Rust 1.91+
- Solana CLI 3.1.13 (Agave)：
  `sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.13/install)"`
- Node.js 24.7.0+
- pnpm 10.15.1+：`npm install -g pnpm@10.15.1`

## 克隆代码仓库

```bash
git clone https://github.com/solana-foundation/solana-private-channels.git
cd solana-private-channels
```

## 生成 TypeScript 客户端

TypeScript 客户端**未**发布至 npm，必须通过 Codama 从 IDL 生成。

```bash
# Escrow program client
cd private-channel-escrow-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..

# Withdraw program client
cd private-channel-withdraw-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..
```

生成的客户端位于：

- `private-channel-escrow-program/clients/typescript/src/generated/`
- `private-channel-withdraw-program/clients/typescript/src/generated/`

在您的应用中通过本地路径导入这些客户端；npm 上不存在 `@private-channels/client` 包。本节的代码示例使用相对路径（`../private-channel-escrow-program/...`），假设您的代码与克隆的仓库位于同一目录；请根据实际导入路径自行调整。

## 连接到 Devnet

```bash
solana config set --url devnet
solana airdrop 2
solana balance
```

- Escrow 程序 ID：`9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- Withdraw 程序 ID：`J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi`

## 下一步

**[存入代币](/docs/tools/private-channels/quickstart/deposit)** — 完成您的第一笔存款
