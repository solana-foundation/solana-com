---
title: SetNewAdmin
description: 将管理员权限转移至新钱包
---

将管理员权限转移至新钱包。

## 账户

| 账户                          | 可写 | 签名者 | 描述                        |
| -------------------------------- | -------- | ------ | ---------------------------------- |
| `payer`                          | ✓        | ✓      | 手续费支付方                          |
| `current_admin`                  |          | ✓      | 当前管理员（必须签名）          |
| `instance`                       | ✓        |        | 实例 PDA（管理员字段已更新） |
| `new_admin`                      |          | ✓      | 接收管理员权限的钱包     |
| `event_authority`                |          |        | 事件授权 PDA                |
| `private_channel_escrow_program` |          |        | 托管程序                     |

此指令不接受任何参数。

## 重要说明

- **不可一步撤销**：当前管理员在新管理员再次调用 `SetNewAdmin` 之前，无法收回该角色
- 调用前请务必核实目标地址，操作无法撤销
- 通过 `EmitEvent` CPI 触发事件
