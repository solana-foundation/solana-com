---
title: React
description:
  通过 @solana/react 和钱包插件钩子，在整个 React 应用中共享 Kit 客户端。
---

`@solana/react`
通过一个提供者将 Kit 客户端发布到组件树中，钩子随后从中读取 RPC 数据、订阅和钱包状态。浏览器钱包钩子位于
`@solana/kit-plugin-wallet/react` 中，并读取同一个提供者——无需单独的钱包提供者。

## 安装

```terminal
$ npm install @solana/kit @solana/kit-plugin-rpc @solana/kit-plugin-wallet @solana/react
```

## 构建客户端

在模块作用域内、React 树之外一次性构建客户端。在浏览器中，`walletSigner`
将已连接的钱包同时设为手续费支付方和身份标识。

```tsx title="client.ts"
import { createClient } from "@solana/kit";
import { solanaRpc } from "@solana/kit-plugin-rpc";
import { walletSigner } from "@solana/kit-plugin-wallet";

export const client = createClient()
  .use(walletSigner({ chain: "solana:devnet" }))
  .use(solanaRpc({ rpcUrl: "https://api.devnet.solana.com" }));

export type AppClient = Awaited<typeof client>;
```

`walletSigner` 添加了 `client.wallet`（Wallet Standard 发现与连接），`solanaRpc`
添加了 `client.rpc` 以及 `client.sendTransaction` 辅助函数。将客户端类型导出为
`Awaited<>` 包装的形式，以便在日后添加异步解析插件时保持类型正确，并将该
`AppClient` 传递给每个 `useClient` 调用。

## 包裹组件树（仅需一次）

`ClientProvider` 负责发布客户端，以下所有钩子均从中读取数据。

```tsx title="providers.tsx"
"use client";

import { ClientProvider } from "@solana/react";
import { client } from "./client";

export function Providers({ children }: { children: React.ReactNode }) {
  return <ClientProvider client={client}>{children}</ClientProvider>;
}
```

## 钱包钩子

钱包钩子读取客户端的 `wallet` 能力，因此它们只能在安装了钱包插件的客户端所对应的
`ClientProvider` 内部使用。

- `useWallets()` — 为当前客户端链发现的 Wallet Standard 钱包。
- `useWalletStatus()` —
  `"pending" | "disconnected" | "connecting" | "connected" | "disconnecting" | "reconnecting"`。
- `useConnectedWallet()` — `{ account, signer, wallet }` 或
  `null`。对于只读钱包，值为 `signer`，即 `null`。
- `useConnect()` / `useDisconnect()` — 返回一个 `ActionResult`，其
  `dispatch`（以及
  `dispatchAsync`）用于执行操作，`isRunning`、`status`、`data`、`error` 和
  `reset` 用于追踪操作结果。

```tsx
"use client";

import {
  useConnect,
  useConnectedWallet,
  useDisconnect,
  useWallets,
  useWalletStatus
} from "@solana/kit-plugin-wallet/react";

function WalletPanel() {
  const status = useWalletStatus();
  const wallets = useWallets();
  const connected = useConnectedWallet();
  const connect = useConnect();
  const disconnect = useDisconnect();

  if (status === "pending") return null; // wait out auto-reconnect

  if (connected) {
    return (
      <div>
        <p>{connected.account.address}</p>
        <button onClick={() => disconnect.dispatch()}>Disconnect</button>
      </div>
    );
  }

  return (
    <div>
      {wallets.map((wallet) => (
        <button
          key={wallet.name}
          disabled={connect.isRunning}
          onClick={() => connect.dispatch(wallet)}
        >
          Connect {wallet.name}
        </button>
      ))}
    </div>
  );
}
```

## 发送交易

`useClient` 返回你已发布的同一客户端。使用生成的程序客户端构建指令，并通过
`client.sendTransaction` 发送。

```tsx
"use client";

import { address, lamports } from "@solana/kit";
import { getTransferSolInstruction } from "@solana-program/system";
import { useClient } from "@solana/react";
import { useConnectedWallet } from "@solana/kit-plugin-wallet/react";
import type { AppClient } from "./client";

function SendSol({ destination }: { destination: string }) {
  const client = useClient<AppClient>();
  const connected = useConnectedWallet();

  async function handleSend() {
    if (!connected?.signer) return;
    const transfer = getTransferSolInstruction({
      source: connected.signer,
      destination: address(destination),
      amount: lamports(10_000_000n) // 0.01 SOL
    });
    const result = await client.sendTransaction([transfer]);
    console.log("sent", result.context.signature);
  }

  return (
    <button disabled={!connected?.signer} onClick={handleSend}>
      Send 0.01 SOL
    </button>
  );
}
```

将导出的 `AppClient` 类型传递给
`useClient`，可使钩子获得完整的插件扩展客户端，从而对 `client.rpc` 和
`client.sendTransaction` 进行类型标注。

## 读取数据

`@solana/react` 提供了通过客户端读取数据的钩子：

- `useRequest(source)` — 单次 RPC 读取；返回
  `{ data, error, status, refresh }`。
- `useSubscription(source)` — 实时订阅；返回
  `{ data, error, reconnect, status }`。
- `useTrackedData(spec)` — 由 RPC 初始化并通过订阅保持最新的值。

```tsx
"use client";

import { address } from "@solana/kit";
import { useClient, useRequest } from "@solana/react";
import { useMemo } from "react";
import type { AppClient } from "./client";

function Balance({ owner }: { owner: string }) {
  const client = useClient<AppClient>();
  const source = useMemo(
    () => client.rpc.getBalance(address(owner)),
    [client, owner]
  );
  const { data, status, refresh } = useRequest(source);

  if (status === "fetching") return <p>Loading…</p>;
  if (status === "error") return <p role="alert">RPC error</p>;

  return (
    <div>
      <p>Lamports: {data?.value.toString()}</p>
      <button onClick={() => refresh()}>Refresh</button>
    </div>
  );
}
```

对数据源进行记忆化（使用 `useMemo`，或对函数使用
`useCallback`），使钩子仅在输入变化时重新获取数据，而非在每次渲染时触发。

对于缓存和重新验证，`@solana/react/swr` 和 `@solana/react/query` 子路径为
[SWR](https://swr.vercel.app) 和 [TanStack Query](https://tanstack.com/query)
封装了相同的钩子。

## Solana 开发者常用模式

- **单一 Provider**：`ClientProvider`
  是你唯一需要的 Provider；钱包钩子通过客户端的 `wallet` 能力读取它。
- **服务端组件感知**：仅对调用钩子的叶子组件标记
  `"use client"`；服务端读取可使用不带钱包的普通 Kit RPC 客户端。
- **测试**：通过 `ClientProvider` 注入模拟客户端，以模拟钱包和 RPC 响应。

<Callout type="warn" title="两个钩子家族，名称相同">
  `useSignIn` 和 `useSignMessage` 同时存在于 `@solana/react`（接受
  `UiWalletAccount` 参数）和
  `@solana/kit-plugin-wallet/react`（基于客户端，无账户参数，返回
  `ActionResult`）中。请使用上述客户端模式中的 `@solana/kit-plugin-wallet/react`
  版本；旧版 `@solana/react` 钱包钩子正逐步被取代。
</Callout>

请将本指南与 [Kit 客户端概览](/docs/frontend/client)
配合阅读，以了解每个钩子所读取的客户端。
