---
title: Keychain
description:
  Einheitliche Solana-Signierung über mehrere Schlüsselverwaltungs-Backends
---

**Solana Keychain bietet eine einheitliche Schnittstelle zur Signierung von
Solana-Transaktionen über mehrere Schlüsselverwaltungs-Backends hinweg.** Nutzen
Sie es, um Signierung auf Unternehmensniveau in Ihre Backend-Dienste zu
integrieren.

### Warum Keychain?

- **Einheitliche Schnittstelle**: Ein `SolanaSigner`-Trait funktioniert über
  alle Backends hinweg
- **Backend-Wechsel**: Ändern Sie Schlüsselverwaltungsanbieter ohne
  Code-Umschreibung
- **Null-Kosten-Abstraktion**: Feature-Flags binden nur das ein, was Sie
  benötigen

### Architektur

- **Sprachen**: Rust + TypeScript
- **Trait**: Einheitliche `SolanaSigner`-Schnittstelle
- **Backends**: Memory, Vault, AWS KMS, GCP KMS, Privy, Turnkey, Fireblocks,
  CDP, Crossmint, Dfns, Openfort, Para
- **Kompatibilität**: `@solana/kit`- und `@solana/signers`-kompatibel
  (TypeScript) | `solana-sdk`- und `solana-sdk-v3`-kompatibel (Rust)

### Unterstützte Backends

| Backend         | Anwendungsfall                | Rust | TypeScript |
| --------------- | ----------------------------- | ---- | ---------- |
| Memory          | Entwicklung, Tests            | ✓    | ✓          |
| HashiCorp Vault | Selbst gehostetes HSM         | ✓    | ✓          |
| AWS KMS         | Cloud-nativ (AWS)             | ✓    | ✓          |
| GCP KMS         | Cloud-nativ (GCP)             | ✓    | ✓          |
| Privy           | Eingebettete Wallets          | ✓    | ✓          |
| Turnkey         | Nicht verwahrend              | ✓    | ✓          |
| Fireblocks      | Institutionelles MPC          | ✓    | ✓          |
| CDP             | Coinbase-Entwicklerplattform  | ✓    | ✓          |
| Crossmint       | Crossmint-verwaltete Wallets  | ✓    | ✓          |
| Dfns            | Dfns-Wallet-Infrastruktur     | ✓    | ✓          |
| Openfort        | Openfort-eingebettete Wallets | ✓    | ✓          |
| Para            | Para-MPC-Wallets              | ✓    | ✓          |

## Schnellstart

### Rust

Installieren Sie die Rust-Crate:

```bash
cargo add solana-keychain
```

Grundlegende Verwendung:

```rust
use solana_keychain::{Signer, SolanaSigner};

// Create a signer from any backend
let signer = Signer::from_memory("base58_private_key")?;

// All signers share the same interface
let pubkey = signer.pubkey();
let signature = signer.sign_transaction(&mut tx).await?;
```

- [Rust-Leitfaden](/docs/tools/keychain/getting-started/rust) - Vollständige
  Installation und Backend-Konfiguration
- [Crates.io](https://crates.io/crates/solana-keychain/1.2.0) - Rust-Crate

### TypeScript

Installieren Sie das TypeScript-Paket:

```bash
pnpm add @solana/keychain
```

Grundlegende Verwendung:

```typescript
import { createKeychainSigner } from "@solana/keychain";
import { signTransactionWithSigners } from "@solana/signers";

// Create any signer via the unified factory
const signer = await createKeychainSigner({
  backend: "vault",
  vaultAddr: "https://vault.example.com:8200",
  vaultToken: "hvs.xxxxx",
  keyName: "my-solana-key",
  publicKey: "base58_public_key"
});

// Sign an already-compiled transaction
const signedTx = await signTransactionWithSigners(
  [signer],
  compiledTransaction
);
```

- [TypeScript-Leitfaden](/docs/tools/keychain/getting-started/typescript) -
  `@solana/keychain` Pakete
- [npm-Paket](https://www.npmjs.com/package/@solana/keychain/v/1.2.0) -
  npm-Paket

## Weitere Ressourcen

- [Hinzufügen benutzerdefinierter Signer](https://github.com/solana-foundation/solana-keychain/blob/main/docs/ADDING_SIGNERS.md) -
  Neue Backends integrieren
- [GitHub-Releases](https://github.com/solana-foundation/solana-keychain/releases) -
  Release-Verlauf

## Quelle

[GitHub-Repository](https://github.com/solana-foundation/solana-keychain)

Entwickelt und gewartet von der [Solana Foundation](https://solana.org).

Lizenziert unter MIT. Siehe
[LICENSE](https://github.com/solana-foundation/solana-keychain/blob/main/LICENSE)
für Details.
