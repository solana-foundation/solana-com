---
title: Agentenbasierte Zahlungen
description:
  Ermöglichen Sie KI-Agenten, autonom für Dienste, APIs und Ressourcen mit dem
  x402-Protokoll zu bezahlen.
---

KI-Agenten werden in ihren Arbeitsabläufen zunehmend autonom: Sie schreiben
Code, rufen Daten ab, stellen Ressourcen bereit und vieles mehr. Um vollständig
unabhängig zu arbeiten, benötigen sie eine Möglichkeit, programmatisch, in
Echtzeit und zu minimalen Kosten "Mikrozahlungen" für Dienste zu leisten.
Traditionelle Zahlungsschienen funktionieren nicht für Mikrozahlungen. Solanas
Sub-Cent-Gebühren und Sub-Sekunden-Finalität machen dies technisch und
wirtschaftlich machbar.

## Warum ist das wichtig?

Stellen Sie sich zum Beispiel vor, ein Entwickler verwendet ein LLM, um eine
Anwendung zu erstellen, und benötigt Daten von einer kostenpflichtigen API. Um
wichtige Daten abzurufen, müsste ein Entwickler seinen Arbeitsablauf
unterbrechen, APIs recherchieren, einen Anbieter auswählen, ein Konto erstellen,
eine Zahlungsmethode hinzufügen, einen API-Schlüssel generieren, den Schlüssel
in seine Umgebung kopieren und dann seinen Arbeitsablauf fortsetzen. Diese
Reibung unterbricht den Flow-Zustand. Für KI-gestützte Arbeitsabläufe ist es ein
Hindernis.

Stattdessen kann der Agent mit agentenbasierten Zahlungen die Anfrage stellen,
dafür bezahlen und seinen Arbeitsablauf fortsetzen. Keine Konten. Keine
API-Schlüssel. Kein manuelles Eingreifen.

### Bezahlen Sie APIs mit pay.sh

[pay.sh](https://pay.sh) ist eine Zahlungsschicht für HTTP-Agenten und
Befehlszeilentools. Lesen Sie die [pay.sh-Dokumentation](https://pay.sh/docs),
um `pay` zu installieren. Die `pay`-CLI umschließt Tools wie `curl`, `codex` und
`claude`, sodass sie kostenpflichtige APIs ohne Anbieterkonten oder
API-Schlüssel aufrufen können. Wenn eine API eine MPP- oder
x402-`402 Payment Required`-Anforderung zurückgibt, fordert `pay` Ihr lokales
Wallet auf, das Signieren zu genehmigen.

Sie können Pay installieren mit:

```bash
brew install pay
```

oder

```bash
npm install -g @solana/pay
```

Überprüfen Sie die Installation mit:

```bash
pay --version
```

Die Toolchain richtet Ihre Adresse automatisch ein. Führen Sie Folgendes aus, um
Ihr Wallet einzurichten und aufzuladen:

```bash
pay setup
```

## Das x402-Protokoll

Agentische Zahlungen benötigen eine Möglichkeit für Clients und Server,
Zahlungsbedingungen über standardmäßige Web-Infrastruktur auszuhandeln, ohne
herkömmliche Web-Oberflächen zu beeinträchtigen. Der Mikrozahlungsbereich steckt
noch in den Kinderschuhen und neue Tools und Standards werden voraussichtlich
entstehen, wenn sich der Bereich weiterentwickelt. Das
[x402-Protokoll](https://www.x402.org/) hat sich jedoch als früher Standard mit
starker Unterstützung im Ökosystem etabliert. x402 verwendet den
[HTTP-Statuscode 402 „Payment Required“](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/402)
– ein Standard seit HTTP/1.1, der erst durch Blockchain-Abwicklung praktisch
umsetzbar wurde.

### So funktioniert es

Der Client sendet eine Anfrage, erhält eine 402-Antwort mit Zahlungsbedingungen
und wiederholt die Anfrage dann mit einer signierten Zahlung. Der Server
delegiert Verifizierung und Abwicklung an einen Fazilitator – eine optionale
Zwischeninstanz, die die Onchain-Transaktionsübermittlung übernimmt. Sobald der
Fazilitator die Abwicklung bestätigt, gibt der Server den angeforderten Inhalt
zurück. Diese Trennung ermöglicht es API-Anbietern, Zahlungen zu akzeptieren,
ohne Onchain-Infrastruktur direkt verwalten zu müssen.

![x402-Flussdiagramm](/assets/guides/x402/x402-flow-solana.png)

Das Protokoll ist zustandslos (keine Sitzungen oder API-Schlüssel), funktioniert
mit standardmäßiger HTTP-Infrastruktur und unterstützt jeden SPL-Token, den der
Server akzeptiert. Hier ist ein Ausschnitt, wie ein Express-Server mit
x402-Middleware eingerichtet wird:

```ts
app.use(
  paymentMiddleware(
    {
      "GET /costly-data": {
        accepts: [
          {
            scheme: "exact",
            price: "$0.001",
            network: "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
            payTo: svmAddress
          }
        ],
        description: "Costly data",
        mimeType: "application/json"
      }
    },
    new x402ResourceServer(facilitatorClient).register(
      "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
      new ExactSvmScheme()
    )
  )
);

app.get("/costly-data", (req, res) => {
  res.send({
    report: {
      data: "costly data"
    }
  });
});
```

Um mehr über x402 zu erfahren, lesen Sie unsere Leitfäden:

<Cards>
  <Card
    title="Einführung in x402"
    href="/developers/guides/getstarted/intro-to-x402"
    description="Protokollgrundlagen und SDK-Optionen für die Entwicklung zahlungsfähiger APIs."
  />
  <Card
    title="Mit Kora entwickeln"
    href="/developers/guides/getstarted/build-a-x402-facilitator"
    description="Implementieren Sie gasfreie x402-Zahlungen mithilfe der Signing-Infrastruktur von Kora."
  />
</Cards>

### x402 Tooling

| SDK                                                             | Beschreibung                        |
| --------------------------------------------------------------- | ----------------------------------- |
| [Corbits](https://corbits.dev/)                                 | Solana-first x402-Implementierung   |
| [MCPay.tech](https://mcpay.tech/)                               | Pay-per-Request für MCP-Server      |
| [PayAI](https://payai.network/)                                 | x402-Facilitator mit Solana-Support |
| [x402 GitHub](https://github.com/coinbase/x402)                 | Referenzimplementierung             |
| [ACK](https://github.com/agentcommercekit/ack)                  | Agent Commerce Kit                  |
| [A2A x402](https://github.com/google-agentic-commerce/a2a-x402) | Googles Agent-zu-Agent-Zahlungen    |
