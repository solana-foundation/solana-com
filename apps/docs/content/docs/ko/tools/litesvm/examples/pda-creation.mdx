---
title: PDA 생성
description: Program Derived Address 생성 및 상호작용 방법
---

## 전체 테스트

```rust title="tests/pda_test.rs"
use litesvm::LiteSVM;
use solana_account::Account;
use solana_sdk::pubkey::Pubkey;

#[test]
fn test_pda_creation() {
    let mut svm = LiteSVM::new();

    // Program that owns the PDA
    let program_id = Pubkey::new_unique();

    // Derive PDA address
    let seed = b"my_pda";
    let (pda, bump) = Pubkey::find_program_address(
        &[seed],
        &program_id
    );

    println!("PDA: {}", pda);
    println!("Bump: {}", bump);

    // Create PDA account manually (for testing)
    svm.set_account(pda, Account {
        lamports: 1_000_000,
        data: vec![bump], // Store bump seed in data
        owner: program_id,
        executable: false,
        rent_epoch: 0,
    }).unwrap();

    // Verify PDA was created
    let account = svm.get_account(&pda).unwrap();
    assert_eq!(account.owner, program_id);
    assert_eq!(account.data[0], bump);

    println!("PDA created successfully!");
}
```

## 핵심 포인트

1. **PDA 파생**: `Pubkey::find_program_address()`를 사용하여 PDA 주소를
   파생합니다
2. **Seeds**: PDA는 seed와 프로그램 ID로부터 파생됩니다
3. **Bump Seed**: bump seed는 주소가 ed25519 곡선 밖에 있도록 보장합니다
4. **계정 생성**: `set_account()`를 사용하여 테스트용 계정을 수동으로 생성합니다
5. **소유권**: PDA는 해당 PDA를 파생한 프로그램이 소유해야 합니다
