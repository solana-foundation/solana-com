---
title: 설치
description:
  환경을 설정하고 Private Channels TypeScript 클라이언트를 생성합니다
---

## 사전 요구 사항

- Rust 1.91+
- Solana CLI 3.1.13 (Agave):
  `sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.13/install)"`
- Node.js 24.7.0+
- pnpm 10.15.1+: `npm install -g pnpm@10.15.1`

## 저장소 복제

```bash
git clone https://github.com/solana-foundation/solana-private-channels.git
cd solana-private-channels
```

## TypeScript 클라이언트 생성

TypeScript 클라이언트는 npm에 **게시되지 않습니다**. Codama를 사용하여
IDL에서 직접 생성해야 합니다.

```bash
# Escrow program client
cd private-channel-escrow-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..

# Withdraw program client
cd private-channel-withdraw-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..
```

생성된 클라이언트의 경로:

- `private-channel-escrow-program/clients/typescript/src/generated/`
- `private-channel-withdraw-program/clients/typescript/src/generated/`

애플리케이션에서 이 로컬 경로를 통해 가져옵니다. npm에는
`@private-channels/client` 패키지가 없습니다. 이 섹션의 코드 예제는
상대 경로(`../private-channel-escrow-program/...`)를 사용하며, 코드가
복제된 저장소와 나란히 위치한다고 가정합니다. 실제 프로젝트에서
가져오는 위치에 맞게 경로를 조정하세요.

## Devnet 연결

```bash
solana config set --url devnet
solana airdrop 2
solana balance
```

- 에스크로 프로그램 ID: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- 출금 프로그램 ID: `J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi`

## 다음 단계

**[토큰 예치](/docs/tools/private-channels/quickstart/deposit)** - 첫 번째
예치를 진행합니다
