---
title: SetNewAdmin
description: Transfers admin rights to a new wallet
---

Transfers admin rights to a new wallet.

## Accounts

| Account                          | Writable | Signer | Description                        |
| -------------------------------- | -------- | ------ | ---------------------------------- |
| `payer`                          | ✓        | ✓      | Fee payer                          |
| `admin`                          |          | ✓      | Current admin (must sign)          |
| `new_admin`                      |          | ✓      | Wallet to receive admin rights     |
| `instance`                       | ✓        |        | Instance PDA (admin field updated) |
| `event_authority`                |          |        | Event authority PDA                |
| `private_channel_escrow_program` |          |        | Escrow Program                     |

This instruction takes no parameters.

## Important Notes

- **Irreversible in one step** - the current admin cannot reclaim the role
  without the new admin calling `SetNewAdmin` again
- Verify the target address before calling - there is no undo
- Emits an event via `EmitEvent` CPI
