---
title: Keychain
description: توقيع موحد لـ سولانا عبر خلفيات متعددة لإدارة المفاتيح
---

**توفر سولانا Keychain واجهة موحدة لتوقيع معاملات سولانا عبر منصات إدارة
المفاتيح المتعددة.** طوّر محلياً باستخدام مفتاح في الذاكرة، ثم انتقل إلى إدارة
المفاتيح على مستوى المؤسسات، أو وحدات HSM السحابية، أو خدمات المحافظ المُدارة في
بيئة الإنتاج — دون الحاجة إلى إعادة كتابة كود التوقيع الخاص بك.

### لماذا Keychain؟

- **واجهة موحدة**: سمة `SolanaSigner` واحدة تعمل عبر جميع المنصات
- **تبديل المنصات**: غيّر مزودي إدارة المفاتيح من خلال الإعدادات، لا الكود
- **تجريد بدون تكلفة**: تتضمن أعلام الميزات فقط ما تحتاجه
- **مُدقَّق**: مراجعة أمنية من قِبل [Accretion](https://accretion.xyz)

### البنية المعمارية

- **اللغات**: Rust + TypeScript، بتكافؤ كامل
- **السمة**: واجهة `SolanaSigner` موحدة
- **المنصات**: Memory، Vault، AWS KMS، GCP KMS، Privy، Turnkey، Fireblocks، CDP،
  Crossmint، Dfns، Openfort، Para، Utila
- **التوافق**: `@solana/kit` و `@solana/signers` متوافق (TypeScript) |
  `solana-sdk` و `solana-sdk-v3` متوافق (Rust)

### الخلفيات المدعومة

تمتد المنصات لتشمل بيئة التطوير المحلي، وإدارة المفاتيح الذاتية والسحابية،
وخدمات المحافظ المُدارة أو MPC. راجع
[اختيار منصة](/docs/tools/keychain/choosing-a-backend) لمقارنة نماذج الحضانة
واختيار المناسب منها.

| المنصة          | حالة الاستخدام              | Rust | TypeScript |
| --------------- | --------------------------- | ---- | ---------- |
| Memory          | التطوير والاختبار           | ✓    | ✓          |
| HashiCorp Vault | إدارة المفاتيح الذاتية      | ✓    | ✓          |
| AWS KMS         | السحابة الأصلية (AWS)       | ✓    | ✓          |
| GCP KMS         | السحابة الأصلية (GCP)       | ✓    | ✓          |
| Privy           | المحافظ المدمجة             | ✓    | ✓          |
| Turnkey         | إدارة المفاتيح غير الحضانية | ✓    | ✓          |
| Fireblocks      | الحضانة المؤسسية            | ✓    | ✓          |
| CDP             | منصة Coinbase للمطورين      | ✓    | ✓          |
| Crossmint       | محافظ Crossmint المُدارة    | ✓    | ✓          |
| Dfns            | بنية تحتية لمحافظ Dfns      | ✓    | ✓          |
| Openfort        | محافظ Openfort الخلفية      | ✓    | ✓          |
| Para            | محافظ Para MPC              | ✓    | ✓          |
| Utila           | حضانة Utila MPC             | ✓    | ✓          |

## البدء السريع

### Rust

تثبيت حزمة Rust:

```bash
cargo add solana-keychain
```

الاستخدام الأساسي:

```rust
use solana_keychain::{Signer, SolanaSigner};

// Create a signer from any backend
let signer = Signer::from_memory("base58_private_key")?;

// All signers share the same interface
let pubkey = signer.pubkey();
let signature = signer.sign_transaction(&mut tx).await?;
```

- [دليل Rust](/docs/tools/keychain/getting-started/rust) - التثبيت الكامل وإعداد
  الواجهة الخلفية
- [Crates.io](https://crates.io/crates/solana-keychain) - حزمة Rust

### TypeScript

تثبيت حزمة TypeScript:

```bash
pnpm add @solana/keychain
```

الاستخدام الأساسي:

```typescript
import { createKeychainSigner } from "@solana/keychain";
import { signTransactionWithSigners } from "@solana/signers";

// Create any signer via the unified factory
const signer = await createKeychainSigner({
  backend: "vault",
  vaultAddr: "https://vault.example.com:8200",
  vaultToken: "hvs.xxxxx",
  keyName: "my-solana-key",
  publicKey: "base58_public_key"
});

// Sign an already-compiled transaction
const signedTx = await signTransactionWithSigners(
  [signer],
  compiledTransaction
);
```

- [دليل TypeScript](/docs/tools/keychain/getting-started/typescript) - حزم
  `@solana/keychain`
- [حزمة npm](https://www.npmjs.com/package/@solana/keychain) - حزمة npm

## الخطوات التالية

<Cards>
  <Card title="دليل Rust" href="/docs/tools/keychain/getting-started/rust">
    تثبيت الحزمة وإعداد كل واجهة خلفية.
  </Card>
  <Card
    title="دليل TypeScript"
    href="/docs/tools/keychain/getting-started/typescript"
  >
    تثبيت الحزم وإعداد كل واجهة خلفية.
  </Card>
  <Card
    title="اختيار الواجهة الخلفية"
    href="/docs/tools/keychain/choosing-a-backend"
  >
    مقارنة نماذج الحفظ واختيار واجهة التوقيع المناسبة.
  </Card>
  <Card
    title="أفضل الممارسات في بيئة الإنتاج"
    href="/docs/tools/keychain/production-best-practices"
  >
    إدارة التوقيع بأمان: الفصل بين الساخن والبارد، والتدوير، والمراقبة.
  </Card>
  <Card title="إضافة موقّعين مخصصين" href="/docs/tools/keychain/adding-signers">
    دمج واجهة خلفية جديدة لإدارة المفاتيح.
  </Card>
</Cards>

## المصدر

- [مستودع GitHub](https://github.com/solana-foundation/solana-keychain)
- [سجل الإصدارات](https://github.com/solana-foundation/solana-keychain/releases)

تم بناؤه وصيانته بواسطة [مؤسسة سولانا](https://solana.org). مرخّص بموجب رخصة
MIT.
