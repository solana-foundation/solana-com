---
title: AddOperator
description: يُنشئ مشغّلاً جديداً عن طريق إنشاء PDA الخاص به
---

يُنشئ مشغّلاً جديداً عن طريق إنشاء `Operator` PDA الخاص به. بعد هذا الاستدعاء، يمكن لمحفظة
`operator` استدعاء `ReleaseFunds` و`ResetSmtRoot`.

## الحسابات

| الحساب                          | قابل للكتابة | موقِّع | الوصف                                                         |
| -------------------------------- | -------- | ------ | ------------------------------------------------------------------- |
| `payer`                          | ✓        | ✓      | دافع الرسوم                                                           |
| `admin`                          |          | ✓      | مسؤول النسخة                                                      |
| `instance`                       |          |        | PDA النسخة                                                        |
| `operator`                       |          |        | محفظة المشغّل المراد تفعيلها                                        |
| `operator_pda`                   | ✓        |        | PDA المشغّل - مشتق تلقائياً من `["operator", instance, operator]` |
| `system_program`                 |          |        | System Program                                                      |
| `event_authority`                |          |        | PDA سلطة الأحداث                                                 |
| `private_channel_escrow_program` |          |        | برنامج الضمان                                                      |

## المعاملات

| المعامل | النوع | الوصف                                                                               |
| --------- | ---- | ----------------------------------------------------------------------------------------- |
| `bump`    | `u8` | bump seed لـ`operator_pda`. اختياري: يشتقه العميل `Async` تلقائياً إذا لم يُذكر |

> يحتوي IDL على وسيطة `discriminator: u8` (القيمة `3`) يتم ضبطها تلقائياً
> بواسطة العميل المُولَّد ولا ينبغي تمريرها يدوياً.

## ملاحظات مهمة

- استخدم `getAddOperatorInstructionAsync`؛ إذ يشتق تلقائياً `bump` و
  `operator_pda` عند عدم توفيرهما. كما يتوفر متغير متزامن
  `getAddOperatorInstruction` إذا كنت قد حسبت `bump`
  بنفسك مسبقاً
- بعد هذا الاستدعاء، يمكن لمحفظة `operator` استدعاء `ReleaseFunds` و
  `ResetSmtRoot`
- يُصدر حدثاً عبر `EmitEvent` CPI
