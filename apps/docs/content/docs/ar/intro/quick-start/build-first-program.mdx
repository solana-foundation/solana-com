---
title: ابنِ برنامجك الأول
description:
  أنشئ برنامج Anchor محليًا، وابنِه واختبره، وتصفَّح كل ملف في برنامج العداد
  الافتراضي.
h1: ابنِ برنامجك الأول على سولانا
---

يستخدم هذا الدليل السريع مشروع البداية الخاص بـ
[إطار عمل Anchor](https://www.anchor-lang.com/docs) الذي يُنشئه `anchor init`.
ستقوم بإنشاء المشروع محليًا، وتشغيل اختباراته، وبناء البرنامج، والتجوُّل عبر كود
البرنامج الذي يُولِّده Anchor.

## المتطلبات الأساسية

قبل أن تبدأ، [ثبِّت أدوات تطوير سولانا](/docs/intro/installation). يشمل التثبيت
Rust وواجهة سطر أوامر سولانا وواجهة سطر أوامر Anchor.

استخدم الإصدار 1.1.2 أو أعلى من واجهة سطر أوامر Anchor لهذا القالب. تحقق من
الإصدار المثبت لديك:

```terminal
$ anchor --version
```

<Steps>

<Step>

### إنشاء المشروع

شغِّل الأوامر التالية في طرفيتك:

```terminal
$ anchor init my-program
$ cd my-program
```

يتضمن مشروع البداية برنامج سولانا واحدًا ضمن `programs/my-program`. يحتوي
البرنامج على تعليمتين: إحداهما لتهيئة حساب عداد والأخرى لزيادة قيمة العداد.

تُوضح بعض أجزاء القالب أنماطًا شائعة في برامج سولانا: اشتقاق عناوين حسابات
[PDA](/docs/core/pda)، وإجراء Cross Program Invocation ([CPI](/docs/core/cpi))
لنقل SOL، واستخدام فحوصات أخطاء مخصصة لإيقاف التعليمة عند فشل أحد الشروط.

<Files>
  <Folder name="my-program" defaultOpen>
    <File name="Anchor.toml" />
    <File name="Cargo.toml" />
    <Folder name="programs" defaultOpen>
      <Folder name="my-program" defaultOpen>
        <File name="Cargo.toml" />
        <Folder name="src" defaultOpen>
          <File name="lib.rs" />
          <File name="constants.rs" />
          <File name="error.rs" />
          <File name="instructions.rs" />
          <Folder name="instructions" defaultOpen>
            <File name="initialize.rs" />
            <File name="increment.rs" />
          </Folder>
          <File name="state.rs" />
        </Folder>
        <Folder name="tests" defaultOpen>
          <File name="test_initialize.rs" />
        </Folder>
      </Folder>
    </Folder>
  </Folder>
</Files>

</Step>

<Step>

### بناء البرنامج

شغِّل `anchor build` لتصريف برنامج البداية:

```terminal
$ anchor build
```

يُكتب البرنامج المُصرَّف إلى `target/deploy/my_program.so`. عند نشر البرنامج،
يُخزَّن محتوى ملف `.so` هذا في حساب على السلسلة.

</Step>

<Step>

### تشغيل الاختبار

شغّل الاختبار الافتراضي:

```terminal
$ anchor test
```

يستخدم `Anchor.toml` الخاص بهذا القالب أمر اختبار Rust:

```toml title="Anchor.toml"
skip_local_validator = true

[scripts]
test = "cargo test"
```

يقوم الاختبار بتحميل البرنامج المُجمَّع في [LiteSVM](https://www.litesvm.com/)،
وإنشاء دافع، وإرسال تعليمتَي _rs`initialize`_ و*rs`increment`*، ثم التحقق من
حالة حساب العداد.

يقوم تشغيل `anchor test` أيضاً بتجميع البرنامج، لذا لا تحتاج إلى تشغيل
`anchor build` أولاً عند الاختبار محلياً.

</Step>

<Step>

### نشر البرنامج

تُعدّ الاختبارات المحلية أسرع حلقة للتغذية الراجعة. عندما تكون مستعداً للنشر على
شبكة، مثل devnet، قم أولاً بالبناء ثم انشر على الكلاستر.

يتطلب نشر برنامج سولانا SOL لأن البرنامج مخزّن في program account، ويجب على
الحساب دفع تكلفة المساحة التي يستخدمها. على devnet، اطلب SOL مجاني من
[Solana Faucet](https://faucet.solana.com/) أو باستخدام Solana CLI:

```terminal
$ solana airdrop 2 --url devnet
```

```terminal
$ anchor build
$ anchor deploy --provider.cluster devnet
```

</Step>

</Steps>

## الملفات المصدرية

يحتوي دليل `src` على برنامج سولانا. تشرح وثائق
[هيكل البرنامج](https://www.anchor-lang.com/docs/basics/program-structure)
الخاصة بـ Anchor الماكرو الأساسية المستخدمة هنا، بما فيها _rs`declare_id!`_،
و*rs`#[program]`*، و*rs`#[derive(Accounts)]`*، و*rs`#[account]`*. يستعرض هذا
القسم ملفات القالب.

### lib.rs

`lib.rs` هو نقطة دخول البرنامج. يربط الملفات المصدرية، ويحدد عنوان البرنامج،
ويعرّف تعليمات البرنامج التي يمكن للمستخدمين استدعاؤها.

<WithNotes>

```rust title="programs/my-program/src/lib.rs" -w
// !tooltip[/pub mod constants/] constantsMod
pub mod constants;
// !tooltip[/pub mod error/] errorMod
pub mod error;
// !tooltip[/pub mod instructions/] instructionsMod
pub mod instructions;
// !tooltip[/pub mod state/] stateMod
pub mod state;

// !tooltip[/anchor_lang::prelude/] prelude
use anchor_lang::prelude::*;

pub use constants::*;
pub use instructions::*;
pub use state::*;

// !tooltip[/declare_id!/] declareId
declare_id!("82sFkffP9wxwpyfZyeaKHH2chQoJPUGsJZSPi9mrUuXd");

// !tooltip[/#\[program\]/] programMacro
#[program]
pub mod my_program {
    use super::*;

    // !tooltip[/pub fn initialize/] publicInstructions
    pub fn initialize(ctx: Context<Initialize>) -> Result<()> {
        // !tooltip[/handle_initialize/] handlers
        crate::instructions::initialize::handle_initialize(ctx)
    }

    // !tooltip[/pub fn increment/] publicInstructions
    pub fn increment(ctx: Context<Increment>) -> Result<()> {
        // !tooltip[/handle_increment/] handlers
        crate::instructions::increment::handle_increment(ctx)
    }
}
```

### !constantsMod

_rs`pub mod constants`_ يجعل `constants.rs` جزءًا من حزمة البرنامج. هذا هو
المكان الذي يحتفظ فيه القالب بالقيم المشتركة للبرنامج، مثل seed الـ PDA، ومبلغ
التحويل، والحد الأقصى لقيمة العداد.

### !errorMod

_rs`pub mod error`_ يجعل `error.rs` جزءًا من حزمة البرنامج. يُعرِّف هذا الملف
الأخطاء المخصصة التي يمكن للبرنامج إرجاعها عندما تكون التعليمة غير صالحة.

### !instructionsMod

_rs`pub mod instructions`_ يجعل `instructions.rs` جزءًا من حزمة البرنامج. يعمل
هذا الملف كفهرس للملفات الخاصة بالتعليمات في `src/instructions/`.

### !stateMod

_rs`pub mod state`_ يجعل `state.rs` جزءًا من حزمة البرنامج. يُعرِّف هذا الملف
تخطيط البيانات للحسابات المملوكة لهذا البرنامج، بما في ذلك حساب _rs`Counter`_.

### !prelude

_rs`anchor_lang::prelude::*`_ يستورد أنواع Anchor ووحداته الماكروية المستخدمة
لتعريف برنامج سولانا، بما في ذلك _rs`Context`_، و*rs`Result`*، و*rs`Pubkey`*،
و*rs`Account`*، و*rs`Signer`*، وسمات حسابات Anchor.

### !reexports

أسطر _rs`pub use`_ تجعل العناصر العامة من الوحدات الفرعية متاحةً من جذر الحزمة.
يُبقي ذلك نقاط دخول التعليمات مختصرة، مثل _rs`Context<Initialize>`_ بدلاً من
مسار وحدة أطول.

### !declareId

_rs`declare_id!`_ يُعرِّف عنوان البرنامج. يجب أن يتطابق مع العنوان في
`Anchor.toml`، وفي program account المنشور، وفي أي مستخدمين أو اختبارات تستدعي
البرنامج.

### !programMacro

_rs`#[program]`_ يُخبر Anchor بتوليد نقطة دخول برنامج سولانا وكود توزيع
التعليمات. تصبح الدوال العامة في هذه الوحدة هي التعليمات التي يمكن للمستخدمين
استدعاؤها.

### !publicInstructions

_rs`initialize`_ و*rs`increment`* هما اسما التعليمتين اللتين يمكن للمستخدمين
استدعاؤهما. تتلقى كل منهما _rs`Context<T>`_. يُعرِّف النوع داخل _rs`Context`_
الحسابات المطلوبة لتلك التعليمة، ويتحقق Anchor من صحة تلك الحسابات قبل تنفيذ
التعليمة.

### !handlers

تحتوي دالة المعالج على المنطق الخاص بالتعليمات.

</WithNotes>

<SideBySide>

### !left

```toml title="Anchor.toml" -w
[programs.localnet]
# !mark
my_program = "82sFkffP9wxwpyfZyeaKHH2chQoJPUGsJZSPi9mrUuXd"
```

### !right

```rust title="lib.rs" -w
// !mark
declare_id!("82sFkffP9wxwpyfZyeaKHH2chQoJPUGsJZSPi9mrUuXd");
```

</SideBySide>

تظهر نفس عنوان البرنامج في الإعدادات والكود. `Anchor.toml` يخبر Anchor بالعنوان
الذي سيتم نشره أو استدعاؤه على الشبكة. _rs`declare_id!`_ يُعرِّف عنوان البرنامج
داخل البرنامج نفسه لأغراض التحقق الأمني.

### constants.rs

يحتفظ `constants.rs` بالقيم المشتركة في مكان واحد. في هذا القالب، يشتق
_rs`COUNTER_SEED`_ عنوان PDA الخاص بالعداد، ويُنقل _rs`HELLO_WORLD_LAMPORTS`_
أثناء التهيئة، ويُفحص _rs`MAX_COUNT`_ قبل عملية الزيادة.

<WithNotes>

```rust title="programs/my-program/src/constants.rs" -w
use anchor_lang::prelude::*;

// !tooltip[/#\[constant\]/] anchorConstant
// !tooltip[/COUNTER_SEED/] counterSeed
#[constant]
pub const COUNTER_SEED: &[u8] = b"counter";

// !tooltip[/HELLO_WORLD_LAMPORTS/] helloLamports
#[constant]
pub const HELLO_WORLD_LAMPORTS: u64 = 1;

// !tooltip[/MAX_COUNT/] maxCount
#[constant]
pub const MAX_COUNT: u64 = 10;
```

### !anchorConstant

يخبر _rs`#[constant]`_ Anchor بتضمين القيمة في IDL الخاص بالبرنامج. وهذا يجعل
ثوابت البرنامج المهمة مرئية للعملاء والأدوات التي يتم توليدها تلقائياً.

### !counterSeed

_rs`COUNTER_SEED`_ هو السلسلة الثنائية المستخدمة لاشتقاق عنوان PDA الخاص
بالعداد. يستخدم كل من البرنامج والاختبار هذا الـ seed لكي يتفقا على عنوان الحساب
دون الحاجة إلى تخزين keypair منفصل. يستخدم هذا القالب seed ثابتاً واحداً، لذا
يمتلك البرنامج عنوان عداد واحداً. غالباً ما تُدرج البرامج الفعلية عنوان المستخدم
أو معرِّفاً آخر في الـ seeds لإنشاء حسابات مترابطة متعددة.

### !helloLamports

_rs`HELLO_WORLD_LAMPORTS`_ هو مقدار الـ lamport المنقول من الدافع إلى حساب
العداد داخل _rs`initialize`_. وهو صغير بشكل مقصود لأن هذا القالب يوضح استخدام
CPI للـ System Program.

### !maxCount

_rs`MAX_COUNT`_ هو الحد الأعلى الذي يفحصه _rs`increment`_. إبقاؤه هنا يمنح
البرنامج والاختبارات وIDL المولَّد اسماً مشتركاً واحداً لهذه القاعدة.

</WithNotes>

<SideBySide>

### !left

```rust title="initialize.rs" -w
#[derive(Accounts)]
pub struct Initialize<'info> {
    // ...
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        // !mark
        // !mention counter-seed
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    // ...
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    // ...

    // !mark
    // !mention hello-world-lamports
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    Ok(())
}
```

### !right

يستخدم `initialize.rs` ثابتَين:

- [_rs`COUNTER_SEED`_](mention:counter-seed) يستخرج عنوان PDA الخاص بالعداد.
- [_rs`HELLO_WORLD_LAMPORTS`_](mention:hello-world-lamports) يحدد المبلغ
  المحوَّل من الدافع إلى حساب العداد.

</SideBySide>

<SideBySide>

### !left

```rust title="increment.rs" -w
pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    // ...

    require!(
        // !mark
        // !mention max-count
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    Ok(())
}
```

### !right

يستخدم `increment.rs` الثابتَ [_rs`MAX_COUNT`_](mention:max-count) بوصفه الحدَّ
الأقصى للعداد. إذا كان العدد الحالي قد بلغ الحد الأقصى بالفعل، فإن
_rs`require!`_ يُعيد _rs`CounterOverflow`_ ولا يتغير بيانات الحساب.

</SideBySide>

### state.rs

يُعرِّف `state.rs` أنواع بيانات مخصصة للحسابات التي يُنشئها البرنامج ويمتلكها.
يُعرِّف البرنامج تعليمات لإنشاء تلك البيانات وتهيئتها وتحديثها، غير أن بيانات
العداد لا تُخزَّن داخل البرنامج نفسه، بل تُخزَّن في حساب منفصل له عنوانه الخاص.

<WithNotes>

```rust title="programs/my-program/src/state.rs" -w
use anchor_lang::prelude::*;

// !tooltip[/#\[account\]/] accountMacro
#[account]
// !tooltip[/InitSpace/] initSpace
#[derive(InitSpace)]
pub struct Counter {
    // !tooltip[/count/] count
    pub count: u64,
    // !tooltip[/authority/] authority
    pub authority: Pubkey,
}
```

### !accountMacro

يُخبر _rs`#[account]`_ برنامجَ Anchor بأن هذا الهيكل هو بيانات يمكن تخزينها في
حساب على السلسلة تمتلكه هذا البرنامج. يستخدمه Anchor لتسلسل البيانات وفك تسلسلها
والتحقق من صحة حسابات _rs`Counter`_.

### !initSpace

يتيح _rs`InitSpace`_ لبرنامج Anchor حساب _rs`Counter::INIT_SPACE`_، وهو عدد
البايتات اللازمة لحقول هذا الهيكل. يُضاف محدد حساب Anchor المكوَّن من 8 بايت
bseparately عند إنشاء الحساب.

### !count

_rs`count`_ هو قيمة العداد المخزَّنة داخل بيانات الحساب.

### !authority

يخزِّن _rs`authority`_ العنوانَ المسموح له بزيادة العداد. يكتب _rs`initialize`_
هذا العنوان، ويشترط _rs`increment`_ توقيع ذلك العنوان نفسه.

</WithNotes>

<SideBySide>

### !left

```rust title="initialize.rs" -w
#[account(
    init,
    payer = payer,
    // !mark
    // !mention counter-space
    space = 8 + Counter::INIT_SPACE,
    seeds = [COUNTER_SEED],
    bump
)]
pub counter: Account<'info, Counter>,
// ...

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    // !mark(1:2)
    // !mention(1:2) initialize-fields
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    Ok(())
}
```

```rust title="increment.rs" -w
pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    // ...

    // !mark
    // !mention increment-count
    ctx.accounts.counter.count += 1;
    Ok(())
}
```

### !right

`state.rs` يُعرِّف بيانات حساب _rs`Counter`_. تستخدم ملفات التعليمات هذا النوع
عند إنشاء الحساب وتحديثه:

- [_rs`Counter::INIT_SPACE`_](mention:counter-space) يُحدِّد حجم الحساب وفقاً
  للحقول المُعرَّفة في `state.rs`.
- [_rs`count`_ و _rs`authority`_](mention:initialize-fields) هي قيم الحقول
  المكتوبة عند تهيئة الحساب.
- [_rs`count += 1`_](mention:increment-count) يُحدِّث قيمة العداد المخزَّنة بعد
  اجتياز التحقق.

</SideBySide>

### error.rs

`error.rs` يُعرِّف الأخطاء المخصَّصة للبرنامج. في هذا القالب، توضِّح الأخطاء كيف
تتوقف معالجات التعليمات عندما لا يكون للمُستدعي صلاحية تحديث العداد، أو عندما
يكون العداد قد بلغ _rs`MAX_COUNT`_ بالفعل.

<WithNotes>

```rust title="programs/my-program/src/error.rs" -w
use anchor_lang::prelude::*;

// !tooltip[/#\[error_code\]/] errorCode
#[error_code]
pub enum ErrorCode {
    // !tooltip[/Unauthorized/] unauthorized
    #[msg("Only the counter authority can update this counter")]
    Unauthorized,
    // !tooltip[/CounterOverflow/] counterOverflow
    #[msg("Counter has reached the maximum value")]
    CounterOverflow,
}
```

### !errorCode

_rs`#[error_code]`_ يُخبر Anchor بتحويل هذه المتغيرات إلى أخطاء برنامج مخصَّصة.
يمكن للعملاء والاختبارات استخدام تلك الأخطاء بدلاً من التخمين من رموز الأخطاء
الخام.

### !unauthorized

_rs`Unauthorized`_ يُرجَع عندما لا يكون موقِّع المعاملة هو الجهة المخوَّلة
المخزَّنة في حساب العداد.

### !counterOverflow

_rs`CounterOverflow`_ يُرجَع عندما كان التزايد سيُخلّ بقاعدة _rs`MAX_COUNT`_
الخاصة بالبرنامج.

</WithNotes>

<SideBySide>

### !left

```rust title="increment.rs" -w
pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        // !mark
        // !mention unauthorized-error
        ErrorCode::Unauthorized,
    );
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        // !mark
        // !mention overflow-error
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

### !right

`error.rs` يُسمِّي الأخطاء التي يمكن أن تُرجعها التعليمة:

- [_rs`ErrorCode::Unauthorized`_](mention:unauthorized-error) يُرجَع عندما لا
  يكون الموقِّع هو الجهة المخوَّلة المخزَّنة في حساب العداد.
- [_rs`ErrorCode::CounterOverflow`_](mention:overflow-error) يُرجَع عندما يكون
  العداد قد بلغ _rs`MAX_COUNT`_ بالفعل.

</SideBySide>

### instructions.rs

`instructions.rs` يربط ملفات التعليمات بـ crate الخاص بالبرنامج حتى يتمكن
`lib.rs` من الوصول إلى كود تعليمات _rs`initialize`_ و*rs`increment`*. يُعرِّف كل
ملف تعليمات الحسابات المطلوبة لتلك التعليمة ومنطق المعالج الذي يعمل بعد أن يتحقق
Anchor من صحة تلك الحسابات.

<WithNotes>

```rust title="programs/my-program/src/instructions.rs" -w
// !tooltip[/pub mod initialize/] moduleDeclarations
pub mod initialize;
pub mod increment;

// !tooltip[/pub use initialize/] reexports
pub use initialize::*;
pub use increment::*;
```

### !moduleDeclarations

أسطر _rs`pub mod`_ تجعل `src/instructions/initialize.rs` و
`src/instructions/increment.rs` جزءًا من crate الخاص بالبرنامج.

### !reexports

أسطر _rs`pub use`_ تُعيد تصدير العناصر العامة من تلك الملفات. مما يجعل هياكل
حسابات التعليمات ودوال المعالج متاحةً من خلال وحدة _rs`instructions`_.

</WithNotes>

### initialize.rs

`initialize.rs` يُعرِّف الحسابات المطلوبة لإنشاء حساب العداد، ثم يكتب القيم
الأولية للحساب. تستخدم بنية _rs`#[derive(Accounts)]`_
[قيود الحساب](https://www.anchor-lang.com/docs/references/account-constraints)
الخاصة بـ Anchor لتحديد الحسابات المطلوبة وكيفية إنشاء حساب العداد الجديد.

<WithNotes>

```rust title="programs/my-program/src/instructions/initialize.rs" -w
use anchor_lang::prelude::*;

use crate::{constants::*, state::Counter};

// !tooltip[/derive\(Accounts\)/] accountsStruct
#[derive(Accounts)]
pub struct Initialize<'info> {
    // !tooltip[/#\[account\(mut\)\]/] payerMutable
    #[account(mut)]
    // !tooltip[/Signer<'info>/] payerSigner
    pub payer: Signer<'info>,
    // !tooltip[/#\[account\(/] counterConstraints
    #[account(
        // !tooltip[/init/] initConstraint
        init,
        // !tooltip[/payer = payer/] payerConstraint
        payer = payer,
        // !tooltip[/space = 8 \+ Counter::INIT_SPACE/] space
        space = 8 + Counter::INIT_SPACE,
        // !tooltip[/seeds = \[COUNTER_SEED\]/] seeds
        seeds = [COUNTER_SEED],
        // !tooltip[/bump/] bump
        bump
    )]
    // !tooltip[/Account<'info, Counter>/] counterAccountType
    pub counter: Account<'info, Counter>,
    // !tooltip[/Program<'info, System>/] systemProgram
    pub system_program: Program<'info, System>,
}

// !tooltip[/handle_initialize/] handler
pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    // !tooltip[/counter.count = 0/] initialCount
    ctx.accounts.counter.count = 0;
    // !tooltip[/counter.authority = ctx.accounts.payer.key\(\)/] initialAuthority
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    // !tooltip[/system_program::Transfer/] cpiAccounts
    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    // !tooltip[/CpiContext::new/] cpiContext
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    // !tooltip[/system_program::transfer/] transfer
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    // !tooltip[/msg!/] programLog
    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

<ScrollyCoding>

## !!steps Account Context

تُعرِّف بنية _rs`Initialize`_ الحسابات التي يجب تضمينها عندما يستدعي مستخدم
تعليمة _rs`initialize`_. يتحقق Anchor من هذه الحسابات قبل تشغيل المعالج.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
// !focus(1:2)
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}
```

## !!steps Payer Account

يدفع حساب _rs`payer`_ تكلفة إنشاء حساب العداد. يعني النوع _rs`Signer<'info>`_ أن
الدافع يجب أن يوقّع المعاملة، و _rs`#[account(mut)]`_ يعني أن حساب الدافع قابل
للتعديل لأنه سيتم خصم lamports منه.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    // !focus(1:2)
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}
```

## !!steps Counter Account

يخزّن حساب _rs`counter`_ بيانات _rs`Counter`_ من `state.rs`. _rs`init`_ يخبر
Anchor بإنشاء هذا الحساب قبل تشغيل المعالج، و _rs`payer = payer`_ يخبر Anchor
بالحساب الذي يدفع تكلفة الإنشاء.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    // !focus(1:8)
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}
```

## !!steps حجم الحساب

يخبر القيد _rs`space`_ Anchor بمقدار بيانات الحساب المراد تخصيصها. يخزّن Anchor
أولاً محدِّداً بحجم 8 بايت، ثم البايتات المطلوبة لحقول _rs`Counter`_. يتيح
المحدِّد لـ Anchor التعرف على هذا الحساب باعتباره حساب _rs`Counter`_ قبل إلغاء
تسلسل بيانات الحساب.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        // !focus
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}
```

## !!steps عنوان العداد

يحدد القيدان _rs`seeds`_ و _rs`bump`_ عنوان PDA المتوقع لحساب العداد. يتحقق
Anchor من أن حساب _rs`counter`_ المقدَّم يتطابق مع ذلك العنوان. يستخدم القالب
PDA حتى يتمكن المستخدمون من اشتقاق عنوان العداد من معرّف البرنامج وseed، مما
يجعل عنوان العداد حتمياً.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        // !focus(1:2)
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}
```

## !!steps System Program

حساب _rs`system_program`_ مطلوب لأن إنشاء حساب جديد يستخدم System Program.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    // !focus
    pub system_program: Program<'info, System>,
}
```

## !!steps دالة المعالج

تعمل الدالة _rs`handle_initialize`_ بعد أن يتحقق Anchor من صحة الحسابات في
_rs`Initialize`_. تمنح قيمة _rs`ctx`_ المعالجَ إمكانية الوصول إلى تلك الحسابات
المتحقق منها.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

// !focus
pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

## !!steps البيانات الأولية

يكتب المعالج القيم الأولى في حساب العداد الجديد. يبدأ العداد عند _rs`0`_، ويصبح
الدافع هو الجهة المخوّلة بزيادة العداد لاحقاً.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    // !focus(1:2)
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

## !!steps حسابات التحويل

يُضمَّن CPI الخاص بالتحويل هذا فقط لتوضيح كيفية تمرير CPI للحسابات إلى برنامج
آخر. تسرد بنية _rs`Transfer`_ الحسابات التي يستخدمها System Program في عملية
التحويل.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    // !focus(1:4)
    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

## !!steps سياق CPI

_rs`CpiContext::new`_ يجمع البرنامج المُستدعى مع الحسابات التي يتم تمريرها إلى
ذلك البرنامج. هذا هو الشكل الأساسي لـ CPI: اختر البرنامج المراد استدعاؤه، اجمع
الحسابات التي يتوقعها ذلك البرنامج، ثم مرّر كليهما إلى الاستدعاء. هنا، البرنامج
المُستدعى هو System Program.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    // !focus
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

## !!steps Invoke Transfer

_rs`anchor_lang::system_program::transfer`_ يستدعي تعليمة التحويل الخاصة بـ
System Program. في هذا القالب، يُعدّ التحويل مثالاً بسيطاً على استدعاء برنامج
آخر من برنامجك. إذا فشل CPI الخاص بالتحويل، فإن تعليمة _rs`initialize`_ تفشل
أيضاً.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    // !focus
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

## !!steps Log Message

_rs`msg!`_ يكتب رسالةً في سجلات البرنامج. _rs`Ok(())`_ يشير إلى أن التعليمة
أُنجزت بنجاح.

<CodePlaceholder title="initialize.rs" />

```rust !! title="initialize.rs"
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub system_program: Program<'info, System>,
}

pub fn handle_initialize(ctx: Context<Initialize>) -> Result<()> {
    ctx.accounts.counter.count = 0;
    ctx.accounts.counter.authority = ctx.accounts.payer.key();

    let cpi_accounts = anchor_lang::system_program::Transfer {
        from: ctx.accounts.payer.to_account_info(),
        to: ctx.accounts.counter.to_account_info(),
    };
    let cpi_ctx = CpiContext::new(anchor_lang::system_program::ID, cpi_accounts);
    anchor_lang::system_program::transfer(cpi_ctx, HELLO_WORLD_LAMPORTS)?;

    // !focus(1:2)
    msg!("Hello, world! Counter initialized");
    Ok(())
}
```

</ScrollyCoding>

### !imports

يستورد هذا الملف الثوابت المشتركة ونوع حساب _rs`Counter`_. تستخدم التعليمة تلك
التعريفات لاشتقاق PDA، وتحديد مبلغ التحويل، وتخصيص الحساب بحجم البيانات الصحيح.

### !accountsStruct

_rs`Initialize`_ هو سياق الحساب لتعليمة _rs`initialize`_. يمثّل كل حقل في البنية
حساباً منفصلاً يجب تضمينه عند استدعاء التعليمة. يستخدم Anchor البنية للتحقق من
صحة تلك الحسابات الواردة قبل تشغيل المعالج.

### !payerMutable

_rs`#[account(mut)]`_ يُخبر Anchor بأن حساب الدافع يجب أن يكون قابلاً للكتابة.
هذا مطلوب لأن إنشاء الحساب وCPI الخاص بالتحويل كلاهما يخصمان lamports من الدافع.

### !payerSigner

_rs`Signer<'info>`_ يُخبر Anchor بأن الدافع يجب أن يوقّع على المعاملة.

### !counterConstraints

يُخبر الكتلة _rs`#[account(...)]`_ Anchor بكيفية إنشاء حساب العداد والتحقق من
صحته قبل تشغيل المعالج.

### !initConstraint

_rs`init`_ يخبر Anchor بإنشاء حساب _rs`counter`_ قبل تشغيل المعالج. الحساب
الجديد مملوك لهذا البرنامج، لذا يمكن لهذا البرنامج تخزين بيانات _rs`Counter`_
فيه.

### !payerConstraint

_rs`payer = payer`_ يحدد حساب _rs`payer`_ بوصفه الجهة الدافعة لتكاليف إنشاء حساب
العداد الجديد.

### !space

_rs`space = 8 + Counter::INIT_SPACE`_ يخصص بايتات لمُميِّز حساب Anchor إضافةً
إلى حقول _rs`Counter`_ من `state.rs`. يساعد المُميِّز Anchor على التحقق من أن
الحساب يخزّن النوع المتوقع قبل قراءته.

### !seeds

_rs`seeds = [COUNTER_SEED]`_ يشترط أن يكون عنوان حساب العداد هو PDA المشتق من
هذا الـ seed ومعرّف البرنامج. مع seed ثابت واحد، يوجد PDA عداد واحد لهذا
البرنامج. لإنشاء عدادات منفصلة في برنامج حقيقي، عادةً ما تضيف seed آخر مثل عنوان
المستخدم.

### !bump

_rs`bump`_ يخبر Anchor بإيجاد bump الخاص بـ PDA والتحقق منه، وهو البايت الإضافي
الذي يجعل العنوان المشتق صالحاً كـ PDA.

### !counterAccountType

_rs`Account<'info, Counter>`_ يخبر Anchor بمعاملة بيانات الحساب باعتبارها من
النوع _rs`Counter`_ المأخوذ من `state.rs`. كما يتحقق Anchor من أن الحساب مملوك
لهذا البرنامج.

### !systemProgram

_rs`Program<'info, System>`_ يشترط على المُستدعي توفير System Program. يستخدمه
Anchor لقيد إنشاء حساب _rs`init`_. كما يستدعي CPI الخاص بالنقل في المعالج System
Program أيضاً.

### !handler

يعمل المعالج بعد أن يتحقق Anchor من الحسابات وينشئ حساب العداد، وبذلك يمكنه
كتابة البيانات الأولية للحساب بأمان.

### !initialCount

يضبط حقل _rs`count`_ في حساب العداد على القيمة _rs`0`_.

### !initialAuthority

يضبط حقل _rs`authority`_ في حساب العداد على عنوان الجهة الدافعة.

### !cpiAccounts

هذه هي الحسابات التي تُمرَّر إلى تعليمة التحويل الخاصة بـ System Program. يُدرج
تعليمة Cross Program Invocation هذه فقط لتوضيح كيفية تمرير CPI للحسابات إلى
برنامج آخر.

### !cpiContext

_rs`CpiContext`_ يُغلّف البرنامج المستهدف والحسابات المطلوبة لإجراء
cross-program invocation. يُعدّ إنشاء السياق لـ CPI عن طريق إقران البرنامج الذي
تريد استدعاءه بالحسابات التي يتوقعها ذلك البرنامج.

### !transfer

يستدعي هذا الـ CPI تعليمة التحويل الخاصة بـ System Program وينقل
_rs`HELLO_WORLD_LAMPORTS`_ من الدافع إلى حساب العداد.

### !programLog

_rs`msg!`_ يكتب رسالة إلى سجلات البرنامج عند استدعاء البرنامج.

</WithNotes>

### increment.rs

`increment.rs` يُعرّف الحسابات المطلوبة لتحديث حساب عداد موجود. يتحقق المعالج من
أن الموقّع هو الجهة المخوّلة المخزّنة، ويتحقق من أن العدد لم يبلغ الحد
_rs`MAX_COUNT`_ المحدد، ثم يزيد العدد.

<WithNotes>

```rust title="programs/my-program/src/instructions/increment.rs" -w
use anchor_lang::prelude::*;

use crate::{constants::*, error::ErrorCode, state::Counter};

// !tooltip[/derive\(Accounts\)/] incrementAccounts
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        // !tooltip[/mut/] counterMutable
        mut,
        // !tooltip[/seeds = \[COUNTER_SEED\]/] counterSeeds
        seeds = [COUNTER_SEED],
        // !tooltip[/bump/] counterBump
        bump
    )]
    // !tooltip[/Account<'info, Counter>/] incrementCounterAccountType
    pub counter: Account<'info, Counter>,
    // !tooltip[/Signer/] authoritySigner
    pub authority: Signer<'info>,
}

// !tooltip[/handle_increment/] incrementHandler
pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    // !tooltip[/require_keys_eq!/] authorityCheck
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    // !tooltip[/require!/] maxCheck
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    // !tooltip[/count \+= 1/] mutateCount
    ctx.accounts.counter.count += 1;
    // !tooltip[/msg!/] programLog
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

<ScrollyCoding>

## !!steps Account Context

يُعرّف هيكل _rs`Increment`_ الحسابات التي يجب تضمينها عندما يستدعي المستخدم
تعليمة _rs`increment`_. يتحقق Anchor من هذه الحسابات قبل تشغيل المعالج.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
// !focus(1:2)
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}
```

## !!steps Counter Account

يخزّن حساب _rs`counter`_ بيانات _rs`Counter`_. يتيح قيد _rs`mut`_ للمعالج تحديث
العدد المخزّن، ويتحقق قيدا _rs`seeds`_ و*rs`bump`* من عنوان PDA الخاص بالعداد.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    // !focus(1:6)
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}
```

## !!steps Authority Signer

يجب أن يوقّع حساب _rs`authority`_ على المعاملة. يتحقق المعالج لاحقاً من أن هذا
الموقّع يطابق الجهة المخوّلة المخزّنة في حساب العداد.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    // !focus
    pub authority: Signer<'info>,
}
```

## !!steps دالة المعالج

تعمل دالة _rs`handle_increment`_ بعد أن يتحقق Anchor من الحسابات في
_rs`Increment`_. تمنح قيمة _rs`ctx`_ المعالجَ إمكانية الوصول إلى تلك الحسابات
التي تم التحقق منها.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}

// !focus
pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

## !!steps التحقق من الصلاحية

يتحقق الفحص الأول من أن الموقِّع مخوَّل بتحديث هذا العداد. إذا كان عنوان
الموقِّع لا يتطابق مع _rs`counter.authority`_، تتوقف التعليمة مع
_rs`ErrorCode::Unauthorized`_. يوضح هذا التفويضَ على مستوى التطبيق: يمتلك
البرنامج بيانات العداد، لكنه ينفذ قاعدة تحدد أي موقِّع يمكنه تغيير تلك البيانات.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}

pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    // !focus(1:5)
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

## !!steps التحقق من الحد الأقصى للعداد

يمنع الفحص الثاني العداد من تجاوز _rs`MAX_COUNT`_. إذا كان العداد قد بلغ الحد
بالفعل، تتوقف التعليمة مع _rs`ErrorCode::CounterOverflow`_. هذا الحد قاعدة
اصطناعية في القالب تتيح لك رؤية كيف تُوقف الأخطاء المخصصة تنفيذ التعليمة قبل
تغيير بيانات الحساب.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}

pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    // !focus(1:4)
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

## !!steps تحديث العداد

فقط بعد اجتياز كلا الفحصين يقوم المعالج بتحديث بيانات الحساب. يضيف هذا السطر
واحداً إلى قيمة العداد المخزَّنة.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}

pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    // !focus
    ctx.accounts.counter.count += 1;
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

## !!steps رسالة السجل

تكتب _rs`msg!`_ العداد المحدَّث إلى سجلات البرنامج. تشير _rs`Ok(())`_ إلى أن
التعليمة أُعيدت بنجاح.

<CodePlaceholder title="increment.rs" />

```rust !! title="increment.rs"
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    pub counter: Account<'info, Counter>,
    pub authority: Signer<'info>,
}

pub fn handle_increment(ctx: Context<Increment>) -> Result<()> {
    require_keys_eq!(
        ctx.accounts.counter.authority,
        ctx.accounts.authority.key(),
        ErrorCode::Unauthorized,
    );
    require!(
        ctx.accounts.counter.count < MAX_COUNT,
        ErrorCode::CounterOverflow,
    );

    ctx.accounts.counter.count += 1;
    // !focus(1:2)
    msg!("Hello, world! Counter is now {}", ctx.accounts.counter.count);
    Ok(())
}
```

</ScrollyCoding>

### !incrementAccounts

_rs`Increment`_ هو سياق الحساب لتعليمة _rs`increment`_. يستخدمه Anchor للتحقق من
حساب العداد القابل للتعديل والموقِّع المطلوب.

### !counterMutable

_rs`mut`_ يُخبر Anchor بأن حساب العداد يجب أن يكون قابلاً للكتابة لأن المُعالج
يُحدّث قيمة _rs`count`_ المخزّنة.

### !counterSeeds

_rs`seeds = [COUNTER_SEED]`_ يتحقق من أن حساب العداد المُقدَّم هو عنوان PDA
المشتق من هذا seed ومعرّف البرنامج.

### !counterBump

_rs`bump`_ يُخبر Anchor بإيجاد والتحقق من bump الخاص بعنوان PDA لحساب العداد.

### !incrementCounterAccountType

_rs`Account<'info, Counter>`_ يُخبر Anchor بإلغاء تسلسل بيانات الحساب بوصفها نوع
_rs`Counter`_ من `state.rs`. كما يتحقق Anchor من أن الحساب مملوك لهذا البرنامج.

### !authoritySigner

_rs`Signer`_ يشترط توقيع الجهة المخوّلة. ثم يُقارن المُعالج عنوان هذا الموقّع
بالجهة المخوّلة المخزّنة في حساب العداد.

### !incrementHandler

يعمل المُعالج بعد أن يتحقق Anchor من قائمة الحسابات، ويتحقق من الملكية، ويُلغي
تسلسل بيانات حساب العداد.

### !authorityCheck

_rs`require_keys_eq!`_ يُقارن مفتاحين عامَّين ويوقف التعليمة ما لم يتطابق عنوان
الموقّع مع الجهة المخوّلة المخزّنة في حساب العداد. وهذه هي قاعدة التفويض على
مستوى التطبيق في هذا القالب.

### !maxCheck

_rs`require!`_ يتحقق من شرط منطقي ويوقف التعليمة بـ _rs`CounterOverflow`_ إذا
كان العداد قد بلغ _rs`MAX_COUNT`_ بالفعل. يوضح هذا القالب الأخطاء المخصصة والفشل
قبل التعديل.

### !mutateCount

فقط بعد اجتياز كلا الفحصَين يُحدّث المُعالج بيانات حساب العداد. إذا فشل أيٌّ من
الفحصَين، تفشل المعاملة ولا يتغير الحساب.

### !programLog

_rs`msg!`_ يكتب رسالة في سجلات البرنامج عند استدعاء البرنامج.

</WithNotes>

## ملف الاختبار

`programs/my-program/tests/test_initialize.rs` هو اختبار تكامل بلغة Rust. لا
يُشغّل validator محلياً. بدلاً من ذلك، يُحمّل ملف `.so` المُجمَّع في LiteSVM،
ويبني المعاملات التي تستدعي البرنامج، ويقرأ حساب العداد بعد كل معاملة. يبني
الاختبار التعليمات لمعاملة سولانا عن طريق تحديد معرّف البرنامج المراد استدعاؤه،
وتوفير instruction data، وتمرير الحسابات المطلوبة.

<SideBySide>

### !left

```rust title="initialize.rs" -w
#[derive(Accounts)]
pub struct Initialize<'info> {
    #[account(mut)]
    // !mark
    // !mention initialize-payer
    pub payer: Signer<'info>,
    #[account(
        init,
        payer = payer,
        space = 8 + Counter::INIT_SPACE,
        seeds = [COUNTER_SEED],
        bump
    )]
    // !mark
    // !mention initialize-counter
    pub counter: Account<'info, Counter>,
    // !mark
    // !mention initialize-system-program
    pub system_program: Program<'info, System>,
}
```

```rust title="test_initialize.rs" -w
let instruction = Instruction::new_with_bytes(
    program_id,
    // !mention initialize-instruction-data
    &my_program::instruction::Initialize {}.data(),
    my_program::accounts::Initialize {
        // !mark
        // !mention initialize-payer
        payer: payer.pubkey(),
        // !mark
        // !mention initialize-counter
        counter,
        // !mark
        // !mention initialize-system-program
        system_program: system_program::ID,
    }
    .to_account_metas(None),
);
```

### !right

سياق حساب _rs`Initialize`_ يحدد الحسابات المطلوبة من قِبَل تعليمة
_rs`initialize`_. يمرر الاختبار تلك الحسابات نفسها إلى المساعد
_rs`my_program::accounts::Initialize`_ المُولَّد:

- [_rs`payer`_](mention:initialize-payer) يُمرَّر بوصفه
  _rs`payer: payer.pubkey()`_.
- [_rs`counter`_](mention:initialize-counter) يُمرَّر بوصفه _rs`counter`_.
- [_rs`system_program`_](mention:initialize-system-program) يُمرَّر بوصفه
  _rs`system_program::ID`_.

[_rs`my_program::instruction::Initialize {}.data()`_](mention:initialize-instruction-data)
يُنشئ instruction data. هنا يتم ترميز وسيطات التعليمة، غير أن تعليمة
_rs`initialize`_ هذه لا تتطلب أي وسيطات.

</SideBySide>

<SideBySide>

### !left

```rust title="increment.rs" -w
#[derive(Accounts)]
pub struct Increment<'info> {
    #[account(
        mut,
        seeds = [COUNTER_SEED],
        bump
    )]
    // !mark
    // !mention increment-counter
    pub counter: Account<'info, Counter>,
    // !mark
    // !mention increment-authority
    pub authority: Signer<'info>,
}
```

```rust title="test_initialize.rs" -w
let instruction = Instruction::new_with_bytes(
    program_id,
    // !mention increment-instruction-data
    &my_program::instruction::Increment {}.data(),
    my_program::accounts::Increment {
        // !mark
        // !mention increment-counter
        counter,
        // !mark
        // !mention increment-authority
        authority: payer.pubkey(),
    }
    .to_account_metas(None),
);
```

### !right

سياق حساب _rs`Increment`_ يحدد الحسابات المطلوبة من قِبَل تعليمة
_rs`increment`_. يمرر الاختبار تلك الحسابات نفسها إلى المساعد
_rs`my_program::accounts::Increment`_ المُولَّد:

- [_rs`counter`_](mention:increment-counter) يُمرَّر بوصفه _rs`counter`_.
- [_rs`authority`_](mention:increment-authority) يُمرَّر بوصفه
  _rs`authority: payer.pubkey()`_.

[_rs`my_program::instruction::Increment {}.data()`_](mention:increment-instruction-data)
يُنشئ instruction data. هنا يتم ترميز وسيطات التعليمة، غير أن تعليمة
_rs`increment`_ هذه لا تتطلب أي وسيطات.

</SideBySide>

<WithNotes>

```rust title="programs/my-program/tests/test_initialize.rs" -w
use {
    anchor_lang::{
        prelude::Pubkey,
        solana_program::{instruction::Instruction, system_program},
        AccountDeserialize, InstructionData, ToAccountMetas,
    },
    // !tooltip[/litesvm::LiteSVM/] litesvm
    litesvm::LiteSVM,
    solana_keypair::Keypair,
    solana_message::{Message, VersionedMessage},
    solana_signer::Signer,
    solana_transaction::versioned::VersionedTransaction,
};

#[test]
fn test_initialize() {
    // !tooltip[/let program_id/] setup
    let program_id = my_program::id();
    let payer = Keypair::new();
    // !tooltip[/Pubkey::find_program_address/] counterPda
    let counter = Pubkey::find_program_address(
        &[my_program::constants::COUNTER_SEED],
        &program_id,
    )
    .0;
    let mut svm = LiteSVM::new();
    // !tooltip[/include_bytes!/] programBytes
    let bytes = include_bytes!(concat!(
        env!("CARGO_TARGET_TMPDIR"),
        "/../deploy/my_program.so"
    ));
    // !tooltip[/svm.add_program/] addProgram
    svm.add_program(program_id, bytes).unwrap();
    svm.airdrop(&payer.pubkey(), 1_000_000_000).unwrap();

    // !tooltip[/let instruction/] testInstruction
    let instruction = Instruction::new_with_bytes(
        program_id,
        &my_program::instruction::Initialize {}.data(),
        my_program::accounts::Initialize {
            payer: payer.pubkey(),
            counter,
            system_program: system_program::ID,
        }
        .to_account_metas(None),
    );

    // !tooltip[/svm.latest_blockhash/] blockhash
    let blockhash = svm.latest_blockhash();
    // !tooltip[/Message::new_with_blockhash/] message
    let msg = Message::new_with_blockhash(&[instruction], Some(&payer.pubkey()), &blockhash);
    // !tooltip[/VersionedTransaction::try_new/] transaction
    let tx = VersionedTransaction::try_new(VersionedMessage::Legacy(msg), &[&payer]).unwrap();

    // !tooltip[/svm.send_transaction/] sendTx
    let res = svm.send_transaction(tx);
    assert!(res.is_ok());

    // !tooltip[/svm.get_account/] getCounter
    let counter_account = svm.get_account(&counter).unwrap();
    let mut data: &[u8] = &counter_account.data;
    // !tooltip[/Counter::try_deserialize/] deserialize
    let counter_state = my_program::state::Counter::try_deserialize(&mut data).unwrap();
    // !tooltip[/assert_eq!\(counter_state.count, 0\)/] initAssert
    assert_eq!(counter_state.count, 0);
    assert_eq!(counter_state.authority, payer.pubkey());

    // !tooltip[/let instruction/] testInstruction
    let instruction = Instruction::new_with_bytes(
        program_id,
        &my_program::instruction::Increment {}.data(),
        my_program::accounts::Increment {
            counter,
            authority: payer.pubkey(),
        }
        .to_account_metas(None),
    );

    // !tooltip[/svm.latest_blockhash/] blockhash
    let blockhash = svm.latest_blockhash();
    // !tooltip[/Message::new_with_blockhash/] message
    let msg = Message::new_with_blockhash(&[instruction], Some(&payer.pubkey()), &blockhash);
    // !tooltip[/VersionedTransaction::try_new/] transaction
    let tx = VersionedTransaction::try_new(VersionedMessage::Legacy(msg), &[&payer]).unwrap();

    // !tooltip[/svm.send_transaction/] sendTx
    let res = svm.send_transaction(tx);
    assert!(res.is_ok());

    // !tooltip[/svm.get_account/] getCounter
    let counter_account = svm.get_account(&counter).unwrap();
    let mut data: &[u8] = &counter_account.data;
    // !tooltip[/Counter::try_deserialize/] deserialize
    let counter_state = my_program::state::Counter::try_deserialize(&mut data).unwrap();
    // !tooltip[/assert_eq!\(counter_state.count, 1\)/] incrementAssert
    assert_eq!(counter_state.count, 1);
    assert_eq!(counter_state.authority, payer.pubkey());
}
```

### !litesvm

_rs`LiteSVM`_ هو آلة افتراضية لسولانا تعمل داخل العملية وتُستخدم للاختبارات.
يتيح للاختبار تسجيل البرنامج المُترجَم، وإنشاء الحسابات، وإرسال المعاملات، وفحص
حالة الحسابات دون الحاجة إلى تشغيل عملية validator.

### !setup

يحصل الاختبار على عنوان البرنامج من قيمة _rs`declare_id!`_ عبر
_rs`my_program::id()`_ وينشئ keypair جديدًا للدافع لمعاملة الاختبار.

### !counterPda

عنوان العداد مشتق من نفس _rs`COUNTER_SEED`_ الذي يستخدمه البرنامج. هذا يجعل
الاختبار يستهدف PDA الذي يتوقعه `initialize.rs`.

### !programBytes

يقرأ _rs`include_bytes!`_ ملف `my_program.so` المُترجَم الذي ينتجه البناء. تلك
البايتات هي كود البرنامج القابل للتنفيذ الذي سيشغّله LiteSVM في الاختبار.

### !addProgram

يُسجّل _rs`svm.add_program`_ بايتات البرنامج المُترجَمة على عنوان البرنامج في
LiteSVM. يمنح الإسقاط الجوي الدافعَ ما يكفي من SOL المحلي لدفع رسوم المعاملة
وإنشاء الحساب.

### !testInstruction

يُنشئ _rs`Instruction::new_with_bytes`_ تعليمة لاستدعاء البرنامج. وتتضمن معرّف
البرنامج، وinstruction data، وحسابات التعليمة.

### !blockhash

كل معاملة سولانا تحتاج إلى blockhash حديث. في هذا الاختبار، يوفر LiteSVM
blockhash لاستخدامه في المعاملة.

### !message

تحتوي الرسالة على التعليمة، ودافع الرسوم، والـ blockhash. هذه هي بيانات المعاملة
التي سيتم توقيعها.

### !transaction

يوقّع _rs`VersionedTransaction::try_new`_ الرسالةَ باستخدام keypair الدافع.
يوقّع الدافع لأنه يدفع رسوم المعاملة ويموّل حساب العداد الجديد.

### !sendTx

يُنفّذ _rs`svm.send_transaction`_ المعاملة في LiteSVM.

### !getCounter

بعد تشغيل المعاملة، يقرأ الاختبار حساب العداد مباشرةً من LiteSVM حتى يتمكن من
فحص بيانات الحساب.

### !deserialize

يخزّن حساب العداد بيانات مُتسلسلة. يتحقق _rs`Counter::try_deserialize`_ من
مُميِّز حساب Anchor ويحوّل بيانات الحساب إلى nوع _rs`Counter`_ من `state.rs`.

### !initAssert

بعد نجاح _rs`initialize`_، يُجري الاختبار عملية إلغاء تسلسل لحساب العداد ويتحقق
من أن الحالة الابتدائية تطابق ما كتبه _rs`handle_initialize`_.

### !incrementAssert

بعد نجاح _rs`increment`_، يجب أن يكون لدى حساب العداد ذاته _rs`count`_ مضبوطاً
على _rs`1`_ مع الحفاظ على نفس الصلاحية.

</WithNotes>

## إعداد المشروع

تخبر ملفات المشروع الجذرية كلاً من Anchor وCargo بكيفية بناء البرنامج واختباره
ونشره. للاطلاع على مرجع كامل، راجع توثيق Anchor الخاص بـ
[إعداد Anchor.toml](https://www.anchor-lang.com/docs/references/anchor-toml)
و[Anchor CLI](https://www.anchor-lang.com/docs/references/cli).

<WithNotes>

```toml title="Anchor.toml"
# !tooltip[/skip_local_validator/] skipValidator
skip_local_validator = true

[toolchain]

[features]
resolution = true
skip-lint = false

# !tooltip[/programs.localnet/] programs
[programs.localnet]
my_program = "82sFkffP9wxwpyfZyeaKHH2chQoJPUGsJZSPi9mrUuXd"

# !tooltip[/provider/] provider
[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

# !tooltip[/scripts/] scripts
[scripts]
test = "cargo test"

[hooks]
```

### !skipValidator

`skip_local_validator = true` يخبر Anchor بعدم تشغيل validator لـ `anchor test`.
يستخدم اختبار Rust بدلاً من ذلك LiteSVM.

### !programs

`[programs.localnet]` يربط اسم حزمة Rust بعنوان program account المستخدم في
عمليات البناء والاختبار المحلية.

### !provider

`[provider]` يضبط الكتلة الافتراضية والمحفظة للأوامر مثل `anchor deploy`.

### !scripts

`[scripts]` يغيّر ما يُنفّذه `anchor test`. في هذا القالب، يقوم `anchor test`
bتشغيل `cargo test`.

</WithNotes>
