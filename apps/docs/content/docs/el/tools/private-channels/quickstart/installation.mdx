---
title: Εγκατάσταση
description:
  Ρυθμίστε το περιβάλλον σας και δημιουργήστε τα TypeScript clients για τα Private Channels
---

## Προαπαιτούμενα

- Rust 1.91+
- Solana CLI 3.1.13 (Agave):
  `sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.13/install)"`
- Node.js 24.7.0+
- pnpm 10.15.1+: `npm install -g pnpm@10.15.1`

## Κλωνοποίηση του Αποθετηρίου

```bash
git clone https://github.com/solana-foundation/solana-private-channels.git
cd solana-private-channels
```

## Δημιουργία TypeScript Clients

Τα TypeScript clients **δεν** δημοσιεύονται στο npm. Πρέπει να δημιουργηθούν από
το IDL χρησιμοποιώντας το Codama.

```bash
# Escrow program client
cd private-channel-escrow-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..

# Withdraw program client
cd private-channel-withdraw-program
pnpm install --frozen-lockfile
pnpm run generate-clients
cd ..
```

Τα δημιουργημένα clients τοποθετούνται στα:

- `private-channel-escrow-program/clients/typescript/src/generated/`
- `private-channel-withdraw-program/clients/typescript/src/generated/`

Κάντε εισαγωγή από αυτές τις τοπικές διαδρομές στην εφαρμογή σας· δεν υπάρχει
πακέτο `@private-channels/client` στο npm. Τα παραδείγματα κώδικα σε αυτή την ενότητα χρησιμοποιούν
σχετική διαδρομή (`../private-channel-escrow-program/...`) που προϋποθέτει ότι ο κώδικάς σας
βρίσκεται δίπλα στο κλωνοποιημένο αποθετήριο· προσαρμόστε την ανάλογα με τη θέση από την οποία
πραγματικά εισάγετε στο δικό σας έργο.

## Σύνδεση στο Devnet

```bash
solana config set --url devnet
solana airdrop 2
solana balance
```

- ID Προγράμματος Escrow: `9tgHa1DcnaSSUtmMsst8ovKTe1Gfxzezn27KnH9xXYeU`
- ID Προγράμματος Ανάληψης: `J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi`

## Επόμενα Βήματα

**[Κατάθεση Tokens](/docs/tools/private-channels/quickstart/deposit)** - πραγματοποιήστε
την πρώτη σας κατάθεση
