---
title: AddOperator
description: Παρέχει έναν νέο operator δημιουργώντας το Operator PDA του
---

Παρέχει έναν νέο operator δημιουργώντας το `Operator` PDA του. Μετά από αυτή την κλήση, το
πορτοφόλι `operator` μπορεί να καλέσει `ReleaseFunds` και `ResetSmtRoot`.

## Λογαριασμοί

| Λογαριασμός                          | Εγγράψιμο | Υπογράφων | Περιγραφή                                                         |
| -------------------------------- | -------- | ------ | ------------------------------------------------------------------- |
| `payer`                          | ✓        | ✓      | Πληρωτής τελών                                                           |
| `admin`                          |          | ✓      | Διαχειριστής instance                                                      |
| `instance`                       |          |        | Instance PDA                                                        |
| `operator`                       |          |        | Πορτοφόλι operator προς παροχή                                        |
| `operator_pda`                   | ✓        |        | Operator PDA - παράγεται αυτόματα από `["operator", instance, operator]` |
| `system_program`                 |          |        | System Program                                                      |
| `event_authority`                |          |        | PDA αρχής συμβάντων                                                 |
| `private_channel_escrow_program` |          |        | Escrow Program                                                      |

## Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή                                                                               |
| --------- | ---- | ----------------------------------------------------------------------------------------- |
| `bump`    | `u8` | Bump seed για το `operator_pda`. Προαιρετικό: ο client `Async` το παράγει αυτόματα αν παραλειφθεί |

> Το IDL περιέχει ένα όρισμα `discriminator: u8` (τιμή `3`) που ορίζεται αυτόματα
> από τον παραγόμενο client και δεν πρέπει να περνιέται χειροκίνητα.

## Σημαντικές Σημειώσεις

- Χρησιμοποιήστε το `getAddOperatorInstructionAsync`· παράγει αυτόματα το `bump` και το
  `operator_pda` όταν δεν παρέχονται. Υπάρχει επίσης μια σύγχρονη
  παραλλαγή `getAddOperatorInstruction` αν έχετε ήδη υπολογίσει το `bump`
  μόνοι σας
- Μετά από αυτή την κλήση, το πορτοφόλι `operator` μπορεί να καλέσει `ReleaseFunds` και
  `ResetSmtRoot`
- Εκπέμπει ένα συμβάν μέσω `EmitEvent` CPI
