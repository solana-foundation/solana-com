---
title: Ανάληψη Κεφαλαίων
description:
  Κάνει burn τα token της πλευράς του καναλιού για να ξεκινήσει μια ανάληψη πίσω στο escrow του Solana mainnet
---

Κάνει burn το υπόλοιπο token ενός χρήστη στην πλευρά του καναλιού στο Withdraw Program για να σηματοδοτήσει μια
εκκρεμή ανάληψη· ένας χειριστής πρέπει στη συνέχεια να καλέσει το `ReleaseFunds` στο
Escrow Program για να ολοκληρώσει τον διακανονισμό.

## Λογαριασμοί

| Λογαριασμός                    | Εγγράψιμο | Υπογράφων | Περιγραφή                                |
| -------------------------- | -------- | ------ | ------------------------------------------ |
| `user`                     |          | ✓      | Ο κάτοχος του token που ξεκινά την ανάληψη      |
| `mint`                     | ✓        |        | SPL token mint (έναντι του οποίου γίνεται το burn)            |
| `token_account`            | ✓        |        | token account του χρήστη στην πλευρά του καναλιού (πηγή) |
| `token_program`            |          |        | SPL Token Program                          |
| `associated_token_program` |          |        | Associated Token Program                   |

## Παράμετροι

| Παράμετρος     | Τύπος             | Περιγραφή                                                          |
| ------------- | ---------------- | -------------------------------------------------------------------- |
| `amount`      | `u64`            | Ποσό token προς ανάληψη, στη μικρότερη μονάδα του token           |
| `destination` | `Option<Pubkey>` | Πορτοφόλι προορισμού στο Mainnet· εάν είναι `None`, χρησιμοποιείται το πορτοφόλι του υπογράφοντα |

## Επιστρέφει

Επιστρέφει έναν `TransactionBuilder` που μπορεί να χρησιμοποιηθεί για τη δημιουργία και αποστολή της
συναλλαγής.

## Παράδειγμα

```typescript
import { getWithdrawFundsInstruction } from "../private-channel-withdraw-program/clients/typescript/src/generated";

const withdrawIx = getWithdrawFundsInstruction({
  user: userSigner,
  mint: mintAddress,
  tokenAccount: channelTokenAccount,
  tokenProgram: TOKEN_PROGRAM_ID,
  associatedTokenProgram: ASSOCIATED_TOKEN_PROGRAM_ID,
  amount: 1_000_000n,
  destination: null // null = release to signer's Mainnet wallet
});
```

## Σημαντικές Σημειώσεις

- Αυτή η εντολή βρίσκεται στο **Withdraw Program**
  (`J231K9UEpS4y4KAPwGc4gsMNCjKFRMYcQBcjVW7vBhVi` devnet), όχι στο Escrow
  Program
- Το burn token στην πλευρά του καναλιού **δεν** αποδεσμεύει αυτόματα κεφάλαια στο
  Mainnet· ένας χειριστής πρέπει στη συνέχεια να καλέσει το `ReleaseFunds` στο Escrow
  Program με έγκυρη απόδειξη αποκλεισμού SMT
- Το σφάλμα `ZeroAmount` εμφανίζεται εάν το `amount` είναι 0
- Εάν το `destination` είναι `None`, η κλήση `ReleaseFunds` του χειριστή θα αποδεσμεύσει
  κεφάλαια στο πορτοφόλι του υπογράφοντα

Για την πλήρη αναφορά κωδικών σφάλματος και για τα δύο προγράμματα, δείτε
[Instructions -> Error Codes](/docs/tools/private-channels/instructions#error-codes).
