---
title: Πληρωμές από agents
description:
  Επιτρέψτε στους AI agents να πληρώνουν για υπηρεσίες, APIs και πόρους αυτόνομα
  χρησιμοποιώντας το πρωτόκολλο x402.
---

Οι AI agents γίνονται όλο και πιο αυτόνομοι στις ροές εργασίας τους: γράφουν
κώδικα, ανακτούν δεδομένα, παρέχουν πόρους και πολλά άλλα. Για να λειτουργούν
πλήρως ανεξάρτητα, χρειάζονται έναν τρόπο να πραγματοποιούν «μικροπληρωμές» για
υπηρεσίες προγραμματιστικά, σε πραγματικό χρόνο, με ελάχιστο κόστος. Τα
παραδοσιακά συστήματα πληρωμών δεν λειτουργούν για μικροπληρωμές. Τα τέλη κάτω
του ενός σεντ και η οριστικοποίηση σε κλάσματα δευτερολέπτου του Solana
καθιστούν αυτό τεχνικά και οικονομικά εφικτό.

## Γιατί είναι σημαντικό;

Για παράδειγμα, φανταστείτε έναν developer που χρησιμοποιεί ένα LLM για να
δημιουργήσει μια εφαρμογή και χρειάζεται δεδομένα από ένα API επί πληρωμή. Για
να ανακτήσει κάποια σημαντικά δεδομένα, ένας developer θα έπρεπε να διακόψει τη
ροή εργασίας του, να ερευνήσει APIs, να επιλέξει έναν πάροχο, να δημιουργήσει
λογαριασμό, να προσθέσει μέθοδο πληρωμής, να δημιουργήσει ένα API key, να
αντιγράψει το key στο περιβάλλον του και στη συνέχεια να συνεχίσει τη ροή
εργασίας του. Αυτή η τριβή διακόπτει την κατάσταση ροής. Για ροές εργασίας
υποβοηθούμενες από AI, αποτελεί εμπόδιο.

Αντίθετα, με τις πληρωμές από agents, ο agent μπορεί να κάνει το αίτημα, να
πληρώσει για αυτό και να συνεχίσει τη ροή εργασίας του. Χωρίς λογαριασμούς.
Χωρίς API keys. Χωρίς χειροκίνητη παρέμβαση.

### Πληρώστε για APIs με το pay.sh

Το [pay.sh](https://pay.sh) είναι ένα επίπεδο πληρωμών για HTTP agents και
εργαλεία γραμμής εντολών. Ανατρέξτε στην
[τεκμηρίωση του pay.sh](https://pay.sh/docs) για να εγκαταστήσετε το `pay`. Το
CLI `pay` ενσωματώνει εργαλεία όπως τα `curl`, `codex` και `claude` ώστε να
μπορούν να καλούν APIs επί πληρωμή χωρίς λογαριασμούς παρόχων ή κλειδιά API.
Όταν ένα API επιστρέφει μια πρόκληση MPP ή x402 `402 Payment Required`, το `pay`
ζητά από το τοπικό σας πορτοφόλι να εγκρίνει την υπογραφή.

Μπορείτε να εγκαταστήσετε το Pay με:

```bash
brew install pay
```

ή

```bash
npm install -g @solana/pay
```

Επαληθεύστε την εγκατάσταση με:

```bash
pay --version
```

Η αλυσίδα εργαλείων θα ρυθμίσει τη διεύθυνσή σας αυτόματα. Εκτελέστε τα παρακάτω
για να ρυθμίσετε και να τροφοδοτήσετε το πορτοφόλι σας:

```bash
pay setup
```

## Το Πρωτόκολλο x402

Οι πληρωμές τύπου agentic χρειάζονται έναν τρόπο για να διαπραγματεύονται
clients και servers τους όρους πληρωμής μέσω τυπικής διαδικτυακής υποδομής χωρίς
να διαταράσσονται τα παραδοσιακά διαδικτυακά interfaces. Ο χώρος των
micropayments είναι ακόμα σε πρώιμο στάδιο και νέα εργαλεία και πρότυπα
αναμένεται να εμφανιστούν καθώς ο χώρος εξελίσσεται. Το
[πρωτόκολλο x402](https://www.x402.org/), ωστόσο, έχει αναδειχθεί ως ένα πρώιμο
πρότυπο με ισχυρή υποστήριξη από το οικοσύστημα. Το x402 χρησιμοποιεί τον κωδικό
κατάστασης
[HTTP 402 "Payment Required"](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/402)
— ένα πρότυπο από το HTTP/1.1 που έγινε πρακτικό μόνο με την εκκαθάριση μέσω
blockchain.

### Πώς λειτουργεί

Ο client υποβάλλει ένα αίτημα, λαμβάνει απόκριση 402 με τους όρους πληρωμής και
στη συνέχεια επαναποστέλλει με υπογεγραμμένη πληρωμή. Ο server αναθέτει την
επαλήθευση και την εκκαθάριση σε έναν διευκολυντή, έναν προαιρετικό ενδιάμεσο
που χειρίζεται την υποβολή της onchain συναλλαγής. Αφού ο διευκολυντής
επιβεβαιώσει την εκκαθάριση, ο server επιστρέφει το αιτούμενο περιεχόμενο. Αυτός
ο διαχωρισμός επιτρέπει στους παρόχους API να αποδέχονται πληρωμές χωρίς να
διαχειρίζονται άμεσα οποιαδήποτε onchain υποδομή.

![Διάγραμμα Ροής x402](/assets/guides/x402/x402-flow-solana.png)

Το πρωτόκολλο είναι stateless (χωρίς sessions ή API keys), λειτουργεί με τυπική
υποδομή HTTP και υποστηρίζει οποιοδήποτε SPL token αποδέχεται ο server.
Ακολουθεί ένα απόσπασμα για τη ρύθμιση ενός Express server με χρήση του
middleware x402:

```ts
app.use(
  paymentMiddleware(
    {
      "GET /costly-data": {
        accepts: [
          {
            scheme: "exact",
            price: "$0.001",
            network: "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
            payTo: svmAddress
          }
        ],
        description: "Costly data",
        mimeType: "application/json"
      }
    },
    new x402ResourceServer(facilitatorClient).register(
      "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
      new ExactSvmScheme()
    )
  )
);

app.get("/costly-data", (req, res) => {
  res.send({
    report: {
      data: "costly data"
    }
  });
});
```

Για να μάθετε περισσότερα σχετικά με το x402, δείτε τους οδηγούς μας:

<Cards>
  <Card
    title="Εισαγωγή στο x402"
    href="/developers/guides/getstarted/intro-to-x402"
    description="Θεμελιώδεις αρχές πρωτοκόλλου και επιλογές SDK για τη δημιουργία APIs με δυνατότητα πληρωμής."
  />
  <Card
    title="Δημιουργία με Kora"
    href="/developers/guides/getstarted/build-a-x402-facilitator"
    description="Υλοποίηση gasless πληρωμών x402 χρησιμοποιώντας την υποδομή υπογραφής της Kora."
  />
</Cards>

### Εργαλεία x402

| SDK                                                             | Περιγραφή                                  |
| --------------------------------------------------------------- | ------------------------------------------ |
| [Corbits](https://corbits.dev/)                                 | Υλοποίηση x402 με προτεραιότητα στο Solana |
| [MCPay.tech](https://mcpay.tech/)                               | Πληρωμή ανά αίτημα για διακομιστές MCP     |
| [PayAI](https://payai.network/)                                 | Διευκολυντής x402 με υποστήριξη Solana     |
| [x402 GitHub](https://github.com/coinbase/x402)                 | Υλοποίηση αναφοράς                         |
| [ACK](https://github.com/agentcommercekit/ack)                  | Agent Commerce Kit                         |
| [A2A x402](https://github.com/google-agentic-commerce/a2a-x402) | Πληρωμές agent-to-agent της Google         |
