{
  "name": "@workspace/docs-examples",
  "version": "0.0.0",
  "private": true,
  "description": "Tested code examples that the docs app transcludes via @catmcgee/remark-include-code. Every snippet rendered in apps/docs/content/cookbook is sourced from a real file here, exercised against a local Solana test validator on every CI run.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@solana-program/system": "^0.12.0",
    "@solana/kit": "^6.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "5.8.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
