---
title: Docskit
description: Usage examples
---

## Code Marks

Highlighting lines with default and custom colors:

```js
function lorem(ipsum, dolor = 1) {
  // !mark
  return dolor;
}

function ipsum(lorem, dolor = 1) {
  // !mark(1:2) gold
  const sit = lorem == null ? 0 : 1;
  dolor = sit - amet(dolor);
  return sit ? consectetur(lorem) : [];
}
```

Highlighting tokens using regex:

```js
// !mark[/sit/mg] #d946ef
function ipsum(lorem, dolor = 1) {
  // !mark[/lorem == null/]
  const sit = lorem == null ? 0 : 1;
  dolor = sit - amet(dolor);
  return sit ? consectetur(lorem) : [];
}
```

Highlighting diffs:

```js
function lorem(ipsum, dolor = 1) {
  const sit = 10;
  // !diff -
  dolor = ipsum - sit;
  // !diff +
  dolor = sit - amet(dolor);
  return sit ? consectetur(ipsum) : [];
}
```

## Word Wrap

```py title="main.py" -w
def lorem():
    return ipsum(dolor, sit) + amet + consectetur(adipiscing, elit) + consectetur(adipiscing, elit) + consectetur(adipiscing, elit) + lorem(ipsum, dolor)
```

## Callouts

<WithNotes>

How to use `lorem` from the `ipsum` package:

```js title="index.js"
// !callout[/ipsum/] description
import lorem from "ipsum";

// !callout[/sit/] inspect
const sit = lorem({ a: 1, b: 2 });
```

```js !inspect
sit = [
  { name: "a", value: 1 },
  { name: "b", value: 2 },
];
```

## Tooltips

<WithNotes>

Using `read` and `write` functions from the `lorem` package:

```js title="demo.js"
// !tooltip[/lorem/] install
import { read, write } from "lorem";

// !tooltip[/data.json/] data
var data = read("data.json");

// !tooltip[/test-123/] apikey
write({ x: 1 }, { apiKey: "test-123" });
```

We can also use tooltips [here](tooltip "install") in [prose](tooltip "data")
text.

```json !data title="data.json"
{
  "lorem": "ipsum dolor sit amet",
  "foo": [4, 8, 15, 16]
}
```

### !install

This is a **fake library**. You can install it with:

```terminal
$ npm install lorem
```

It lets you read and write data.

### !apikey

This is a public sample test mode [API key](https://example.com). Don’t submit
any personally information using this key.

Replace this with your secret key found on the
[API Keys page](https://example.com) in the dashboard.

</WithNotes>

## Collapse

Collapsing groups of lines:

```js title="index.js"
// !collapse(1:4)
function lorem(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : 1;
  dolor = sit - amet(dolor);
  return sit ? consectetur(ipsum) : [];
}

// !collapse(1:4) collapsed
function ipsum(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : 1;
  dolor = sit - amet(dolor);
  return sit ? consectetur(ipsum) : [];
}
```

Folding tokens using regex:

```jsx
// !fold[/className="(.*?)"/gm]
function Foo() {
  return (
    <div className="bg-red-200 opacity-50">
      <span className="block">hey</span>
    </div>
  );
}
```

## Inline Code

This is a variable in JavaScript: _`var x = 10`_.

In CSS it looks like this: _css`a { --x: 10 }`_.

And in Pascal it's _pascal`x := 10`_.

## Tabs

This is how you build a website:

<CodeTabs>

```html !! title="index.html"
<link rel="stylesheet" href="styles.css" />
<body>
  <h1>Hello, world!</h1>
  <script src="main.js"></script>
</body>
```

```css !! title="styles.css"
body {
  margin: 0;
  padding: 0;
}
```

```js !! title="main.js"
function main() {
  console.log("Hello, world!");
}

main();
```

</CodeTabs>

### !description

The default export of the `ipsum` package is a function that **returns an
array**.

Kind of like _`Object.entries(x)`_ but different.

</WithNotes>

## Mentions

<WithMentions>

The [base case](mention:one) returns 1.

```cpp
int factorial(int n) {
  if (n == 0) {
    // !mention one
    return 1;
  } else {
    // !mention two
    return n * factorial(n - 1);
  }
}
```

The [recursive case](mention:two) multiplies something.

</WithMentions>
