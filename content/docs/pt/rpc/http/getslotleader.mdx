---
title: getSlotLeader
hideTableOfContents: true
h1: Método RPC getSlotLeader
---

Retorna o líder do slot atual

<APIMethod>

```jsonc !!request curl
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getSlotLeader",
  "params": [
    // !hover(1:3) 0
    {
      // !hover commitment
      "commitment": "finalized"
    }
  ]
}
```

```ts !!request title="Kit"
import { createSolanaRpc } from "@solana/kit";

const rpc_url = "https://api.devnet.solana.com";
const rpc = createSolanaRpc(rpc_url);

let slotLeader = await rpc.getSlotLeader().send();

console.log(slotLeader);
```

```ts !!request title="web3.js"
import { Connection, clusterApiUrl } from "@solana/web3.js";

const connection = new Connection(clusterApiUrl("devnet"), "confirmed");

let slotLeader = await connection.getSlotLeader();

console.log(slotLeader);
```

### !params

#### !! 0

!type object

Objeto de configuração contendo os seguintes campos:

##### !! commitment

!type string

O commitment descreve quão finalizado está um bloco naquele momento. Veja
[Configurando State Commitment](/docs/rpc#configuring-state-commitment).

##### !! minContextSlot

!type number

O slot mínimo em que a solicitação pode ser avaliada

### !!result

```jsonc !response
{
  "jsonrpc": "2.0",
  // !hover result
  "result": "ENvAW7JScgYq6o4zKZwewtkzzJgDzuJAFxYasvmEQdpS",
  "id": 1
}
```

!type string

Pubkey de identidade do nó como string codificada em base-58

</APIMethod>
